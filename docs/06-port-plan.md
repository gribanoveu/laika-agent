# 6. План переноса: этапы и фичи

Рабочий чеклист переноса кода из Alfa Atlas в `atlas-desktop`. Что переносится и в каком
виде — в [04-reuse-map.md](04-reuse-map.md); чего нет и надо писать — в [05-gaps.md](05-gaps.md).
Здесь — порядок и нарезка.

## Исходная точка

`atlas-desktop` — пустой Tauri-каркас (`lib.rs` = шаблонный `greet`) плюс статическая
UI-оболочка (`Sidebar`/`ChatPanel`/`Composer`/`AsidePanel`, данные из `src/mock/data.ts`).
Бэкенда нет. В Alfa Atlas — ~79 000 строк Rust, из которых карта переноса насчитывает
~30 000 переносимых.

**Формат работы: по одной небольшой фиче за раз.** Каждая фича компилируется, проходит
тесты и заканчивается проверяемым «готово, когда». Ничего не переносится каталогами.

## Решения, принятые до начала

| Решение | Почему |
|---|---|
| **CLI не делаем.** Продукт desktop-first | Доки 01/02/03/05 написаны под два фронта и правятся в F-0.2 |
| **Отдельного крейта `agent-core` не заводим.** Порт идёт в `src-tauri/src/{domain,services,infra,commands}` | Во **всех** переносимых сервисах единственное упоминание `tauri::` — это `async_runtime::spawn`/`spawn_blocking` (`file_watcher.rs:52,101`, `index_watcher.rs:83,141`, `embedding_sync.rs:979`, `workspace_index.rs:102,775`), то есть пул потоков, который [AGENTS.md](../AGENTS.md) разрешает явно. `llm_chat.rs` не трогает `tauri::` вообще. Граница уже чистая; крейт только заставил бы переписать эти `spawn_blocking` и ничего не купил бы, раз второго фронта нет. **Триггер на пересмотр:** появился второй потребитель ядра — тогда крейт выделяется, код к этому готов |
| **Служебные строки — английский** | Оболочка уже английская; перевод делается в момент переноса файла, потом дороже (05-gaps §5.7.3) |
| **Индексный слой берём целиком, включая эмбеддинги** | Этап 5 |
| **UI-оболочка своя, из Atlas переносится только логика** | `chatTurnReducer.ts`, сборка блоков транскрипта. Компоненты Atlas — образец поведения, не копипаст. `src/mock/data.ts` заменяется обёртками команд, как написано в его же шапке |

## Метод

После **каждой** фичи: `cd src-tauri && cargo check`, затем `cargo test`. Инлайновые
`#[cfg(test)] mod tests` переносятся вместе с кодом и должны быть зелёными до перехода к
следующей фиче. Не идти дальше с красным деревом: «Atlas — не библиотека, модули видят
друг друга напрямую» (05-gaps §5.7.2) — скрытые связи вскрываются именно так.

Нарезка этапа 1 по одному инструменту — заодно живая проверка контракта CA-13.3:
добавление инструмента = вариант в двух enum'ах + модуль + ветка в `match`, и больше
нигде. Если фича потребовала правок в пятом месте — контракт сломан, это баг переноса.

---

## Этап 0. Подготовка

| Фича | Что делаем | Готово, когда | |
|---|---|---|---|
| **F-0.1** | Этот файл + строка в таблице [README.md](README.md) | Файл на месте, README ссылается | ✅ |
| **F-0.2** | Правка доков под desktop-first (ниже) | Упоминаний CLI как продуктовой сущности не осталось | ✅ |
| **F-0.3** | ~~Скелет `mod.rs`~~ → удалить шаблонный `greet` из `lib.rs` | `cargo check` зелёный | ✅ |
| **F-0.4** | `bunfig.toml` + `src/__tests__/setup/happydom.ts`; сверить `CLAUDE.md` с реальностью | `bun test` запускается | ✅ |

**F-0.3 ужался по дороге.** Планировался скелет из четырёх пустых `mod.rs`, но пустой
`domain/mod.rs` не делает ничего до F-1.1, который создаст его со строкой
`pub mod ai_access;` заодно с самим файлом. Осталось то, что имело смысл само по себе:
удаление шаблонного `greet`. Каталоги слоёв заводит та фича, которая кладёт в них первый
файл — вместе со строкой `mod …;` в `lib.rs`.

**F-0.4 завёл первый тест.** `bun test` без единого файла завершается ошибкой, так что
«готово, когда» требовало хотя бы одного. Взят `Dropdown` — он реализует контракт из
`AGENTS.md` (свой `role="listbox"` вместо `<select>`, закрытие по внешнему `pointerdown`
и по Escape), а обе отмены живут в `useEffect`, который работает только при открытом меню:
сломать их можно молча, меню продолжит открываться. Тест проверен мутацией — с вырезанным
обработчиком Escape падает.

**F-0.2 подробно.** Убрать CLI — затронуто шесть файлов, не четыре:

- `README.md` — заголовок «CLI-агент» → desktop, тезисы 1 и 2, таблица документов.
- `01-vision.md` §1.1 — «два равноправных фронта» → один десктопный; §1.3 убрать S-5
  (headless в CI) и stdin из S-4; §1.4 добавить CLI в осознанно не делаемое; §1.6
  принцип 2 (`clap` внутрь ядра) переписать под реальную раскладку.
- `02-requirements.md` — удалить блок **CA-10** целиком (7 требований) и CA-11.9.
- `03-architecture.md` §3.1 — схема двух фронтов → один; §3.2 раскладка `crates/` →
  раскладка `src-tauri/src/` плюс условие, при котором крейт всё-таки заводится;
  §3.3 п.2 (JSON-вывод) оставить как свойство протокола, убрать как требование;
  §3.6 «CLI и десктоп читают одну конфигурацию» → про права в репозитории.
- `04-reuse-map.md` §4.3 — обоснование «компакция должна быть в ядре» без ссылки на CLI.
- `05-gaps.md` — §5.4 (CLI-фронт) → §5.4 про Windows как платформу (материал оттуда же,
  он к CLI не сводился); §5.7 риск 1; §5.8 заменить ссылкой сюда.

**F-0.4 подробно.** `CLAUDE.md` уже описывает `bun test` с «~135 файлами», `bunfig.toml`,
`app.config.json` и `AI_HARNESS.md` — ничего из этого в репозитории нет (осталось от
копирования из docflow). Либо создать, либо убрать из `CLAUDE.md`. Чинится сейчас, а не
после первого теста.

**Зависимости** добавляются не пачкой, а той фичей, которой понадобились. Исключение —
комментарии из `Cargo.toml` Atlas: при переносе строки `keyring` (платформенные бэкенды
opt-in, иначе mock в памяти, молча теряющий секрет), `tokenizers` (без `esaxx_fast` —
иначе `LNK2038` на Windows), `libssh2-sys` (`openssl-on-win32`), `http` — комментарий
переносится вместе со строкой. Каждый описывает грабли, на которые иначе наступят повторно.

---

## Этап 1. Ядро: цикл, инструменты, провайдер, ключи

Цель этапа — цикл, который ходит в модель и умеет читать/править файлы, под тестами, без
UI. ~11 000 строк, разложенные на 14 фич. Порядок — по зависимостям.

| Фича | Модуль (путь в Alfa Atlas) | Правки при переносе |
|---|---|---|
| **F-1.1** ✅ | `domain/ai_access.rs` (646) → `domain/tools.rs` (278) | Не перенос, а вычленение — разбор ниже |
| **F-1.2** ✅ | `domain/ai_tools.rs` (1686) → `ToolScope` + `ToolError` | `ToolCall`/`ToolResult` **не здесь**: вариант enum'а не существует без своего типа аргументов, поэтому они рождаются в F-1.4 с одним вариантом и растут до F-1.8. Разбор ниже |
| **F-1.3** ✅ | `resolve.rs` (488) + `domain/paths.rs` (427) → `resolve.rs` (~200) | Единственная точка enforce'а корня (CA-3.9). Не «как есть» — разбор ниже |
| **F-1.4** ✅ | `tools/read_file.rs` (450) + `domain/flexible_args.rs` (430) + `ToolCall`/`ToolResult` + диспетчер | `text_diff` уехал в F-1.6, `outline` — на этап 5. Разбор ниже |
| **F-1.5a** ✅ | `infra/workspace_scanner.rs` (490) | Обходчик, общий для `grep`, `listFiles` и этапа 5. Три расхождения с оригиналом — ниже |
| **F-1.5b** ✅ | `tools/grep.rs` (363) + `services/docs_search.rs` (383) → один модуль | Разбор ниже |
| **F-1.5c** ✅ | `tools/list_files.rs` (709) → ~190 | Из четырёх веток осталась одна. Разбор ниже |
| **F-1.6a** ✅ | `services/text_diff.rs` (113) + `tools/write_file.rs` (197) + реестр чтений | CA-3.10 и «сначала прочитай» — один механизм. Разбор ниже |
| **F-1.6b** ✅ | `tools/edit_file.rs` (728) → ~130 | Ядро дословно, `fast_apply` не перенесён. Разбор ниже |
| **F-1.7** ✅ | `create_directory`, `delete_file`, `delete_directory`, `move_path` (845) → ~150 | Реестр чтений применяется несимметрично — разбор ниже |
| **F-1.8a** ✅ | `tools/todo.rs` (320) → ~110 | Исполнитель без состояния (CA-9.6). Разбор ниже |
| **F-1.8b** ✅ | `tools/git.rs` (461) + `git_ops` (257) + `git_repo` (4156) → ~290 написано заново | Разбор ниже |
| **F-1.9** ✅ | `parse.rs` (1437) → ~150; `scope.rs` (916) → **ноль** | Разбор ниже |
| **F-1.10a** ✅ | `domain/llm.rs` (1090) — проводная часть | Сообщения, запрос/ответ, usage, трейт провайдера, `sanitize_tool_call_arguments` |
| **F-1.10b** ✅ | → `domain/turn.rs` | Контрольная точка, проверка решений, поток событий, sink. Steering уехал в F-1.14 |
| **F-1.11** ✅ | `master_key.rs` (1043) + `secret_store.rs` (448) → ~700 с тестами; `key_management.rs` (323) **не берём** | Разбор ниже |
| **F-1.12a** ✅ | `infra/http_agent.rs` (169) | Агент и корпоративный CA. В карте переноса этого модуля нет |
| **F-1.12b** ✅ | `openai_compatible.rs` (1316) | Проводные типы, SSE, накопитель вызовов, `chat_stream`. Разбор ниже |
| **F-1.12c** ✅ | `llm_provider_manifest.rs` (263) → **ноль**; `settings_store.rs` (123), `llm_credentials_store.rs` (314), `llm_debug_log.rs` (212), `llm_session.rs` (214) → ~500 с тестами | Манифест не переносится: пресетов нет, и слой слияния исчезает вместе с ним. Разбор ниже |
| **F-1.13** ✅ | `domain/llm_rate_limit.rs` (885) + `services/llm_rate_limit.rs` (252) → **ноль**; вместо них `domain/llm_retry.rs` (~90) написан заново | Клиентская модель окна уходит вслед за манифестом; 429 в Atlas не обрабатывается вовсе. Разбор ниже |
| **F-1.14a** ✅ | `services/llm_chat.rs` (2826), правила раунда → ~110 | Бюджет, дедуп повторов, примечание об обрыве. Чистые функции, проверяются без модели |
| **F-1.14b** ✅ | схемы инструментов: `tools/*.rs` (`definition`) + `DEFINITIONS` | Того, что модель вообще видит, в переносе не было. Разбор ниже |
| **F-1.14c** ✅ | `run_tool_loop` (632–1187) + `stream`/`stream_resume` → ~400 | Сам цикл. Контрольная точка стала семиполевой — разбор ниже |
| **F-1.14d** ✅ | steering (отложено с F-1.10b) | Очередь заметок, отмена неушедшей. Источник заметки не переносится — разбор ниже |

**Не берём:** `tools/{check,asciidoc_templates,artifact,visualize}.rs` (~1274 строки).
`semantic_search.rs` — этап 5, `plans.rs` и `skill.rs` — этап 6, `conversation.rs` — как
образец на этапе 4.

**F-1.14d: steering. Место расширения вместо поля, которое некому заполнять.**

Пользователь печатает, пока turn идёт. Заметка попадает в историю **следующего** раунда с
префиксом «уточнение, а не новое задание» — без него «используй существующий хелпер» читается
как свежая постановка задачи, и модель начинает заново.

| Решение | Почему |
|---|---|
| Очередь **осушается**, а не читается | Заметка, отданная модели, обязана уйти из очереди тем же действием, иначе повторённый или прерванный раунд доставит её дважды |
| Заметка, пришедшая на последнем раунде, **продолжает** turn | Модель уже ответила, но пользователь что-то сказал. Завершить turn здесь — молча выбросить сказанное, и узнать об этом ему неоткуда |
| Заметки, оставшиеся от прошлого turn'а, в новый не попадают | Пользователь напечатал их разговору, который уже кончился; это их следующее сообщение, а не уточнение к тому, чего они не видят |
| Отмена по идентификатору, а `false` — «не успели» | Два одинаковых уточнения по тексту не различить. Сказанное модели вернуть нельзя, и ответ отличает «сняли» от «поздно» |
| `SteeringSource` **не переносится** | В Atlas источник различает реплику пользователя и служебную заметку приложения — нужен, потому что приложение шлёт свои подсказки (тот самый бэкстоп про диаграмму). Подсказок здесь нет, и вариант из одного значения — не точка расширения. Точка расширения — **место**, где заметка входит в историю, и оно одно |

**Мутации: пять, все пойманы.** Выброшенная поздняя заметка; утечка заметки из прошлого
turn'а; заметка без префикса; отмена, сносящая всю очередь; очередь, которая не осушается.

---

### Этап 1 закрыт

**Что проверено против собственного «готово, когда»:**

| Критерий | Чем закрыт |
|---|---|
| `cargo test` зелёный | 339 тестов |
| Turn читает файл, правит файл, получает `FileDiffStats` | `a_turn_reads_a_file_edits_it_and_is_told_what_changed` — модели уходят и счётчики строк, и сам дифф |
| Пауза и возобновление со **всеми** полями контрольной точки | `the_whole_checkpoint_survives_a_real_pause` — через настоящую паузу, а не сериализацией фикстуры: раунд, бюджет, `event_seq`, вызовы, todo, история и реестр чтений |

Полей в контрольной точке оказалось **семь**, а не шесть: реестр прочитанного — следствие
CA-3.10, которого в Atlas нет.

**F-1.14c: цикл. Семиполевая контрольная точка и две точки отмены.**

Ядро перенесено по смыслу, а не построчно: из `run_tool_loop` ушло всё, чего здесь нет —
режимы разговора и их фильтр инструментов, `askUser`/`requestArtifact` с результатом из
решения пользователя, расширение scope прямо посреди turn'а
(`requestFullRepoAccess`/`requestDependencySources`), `EmbeddingDeps`, журнал вызовов, учёт
лимитов. Осталось то, ради чего цикл существует: раунды, инструменты, пауза, возобновление,
отмена, повтор.

**Контрольная точка — семь полей, а не шесть.** Реестр прочитанного (F-1.6a) обязан пережить
паузу, иначе пауза сама по себе разрушительна: одобренная пользователем запись приходится на
файл, который возобновлённый turn уже не помнит прочитанным, и отклоняется ровно по той
причине, по которой её только что разрешили. У Atlas реестра нет, поэтому и полей шесть. Тест
на это — чтение в первом раунде, запись во втором, пауза, возобновление, файл записан.

| Решение | Почему так |
|---|---|
| `resume` принимает контрольную точку **целиком** | Это B-4 из findings, применённый к собственному коду: Atlas разбирает её на шесть параметров и полагается на фронт, который соберёт обратно; забытое поле там — молча обнулённый потолок, а не ошибка компиляции |
| Отмена и ожидание — функции в `Turn`, а не `AtomicBool` и `thread::sleep` | Ровно та же причина, по которой события идут через sink: цикл проверяется в тесте на полной скорости, и ни ожидание повтора, ни кнопка «стоп» не требуют настоящих часов |
| Две точки проверки отмены | Первая — вверху раунда (ловит «между раундами», «между вызовами внутри раунда» и «пока висела карточка подтверждения»), вторая — **сразу после** ответа модели, до проверки на подтверждение и до любого вызова. Вторая и делает так, что «стоп» перехватывает запись, о которой попросил раунд, а не только следующую фразу модели |
| Пауза — весь раунд, включая безобидные вызовы | Ничего не выполнено — значит, нет половины раунда, которую пришлось бы описывать тому, кто возобновляет |
| Возобновлённый раунд заново списывается с бюджета | Иначе пауза — способ купить бюджет. Проверяется не на слово: вторая пауза показывает тройной вес |
| `RoundCompleted` — до обеих развилок | Раунд, который сейчас отменят или поставят на паузу, уже сказал, что он сказал |
| Подтверждения не требует вызов, который не разбирается | Он всё равно не выполнится, и тратить на него внимание пользователя незачем |
| Отказ несёт причину | `Denied by the user: {reason}` — модель, которой сказали только «отклонено», повторяет тот же вызов, потом его близкий вариант |

**Повтор раунда** собран из F-1.13: `retry_delay` решает, ожидание режется на секундные срезы
с проверкой отмены, `Retrying` уходит до паузы, а флаг «эта попытка уже что-то отдала»
взводится в самих колбэках стрима — не по факту ошибки, а по факту первого байта.

**Двадцать один тест против скриптованного провайдера.** Провайдер отдаёт заготовленную
очередь раундов и записывает запросы, поэтому проверяется не только исход, но и то, **что
увидела модель**: результат инструмента в истории следующего раунда, свой же вызов перед
ответами на него, дерево вместо плоского JSON для листинга, причина отказа.

**Мутации: одиннадцать, все пойманы.** Снятая вторая точка отмены; пауза только когда подтверждения
требуют *все* вызовы; контрольная точка без реестра чтений; бесплатное возобновление;
забытый флаг «уже отдала байты»; ожидание, игнорирующее отмену; перенумерация событий с нуля
после паузы.

**F-1.14b: схемы инструментов. Пробел, который вскрылся на подходе к циклу.**

Фичи F-1.4…F-1.8 перенесли **реализации** инструментов, но не их схемы: `ToolCall` знает,
как разобрать аргументы, а того, что модель читает и по чему решает, чем вызвать, в проекте
не было вовсе. Цикл упирается в это сразу — `ChatRequest.tools` нечем заполнить.

Схема лежит **в модуле своего инструмента**, рядом с кодом, который её исполняет. Это не
вкусовщина, а то же правило CA-13.3: поле, добавленное в структуру аргументов без строчки в
схеме, — ошибка, о которой модель не может сообщить, потому что о поле ей не сказали. В
Atlas эти половины когда-то лежали в 1500 строках друг от друга, и модуль на это прямо
ссылается.

**Фильтра нет.** В Atlas `llm_tool_definitions(scope, mode)` отсеивает по двум осям:
проектный allowlist и режим разговора. Первая ось не переносится (см.
[07-upstream-findings.md](07-upstream-findings.md), B-2: список охраняет множество, где
разрешено всё; настоящий контроль — гейт подтверждений), вторая приедет с режимами на этапе
4 — и будет **фильтром** над этим списком, а не вторым списком.

**Описания переписаны под кодинг.** Оригинальные говорят о режиме «только документация»,
корне документации, шаблонах AsciiDoc и семантическом поиске — здесь ничего этого нет. Взамен
в описании каждого инструмента названы ловушки, которые иначе стоят раунда: диапазон строк
клампится, а не отвергается; пути из `grep`/`listFiles` вставляются обратно как есть; `write`
и `delete` откажут по непрочитанному файлу; якоря `editFile` обязаны быть уникальными, а
неудача не оставляет файл наполовину исправленным; `move` не чинит ссылки на старый путь;
`deleteDirectory` без `recursive` откажется от непустого каталога.

**Тест на расхождение — главное, ради чего таблица `DEFINITIONS` пара `(ToolName, схема)`, а
не просто список схем.** Имена аргументов не перечислены в тесте руками: берётся полностью
заполненный `ToolCall`, сериализуется, и ключи сравниваются с `properties` схемы **в обе
стороны**. Поле, появившееся в структуре, всплывает само; описанный аргумент, которого код не
принимает, — тоже. Отдельно проверяются вложенные `edits` и то, что минимальный вызов из
`required` действительно разбирается парсером.

**Мутации: пять, все пойманы.** Поле, выпавшее из схемы; инструмент, не попавший в
`DEFINITIONS`; проводное имя, разошедшееся с диспетчером; `required`, требующий поля, которого
инструмент не принимает; переименованное поле внутри `edits`.

**F-1.14a: правила раунда. Из 630 строк преамбулы осталось 110.**

Первые 630 строк `llm_chat.rs` — это почти целиком подсказки модели, специфичные для
документационного продукта: бэкстоп «просили схему, а ты не нарисовал» со словарём корней,
примечание о границе режима «только документация», примечание о неподключённых исходниках
зависимостей, список незакрытых макросов, автопроверка после записи с диагностиками и
разбором таблиц. Всё это не переносится вместе с инструментами, которых здесь нет.

Механизм — «бэкстоп-подсказка по эвристике над последним сообщением» — сохранён как точка
расширения: под кодинг он понадобится («правил код и ни разу не запустил тесты»). Сохранён
именно как место, а не как код: подсказки без своего инструмента бессмысленны.

Осталось три правила, каждое — след конкретной переписки:

| Правило | Что за ним |
|---|---|
| Взвешенный бюджет `MAX_TOOL_BUDGET` (250) + потолок раундов `MAX_TOOL_ITERATIONS` (60) | Второй — бэкстоп для первого: вес, ошибочно выставленный в ноль, иначе делает цикл неостановимым. Неизвестное имя инструмента стоит 1, а не 0, иначе модель, выдумывающая имена, крутится против бюджета, который не двигается |
| Дедуп повторного результата | Ключ — имя инструмента плюс **сырые** аргументы (другой диапазон строк или другой запрос — другой вызов), гейт — хэш самой полезной нагрузки. Поэтому после записи файл приходит заново полностью, а не подменяется примечанием. Подавляются только чтения и поиски: подтверждение записи — одна строка, ошибку стоит повторять столько раз, сколько она случается |
| Примечание об обрыве — только **упавшему** вызову | Успевший закрыться вызов выполнился нормально, и сказать ему «твой результат повреждён» хуже, чем промолчать. Флаг `failed` отдельным аргументом, а не выведенный из результата: preflight отвергает вызов **до** того, как результат вообще появится, а обрезанные аргументы падают именно там |

`FileList` под дедуп не заведён, хотя листинг каталога — тоже объёмный повторяемый ответ:
у апстрима это решение подкреплено переписками, а расширять его без такой же опоры я не стал.

**Мутации: пять, все пойманы.** Бесплатное неизвестное имя; ключ без аргументов; дедуп без
проверки хэша; примечание об обрыве успешному вызову; дедуп чего угодно, а не только чтений.

**F-1.13: лимиты. Переносить нечего, писать есть что.**

`llm_rate_limit` — это не «обработка 429», а клиентская модель окна **одного**
корпоративного шлюза: три счётчика (prompt, completion, запросы), скользящее окно в 20
минут, рабочие часы с часовым поясом, шкала со снимками для вкладки таймлайна. Правило
берётся `policy_for(preset)` из манифеста — а манифеста после F-1.12c нет, и `policy_for`
возвращала бы `NoopPolicy` всегда. 1137 строк ради no-op.

**Настоящий пробел нашёлся при проверке.** `grep` по всему Rust-дереву Atlas не находит ни
`429`, ни `Retry-After`: код, который так подробно моделирует лимит изнутри, не обрабатывает
отказ по лимиту снаружи. Против публичного API это и есть тот отказ, который случается —
длинный turn на 20 раундов упирается в 429, и без этого он просто падает.

Поэтому F-1.13 — не перенос, а `domain/llm_retry.rs`: одна чистая функция
`retry_delay(error, attempt, produced_output)` плюс вариант `LlmError::RateLimited` и
разбор `Retry-After` в провайдере.

| Правило | Что за ним |
|---|---|
| Раунд, **что-то уже отдавший**, не повторяется никогда | Обобщение `attempt_emitted_data` из `llm_chat`: если дельта текста или собираемый вызов дошли до вызывающего, повтор допишет текст второй раз и заново запустит то, что первая попытка уже привела в движение. В Atlas это правило действует только для оборванного соединения; здесь — для всех повторяемых классов |
| Подсказка сервера побеждает расчёт | Только сервер знает, когда окно откроется. Угадать короче — получить второй отказ ценой одной из немногих попыток |
| Ожидание дольше двух минут — **отказ с объяснением**, а не сон | Turn иначе на минуты замирает без признаков жизни, и отличить длинное окно от зависшего приложения нельзя |
| Без подсказки — 1, 2, 4, 8, 16 секунд | Долбить сервер ровным интервалом — способ продлить лимит |
| Обрыв до первого байта — фиксированные 10 секунд | Число из Atlas: так ведёт себя прокси, закрывающий простаивающее соединение, и рост задержки ничего не меняет |
| Всё остальное (400, 401, таймаут, TLS) не повторяется | Это обдуманный ответ провайдера; тот же запрос даст тот же ответ, повтор только отложит ошибку |

**Ожидание остаётся в цикле turn'а**, а не здесь: только цикл может держать паузу
прерываемой и сказать интерфейсу, почему ничего не происходит (`ChatEventPayload::Retrying`
с `delay_seconds` уже есть с F-1.10b). Здесь — решение, у которого есть правила, которые
стоит проверять.

**Мутации: семь, две промахнулись — и обе были полезны.** Одна не скомпилировалась (пустой
вывод легко принять за выжившую — проверять код возврата, а не только текст); вторая попала
на путь, которого не было ни в одном тесте: `Retry-After` **присутствует, но не читается**.
Единственный тест про отсутствие подсказки в эту ветку не заходит — `?` срезает раньше.
Добавлен тест на HTTP-дату: нечитаемая подсказка должна читаться как «подсказки нет», а не
как ноль, иначе повтор уходит обратно в тот же отказ немедленно.

**Не разбирается форма HTTP-даты** в `Retry-After`. Спецификация её допускает, ни один из
шлюзов её не шлёт, а нечитаемая подсказка корректно вырождается в расчётную задержку.

**Триггер на возврат модели окна:** появился провайдер, который отказывает **молча** или
без 429 — тогда считать расход на стороне клиента снова единственный способ предупредить
пользователя заранее. До тех пор сервер сам говорит, когда возвращаться.

**F-1.12c: настройки, ключи, разрешение провайдера. Манифест не переносится.**

`llm_provider_manifest.rs` (263) существует ради одного: доставить корпоративный пресет
(`AlfaGen` — адрес, лимиты, внутренний CA), который форк правит в YAML, не трогая `.rs`.
Здесь такого провайдера нет, и «заменить пресеты» нечем: пользователь настраивает свой
эндпойнт сам. Модуль выброшен целиком — вместе с `serde_yaml`, встроенным ассетом и, что
важнее, **со всем слоем слияния**: в Atlas одна запись провайдера — это три типа
(`LlmProviderPreset` + `LlmProviderConfig` из настроек + слитый `ResolvedLlmProvider`) и
~130 строк `resolve_provider`, где каждое поле — `over.or(preset)`. Без пресетов остаётся
один тип, `base_url` становится обязательной строкой вместо `Option`, а разрешение
провайдера — поиском по id.

**Триггер на возврат:** появился провайдер, который приложение обязано знать из коробки
(корпоративный шлюз со своим CA). Тогда возвращается не манифест целиком, а один слой
слияния — и вот тогда `Option` на полях перестаёт быть лишним.

| Решение | Почему |
|---|---|
| `infra/app_dir.rs` — общий каталог `~/.atlas-desktop` и **общий тестовый шов** | В Atlas это `$HOME`, подменяемый на время теста, с блокировкой, которую обязан брать каждый модуль, иначе два теста меняют процесс-глобал одновременно. Здесь `dir()` под `cfg(test)` не смотрит на настоящий дом вообще: тест, забывший установить каталог, получает ошибку, а не чужие ключи. `master_key` переехал на этот же шов, его собственный `SERIAL` исчез |
| Запись — во временный файл плюс `rename`, права `0600` | Запись на месте оставляет после падения либо непарсящийся `settings.json`, либо запечатанный блоб с несовпадающей меткой аутентичности — а во втором случае ключи не «нечитаемы», их больше нет |
| `settings.json`, который не парсится, — **ошибка**, а не молчаливый откат к умолчаниям | Откат означает, что следующее сохранение затрёт конфигурацию пользователя целиком: один лишний символ — и провайдеров нет |
| Кэш `configured_ids` (stat + mtime) из `llm_credentials_store` не перенесён | Он существует, потому что `has_api_key` дёргается по разу на провайдера при каждом обновлении вкладки настроек. Вкладки пока нет. Вместе с кэшем не перенесён и его самый неприятный режим: ответ «ключей нет», полученный при недоступной связке, оставался закэшированным до перезапуска |
| Провайдер строится заново на каждый `resolve`, слота-кэша нет | Ход turn'а разрешает провайдера один раз и переиспользует для всех раундов, а кэш, ключ которого — конфигурация и отпечаток ключа, это ровно та конструкция, которая потом отдаёт старого клиента после ротации ключа. **Триггер:** профиль показал, что сборка агента заметна |
| `chat_once` не перенесён | В Atlas он собирает трёх потребителей (компакция, выжимка памяти); здесь потребителей ноль. Приедет с F-4.1 |

**Ключ на диске.** `secret_store` из F-1.11 наконец получил своего пользователя:
`llm_credentials.enc` — один запечатанный блоб `id провайдера → ключ`, и
`settings.json` рядом физически некуда его записать. Тест на это отдельный: сериализованная
запись провайдера не содержит подстроки `key` — не косметика, а то, что ломается первым,
если кто-нибудь решит «временно» положить ключ в настройки.

Разобран и **сохранён** режим, который выглядит багом: при недоступном мастер-ключе
`load_all` отдаёт пустую карту, и запись нового ключа, казалось бы, затирает остальные.
Затирания не происходит — запечатывание требует того же ключа, что и открытие, поэтому
запись падает раньше. Строгая загрузка на запись сделала бы хуже: при повреждённом блобе
пользователь не смог бы сохранить ни одного нового ключа, не удалив файл руками.

**Отладочный лог (CA-12.2).** Выключен по умолчанию — в переписке лежит содержимое всех
прочитанных агентом файлов. Ключ в него не попадает **по устройству типов**: в записи едут
`ChatRequest`/`ChatStreamResult`/текст ошибки, а ключ живёт внутри провайдера и уходит
заголовком `Authorization`, которого доменные типы не видят. Логировать HTTP-слой вместо
типов — это и есть способ положить ключ в файл.

**Мутации: семь, одна выжила — и это была настоящая дыра.** Убиты: молчаливый откат при
битом `settings.json`; закреп на удалённого провайдера, проваливающийся на первого в
списке; потерянная обратная запись найденной модели; сохранение ключа, затирающее
остальные; снятый гейт отладочного лога; запись учёток открытым текстом. **Выжила** —
подмешивание постороннего значения в полезную нагрузку лога: сторож формата вызывал
`append` напрямую, то есть охранял тестовую сборку записи, а не `log_request`, который
как раз и правят. Тест переписан через публичную точку входа, мутация после этого
падает.

**Отложено с явным адресом:** `ProviderTokenLimit` (кольцо контекста и триггер компакции —
этап 4), `known_models`/`label` (списки и подписи в настройках — этап 3).

**F-1.12b: провайдер. Пять решений, каждое — след конкретного сервера.**

| Решение | Что стоит за ним |
|---|---|
| `chat()` реализован **через** `chat_stream` | Несколько корпоративных прокси отвергают нестриминговый `/chat/completions` с HTTP 400 (иногда с пустым телом), принимая ту же нагрузку с `"stream": true` |
| Пустой `content` — не дельта | Открывающий чанк почти всегда role-only с `"content": ""`. Принять его за дельту значит открыть пустой блок ответа, за которым прячется индикатор набора |
| `finish_reason` **защёлкивается**, а не присваивается | Ровно один чанк потока несёт его, и приходящий следом usage-only чанк с `None` не должен стирать сказанное |
| `\r` срезается | `lines()` снимает только `\n`. На сервере с CRLF `[DONE]` никогда не совпадёт, и поток дочитывается до EOF |
| Фрагмент без `index` — собственная запись, а не приклейка к последней | Ошибочная склейка портит аргументы **чужого**, ещё собирающегося вызова посторонним текстом. Лишний битый вызов просто не разберётся и вернётся модели как исправимая ошибка — это несравнимо дешевле |
| `pending:{index}` для вызова без id | Несколько прокси придерживают `id` до последнего фрагмента; пропускать такие снимки — значит показывать длинную запись как зависший поток |
| `Zeroizing` на заголовке `Authorization` | `ureq` хочет `&str`, поэтому ключ неизбежно существует форматированной копией на время вызова. `Zeroizing` не даёт этой копии остаться в куче после освобождения |

**Тесты идут против настоящего сокета**, а не только против строк: `TcpListener` на
случайном порту отдаёт заготовленный SSE-поток. Так проверяются сборка текста, накопление
вызовов, отмена посередине, защёлкивание `truncated` и чтение тела ошибки — то есть сам
цикл, а не его парсер.

**Проверено мутациями** (четыре): приклейка фрагмента без `index`; присваивание вместо
защёлкивания `finish_reason`; пустой `content` как дельта (роняет 2 теста); нечитаемое тело
ошибки.

**Мелочь, стоившая времени:** `timeout` на macOS нет, и `timeout 180 cargo test` молча дал
пустой вывод, который легко принять за зависший тест. Проверять выход команды, а не только
её вывод.

**F-1.12a: `http_agent` — третий модуль, которого нет в карте переноса.** Карта называет
`build_agent(trusted_cert_pem)` деталью внутри провайдера; на деле это отдельный модуль на
169 строк, и в нём две вещи, каждая из которых выглядит упрощаемой и не является ею:

| Решение | Что ломается при «упрощении» |
|---|---|
| `http_status_as_error(false)` | Умолчание `ureq` превращает не-2xx в ошибку **до** того, как вызывающий прочитал тело. Отказ провайдера приезжает как недиагностируемое «http status: 400», а объяснение остаётся непрочитанным в теле |
| Разбираются **все** сертификаты PEM, а не первый | `Certificate::from_pem` документированно берёт только первый. Корпоративный CA выдают цепочкой — корень плюс промежуточные; доверить корню и молча отбросить промежуточный, который и подписывает сервер, значит падать на рукопожатии, далеко от места ошибки |
| Свои сертификаты **заменяют** доверенные, а не дополняют | У каждого провайдера свой агент, поэтому сузить доверие одному нельзя повлиять на другого |
| Пустой список сертификатов — ошибка | Иначе агент молча остаётся на публичных корнях и падает на рукопожатии, ничем не указывая на вставленный текст |

Тесты идут против настоящих самоподписанных сертификатов (сгенерированы `openssl`, выданы
ничем, никуда не подключаются) — иначе проверялся бы разбор придуманного текста, а не PEM.

**Что мутациями не покрыто:** возврат `http_status_as_error` к умолчанию ничего не роняет —
это видно только против живого сервера, отдающего не-2xx с телом. Отмечено здесь, а не
замаскировано тестом, который ничего не проверяет.

**Почему F-1.12 разделена на три.** `chat()` в оригинале реализован **через**
`chat_stream`: несколько корпоративных прокси отвергают нестриминговый
`/chat/completions` с HTTP 400, принимая ту же нагрузку с `"stream": true`. Значит
«сначала без стрима» — не вариант, и граница проходит по другому месту.

**F-1.11: ключи. `key_management` отпал, миграция отпала, ловушки остались.**

`key_management.rs` (323) — это SSH-ключи для git по SSH. Мы взяли `git2` без сетевых фич,
клонирования нет, модуль не нужен. Из `master_key` отпала вся миграция (`LEGACY_KEY_FILE`,
`shred_legacy_file`, `sealed_blobs_present`, перенос файл→связка): мигрировать не с чего,
продукт новый.

Что перенесено ровно потому, что на это наступают один раз:

| Ловушка | Обход |
|---|---|
| `keyring` 3.x: платформенные бэкенды — **opt-in по фичам**. Без них крейт собирается с mock-хранилищем, чьи записи возвращают `Ok(())` и испаряются при выходе из процесса | Фичи включены per-target в `Cargo.toml` **с комментарием**, плюс зонд в рантайме: записать маркер и прочитать его **отдельной** записью, чего mock не может — он хранит значение внутри той записи, что писала |
| «Связка не содержит ключа» и «связка не смогла ответить» — не одно и то же | Только первое позволяет выпустить новый ключ. На macOS ACL элемента называет создавший его бинарник, поэтому каждый пересобранный дев-бинарник получает отказ на чтение, **сохраняя при этом право создать свой зондовый элемент**. Маркер `master_key_store.json` превращает неотвечающую связку в ошибку вместо чистого старта — иначе все запечатанные секреты станут нечитаемыми навсегда |
| Битый файл ключа | Ошибка, а не «ключа нет»: второе молча выпустило бы второй ключ поверх первого |
| Файловый fallback | `0600`. На общей машине иначе читается всеми аккаунтами |

**Ошибка в собственном коде, найденная тестом.** `open` в первой версии собирал заголовок
**из запрошенного** purpose и игнорировал записанный в файле. Записанный заголовок
оказывался декоративным, смещения вычислялись из догадки о форме файла, а порча заголовка
проходила незамеченной. Теперь заголовок читается из файла, purpose сверяется явно (ошибка
называет несовпадение, а не отдаёт неразличимое «не удалось расшифровать»), и он же идёт в
associated data.

**Ошибка в собственном тестовом дубле, найденная мутацией.** Режим «связка недоступна»
отказывал и зонду тоже — то есть моделировал «связки нет вовсе». Настоящий случай macOS
другой: зонд проходит, отказ получает именно существующий элемент. С исправленным дублём
защита от перевыпуска проверяется по-настоящему.

**Дыра в тестах, найденная мутацией.** Уборка purpose из associated data не роняла ничего:
явная сверка purpose ловит подмену раньше. Пропущен был настоящий сценарий — переклеить
заголовок на другой purpose и открыть **как этот другой**: сверка пройдёт, и только AAD не
даст расшифровать. Добавлен тест; ради него в `SecretPurpose` заведён второй вариант под
`cfg(test)`, который уйдёт, когда появится настоящий второй.

**Проверено мутациями** (четыре): зонд, доверяющий успешной записи (роняет 3 теста);
перевыпуск ключа при нечитаемой связке; битый файл как «ключа нет»; purpose вне AAD.

**F-1.10b: пауза — это один из трёх исходов turn'а, а не исключение.** Вынесено в
отдельный `domain/turn.rs`: у протокола turn'а другой потребитель, чем у протокола
провайдера.

`PendingApproval` воспроизведён со всеми шестью полями, и каждое подписано тем, что ломается
без него:

| Поле | Что ломается без него |
|---|---|
| `history` | Не с чего продолжать: раунд не исполнялся |
| `round`, `budget_used` | Потолки цикла обнуляются, и «спроси ещё раз» превращается в безлимитный бюджет |
| `event_seq` | Нумерация начинается заново, и слушатель, подключившийся в середине, не может ничего упорядочить |
| `calls` | Неопасные вызовы того же раунда молча теряются — раунд исполняется наполовину |
| `todos` | Предыдущий раунд того же turn'а мог уже поменять список |

**Новое против Atlas — две вещи.**

1. **`PendingApproval::check_decisions`.** Решения проверяются до того, как хоть одно
   исполнено: пропущенное решение не должно молча означать «выполняй», а id, не совпавший ни
   с одним вызовом, означает, что две стороны расходятся в том, о чём спрашивали, — момент
   остановиться, а не догадываться. Вызывающий за границей процесса, доверять нечему.
2. **`ToolCallDecision.reason` (CA-2.3).** В Atlas отказ голый. Модель, которой сказали
   только «отклонено», характерно повторяет тот же вызов, потом его близкий вариант.
   «Отклонено: используй существующий хелпер» заканчивает это за один раунд. Поле
   необязательное — отказу без пояснения ничего не навязывается.

**Steering (CA-1.6) отложен в F-1.14**, где живёт очередь: сейчас у него нет потребителя, а
`SteeringNote::user()` тянет `uuid`.

**Два комментария из оригинала перенесены дословно по смыслу**, потому что это следы
конкретных поломок: `RoundStarted` («до его появления проза двух раундов склеивалась на
полуслове — навсегда, в транскрипте и во всём, что из него воспроизводится») и
`RoundCompleted` («раунд, закрытый вызовом инструмента, не сверялся ни с чем: читатель видел
обрывок на полуслове, а полный текст лежал в истории и уходил модели»).

**Проверено мутациями** (четыре): непроверенное пропущенное решение, принятый неизвестный id,
выпавший из сериализации `event_seq`, несериализуемые вызовы в истории.

**F-1.10a: проводной протокол и трейт провайдера.** Модуль разделён надвое по потребителю:
здесь то, что нужно реализации провайдера (F-1.12), отдельно пойдёт то, что нужно циклу
(F-1.14). Настройки, пресеты и `ResolvedLlmProvider` уедут с F-1.12 — там их и собирают.

**Уточнение к docs §3.4.** Док называет контрольную точку возобновления `ResumePoint` и
перечисляет шесть полей. По коду: `ResumePoint` — внутренняя структура `llm_chat.rs`, а
**проводная** контрольная точка это `PendingApproval`, и все шесть полей именно в ней —
`history`, `round`, `budget_used`, `event_seq`, `calls`, `todos`. Воспроизводить надо её;
приедет в F-1.10b.

Что перенесено с обоснованием, а не механически:

| Решение | Почему |
|---|---|
| `sanitize_tool_call_arguments` заменяет аргументы на `{}` только в **эхе** | Проводной формат не требует, чтобы отражённая копия была валидным JSON, но как минимум один реальный шлюз отвечает 500, когда она не валидна. При этом исходные аргументы по-прежнему доходят до парсера: подменить их и здесь означало бы превратить точную жалобу в молча пустой вызов |
| Проверяется, что это **объект**, а не просто валидный JSON | `[1,2]` и `"строка"` — валидный JSON и не то, что ждёт провайдер |
| `content: null` у сообщения с одними вызовами | Так говорит провод. `""` читается как пустой ответ, которого модель не давала |
| `truncated` несётся отдельным полем | Модели не сообщают лимит ответа, поэтому оборванный раунд неотличим от законченного по тексту. Непрочитанный флаг даёт две тихие поломки: ответ, обрывающийся на полуслове и выглядящий намеренным, и вызов с обрезанным JSON, приезжающий как необъяснимая ошибка разбора |
| Трейт синхронный | Цикл последовательный по природе, блокирующий клиент проще отлаживать, а async окрасил бы все сервисы выше |

**Проверено мутациями** (четыре): санитизация, принимающая любой валидный JSON вместо
объекта; отключённая санитизация; `content: ""` вместо `null`; пустой `toolCalls`,
протекающий в проводную форму.

**F-1.9: `scope.rs` не перенесён ни одной строкой.** 916 строк — это `AiAccessMode`,
чтение/запись allowlist'а, миграции allowlist'а между версиями, `@deps` с подсказками про
`node_modules` и `java-sources`. Всё это удалено в F-1.1 и F-1.2. Единственное, что там
оставалось нужного, — `scope_for_config`, который при одном корне равен `ToolScope::new`.

Одна вещь оттуда отложена, а не выброшена: `auto_approved_tools` / `set_tool_auto_approved`
— это персистентность `ApprovalPolicy::always_allowed` в конфиг проекта. Конфига пока нет,
и потребителя тоже: политику собирает вызывающий. Вернётся, когда появится UI, где её
настраивают (этап 3), файлом в репозитории по docs §3.6.

**`parse.rs`: четыре вещи из 1437 строк.**

| Что | Почему именно так |
|---|---|
| `parse_tool_call` | Один `match` по проводному имени. Разворот `todo` в два варианта не нужен — у нас это уже проводная форма (F-1.8a), одной обязанностью меньше |
| Хвост после полного объекта прощается | `end()` не вызывается намеренно: поток, закрывшийся лишней скобкой или повтором фрагмента, всё равно отдаёт уже собранный объект |
| Ошибка называет **путь** к полю, а не смещение в байтах | Наблюдённый случай: модель написала одну правку верно, а несколькими элементами позже — `oldText`/`newText` вместо `old`/`new`. «at line 1 column 7275» не позволяет понять, какая правка сломана, `edits[1]` — позволяет |
| Словарь serde переписывается | `expected u32`, `expected a sequence` описывают типы Rust, которых модель знать не обязана |

**`preflight_tool_call` расширен против оригинала.** В Atlas он ловил запись вне каталога
документации и запрос уже выданного доступа. Здесь — выход за корень **и сверку с реестром
чтений**: «ты не читал этот файл» известно, не трогая диск и никого не спрашивая, поэтому
карточка подтверждения для такой записи — чистая церемония, стоящая пользователю решения,
а turn'у — раунда.

**Проверено мутациями** (четыре): выпавший из разбора инструмент (ловит тест-перечисление
всех имён), вызов `end()` вместо прощения хвоста, снятый перевод словаря, preflight без
сверки с реестром.

**F-1.8b: написано заново, а не перенесено.** Второй случай после `grep`, где «как есть» в
карте скрывает несоразмерную зависимость: за 461 строкой инструмента стоят `git_ops` (257)
и `infra/git_repo.rs` (**4156**) — клонирование, учётные данные, ветки, коммиты, push, pull,
stash. Это git-клиент, а инструменту нужны три операции чтения.

Побочная выгода: `git2` взят **без** `https` и `ssh`. В Atlas они включены ради
клонирования и push — здесь никто не ходит в сеть, и вендоренный OpenSSL из графа сборки
уходит вместе с ними.

Что воспроизведено из оригинала, потому что каждое — чей-то опыт:

| Решение | Почему |
|---|---|
| `gitDiff` на каталоге — ошибка, а не пустой дифф | У каталога нет блоба, поэтому ответ был бы «валидный пустой дифф». Модель читает это как «здесь ничего не менялось» и перестаёт искать. Сообщение называет выход: `gitStatus` |
| Лимит `gitStatus` съедает `unstaged`, а не режет три списка поровну | Конфликты и staged — короткие и решающие списки; раздувает выдачу репозиторий с тысячами неотслеживаемых файлов |
| Своё сообщение при blame некоммиченного файла | libgit2 отвечает «the path … does not exist in the given tree» — правда про дерево и бесполезно здесь: читается как «путь неверный», и модель идёт искать опечатку, которой нет |
| Потолок 400 строк на blame | Дальше это выдача файла с приписанными хэшами |

**Новое против Atlas:** репозиторий *обнаруживается* от корня области (`Repository::discover`),
а не предполагается им. Открыть подкаталог репозитория — обычное дело; пути при этом
пересчитываются в корень области в обе стороны, а изменения вне области отбрасываются, а не
переименовываются.

**Проверено мутациями** (четыре, каждая роняет свой тест): пустой дифф на каталоге, равная
обрезка трёх списков, сырое сообщение libgit2, отсутствие пересчёта путей.

**F-1.8a: `todo`, и два инварианта, которые держит рантайм, а не промпт.**

| Инвариант | Как обеспечен |
|---|---|
| Модель никогда не выбирает следующую задачу | `TodoUpdateStatus` не содержит `Pending`/`InProgress` — это отвергается **схемой**, а не просьбой в промпте |
| Ровно одна задача активна, следующая поднимается сразу | `advance()` после обеих операций: первая запись оставляет список без активной, завершение текущей — тоже |
| Id никогда не переиспользуются | Нумерация от длины всего списка, который только растёт. Переиспользованный id молча перенацелил бы более позднее обновление |
| Превышение лимита — отказ, не обрезка | Модель решает, что выбросить, а не узнаёт потом, что её обрезали |

`id` в обновлении необязателен, и это не мелочь удобства: в транскрипте, из-за которого
умолчание появилось, модель закрыла `t6`, имея в виду `t5`, и список закончил turn,
утверждая, что выполненный шаг не сделан. «Та задача, над которой я работаю» идентификатора
не требует — а это почти всякое обновление.

Отличие от Atlas: там `todo` — один проводной инструмент с `op`, который `parse`
разворачивает в **два** варианта `ToolCall`. Здесь `ToolCall::Todo(TodoArgs)` с
`#[serde(tag = "op")]` — это и есть проводная форма, разворачивать нечего. На одну
обязанность у `parse` (F-1.9) меньше.

Список ходит через `execute_tool` параметром, как и реестр чтений, и заменяется в
вызывающем **только** после успеха — неудачный вызов не оставляет список наполовину
изменённым.

**Проверено мутациями** (четыре): отключённое автопродвижение (роняет 4 теста),
переиспользование id, обрезка вместо отказа, запись списка при неудаче.

**F-1.8b: карта переноса снова недосчитала, и сильно.** `tools/git.rs` помечен «как есть»
(461 строка), но опирается на `services/git_ops.rs` (257) и `infra/git_repo.rs` —
**4156 строк**: клонирование, учётные данные, ветки, коммиты, push/pull, история. Инструменту
нужны три операции чтения. Переносить слой целиком бессмысленно — `gitDiff`/`gitBlame`/
`gitStatus` пишутся прямо на `git2`, порядка 150 строк.

**F-1.7: четыре инструмента, и несимметричная защита — намеренно.**

Реестр чтений применяется не ко всему, что меняет дерево, а к тому, что уничтожает
содержимое:

| Инструмент | Требует чтения | Почему |
|---|---|---|
| `deleteFile` | **Да, целиком** | Удаление — то же уничтожение, что перезапись. Частичное чтение не даёт права снести непрочитанное |
| `deleteDirectory` | Нет | «Прочитал ли агент каталог» не имеет смысла, а требовать чтения каждого файла внутри сделало бы инструмент непригодным для деревьев, ради которых он и нужен. Вместо этого — подтверждение человеком плюс нерекурсивность по умолчанию |
| `move` | Нет | Перемещение ничего не уничтожает: приёмник обязан быть свободен, источник сохраняет содержимое |
| `createDirectory` | Нет | Создание не разрушает |

**Реестр при удалении и перемещении не правится** — вопреки первому порыву. Я собирался
добавить `forget`/`rename`, но сценарии этого не требуют: после удаления запись безвредна
(записи в несуществующий файл проверка не касается), а если файл с тем же именем создаст
человек, несовпадение хэша честно остановит запись — это правильный ответ, а не случайность.
Перемещение без переноса записи стоит одного лишнего чтения и оставляет модель реестра
простой: «что прочитал, по какому пути».

Отдельно: `deleteDirectory` отказывается удалять сам корень. `resolve_existing(".")`
возвращает корень, и без этой проверки `recursive: true` снёс бы рабочий каталог целиком.

Не перенесено: шаблон `restEndpoint` у `createDirectory` (документационная специфика) и
переписывание ссылок `include::`/`xref:` у `move` — для кода ссылки дело компилятора, а не
файлового инструмента. Именно поэтому `move` здесь весит 1, а не 2 (см. F-1.1).

**Проверено мутациями** (четыре, каждая роняет свои тесты): снятая сверка с реестром в
`deleteFile` (роняет 3), удаление непустого каталога без `recursive`, удаление корня,
затирание существующего пути при `move`.

**F-1.6b: `editFile`, четыре гарантии и одно невзятое.**

Ядро (`exact_match_ranges` / `find_unique`) перенесено дословно — уникальность якоря
(CA-3.6) в апстриме уже была, писать заново не пришлось. Гарантии:

| Гарантия | Почему |
|---|---|
| Якорь ищется в **исходном** содержимом, никогда в результате предыдущей правки | Пакет правок значит одно и то же независимо от своего порядка |
| Якорь обязан быть уникален, счётчик — в ошибке | Счётчик говорит модели, что якорь короткий, вместо «почему-то не применилось» |
| Пересекающиеся правки отвергают весь вызов | Иначе результат зависит от порядка применения либо одна портит другую |
| Всё или ничего | Одна негодная правка в пакете не оставляет файл наполовину изменённым — это хуже любого из двух исходов |

Проверка реестра — с `require_whole: false`, в отличие от `writeFile`. Якорная правка
адресуется по содержимому: якорь уникален по **всему** файлу, а хэш подтверждает, что
ничего не сдвинулось, — значит непрочитанные части защищены не хуже прочитанных. Но
`record_write` после правки флаг «видела целиком» не повышает: правка не рассказала о том,
чего агент не читал.

**`fast_apply` не перенесён.** В Atlas при неудаче точного совпадения файл целиком и
намерение правки уходят в модель, та вносит изменение сама, после чего проверяется, что всё
за пределами правки побайтово не изменилось. Изобретательно — и превращает
детерминированный инструмент в ещё один вызов модели, с его ценой и разбросом. Неудачный
якорь и так точная, действенная ошибка: модель расширит его и повторит.

**Проверено мутациями** (четыре, каждая роняет свой тест): последовательное применение
вместо пакетного, снятая уникальность, снятая проверка пересечений, повышение флага
«видела целиком» правкой.

Повторился урок F-1.3: первая мутация упала не на том тесте, для которого писалась.
`edits_are_independent_of_their_order` на непересекающихся правках не различает пакетное и
последовательное применение — они дают одно и то же. Различает случай, где `new` одной
правки **является** якорем другой: `[("A","B"), ("B","C")]` над `"A\nB\n"` пакетно
однозначен, а последовательно первая правка создала бы второе вхождение `B` и следующий
якорь стал бы неоднозначным. Добавлен отдельным тестом.

**F-1.6a: «сначала прочитай файл» и CA-3.10 — это один механизм, а не два.**

Реестр `ReadFiles` хранит на каждый прочитанный путь хэш **всего** файла на момент чтения
и флаг «видела целиком». Один поиск отвечает на оба вопроса:

| Состояние | Ответ модели |
|---|---|
| Записи нет | `read {path} before writing to it` |
| Есть, хэш совпал, видела целиком | пишем |
| Есть, но прочитано частично, а просят полную перезапись | `read it in full, or use editFile` |
| Есть, хэш на диске другой | `changed on disk since you read it` |

Три решения, каждое закрывает свою потерю данных:

1. **Новый файл читать нечего** — проверка применяется только к существующему.
2. **Частичное чтение не даёт права на полную перезапись.** Прочитать 50 строк из 500 и
   заменить файл пятьюдесятью — уничтожить 450, которых никто не видел. Диапазон, который
   случайно покрыл весь файл, тоже считается частичным: осторожность стоит одного
   перечитывания, ошибка — файла.
3. **После записи реестр обновляется.** Иначе вторая запись подряд отвергалась бы как
   «файл изменился» — агентом же и изменённый. Но `whole` при этом не повышается: правка
   не рассказала агенту ничего о тех частях файла, которых он не читал.

Реестр принадлежит **вызывающему** и ходит через `execute_tool` параметром — тот же
принцип, что для todo-списка: исполнитель остаётся без состояния, а состояние переживает
паузу на подтверждении. Иначе каждая одобренная запись возвращалась бы как «файл
изменился». Ключ — канонический путь, а не то, что написала модель: иначе `./a.txt` и
`a.txt` были бы разными файлами.

От `write_file` из Atlas не осталось почти ничего: он писал только внутрь каталога
документации, только распознанные расширения и переписывал скобки asciidoc-макросов по
дороге на диск. Осталась форма — прочитать старое, записать, вернуть дифф.

**Проверено мутациями** (каждая роняет свой тест): ослабление «видела целиком» до
«видела», снятая сверка хэша, необновление реестра после записи, потеря различия между
полным и частичным чтением.

**F-1.5c: из четырёх веток осталась одна.** В Atlas `listFiles` ветвится на
документационный листинг (через свой обходчик дерева), полнорепозиторный, виртуальный
каталог `@deps/` и листинг внутри одного внешнего корня. Три ушли вместе с двухкорневой
моделью. С ними ушёл `is_dependency_noise` (прятал `test/`, `.min.js` и sourcemap'ы внутри
внешних корней) — и это правильно: он сознательно **не** применялся к самому репозиторию,
потому что собственный `test/` проекта — ровно то, про что могут спрашивать.

Сохранено то, ради чего инструмент так устроен:

| Решение | Почему |
|---|---|
| Результат для модели — **дерево с отступами**, не массив | Вложенность видно сразу, а не собирается из N отдельных строк-путей. Первая строка всегда `./`, а не имя каталога на диске: иначе модель примет рабочий каталог за дочерний и начнёт приписывать его ко всем путям |
| Лимит применяется **после** фильтра | Иначе `pattern` стал бы фильтром поверх уже обрезанного списка, и до остального не добраться никаким сужением запроса |
| Каталоги переживают `pattern` | Фильтр сужает, какие *файлы* вернутся, а не структуру, по которой до них идти |
| `BTreeMap` в узле дерева | Рендер детерминирован независимо от порядка обхода |

`render_file_tree` перенесён вместе с инструментом, хотя его потребитель приедет только в
F-1.14: в Atlas его зовёт `llm_chat` при сериализации результата для модели, то есть это и
есть модель-facing форма результата, а не вспомогательная утилита.

**Проверено мутациями:** лимит до фильтра и фильтр, выбрасывающий каталоги, — оба роняют
по тесту.

**F-1.5b: `grep` собран в один модуль.** В Atlas он разнесён на `tools/grep.rs` и
`services/docs_search.rs`, потому что поиск делит с пользовательским IPC. Второго
потребителя здесь нет — модуль один; разделение снова окупится в тот день, когда у UI
появится собственный поиск. Резолв пути и обход не переписаны: переиспользованы F-1.3 и
F-1.5a.

Что перенесено дословно, потому что каждое число — чей-то опыт:

| Ограничение | Зачем |
|---|---|
| Лимит считает **совпадения, а не строки** | Контекст принадлежит попаданию. Лимит по строкам при `contextLines: 3` вернул бы четверть запрошенного, молча |
| `truncated` проверяется **после** совпадения | Значит «дальше правда есть ещё», а не «мы упёрлись ровно в край». Поиск, тихо остановившийся на лимите, читается моделью как исчерпывающий ответ — единственное, ради чего `grep` и нужен |
| 1 МиБ на файл, NUL в первых 8 КБ | Бандлы, дампы, бинарники |
| 300 символов на строку, **символов, а не байт** | Одна минифицированная строка бывает целым файлом; срез по байтам паникует на середине многобайтового символа |
| Потолок 5 контекстных строк | Достаточно увидеть, во что попадание вложено, но не превращает поиск в вывод файла |

**Проверено мутациями** (все три роняют по тесту): лимит по строкам вместо совпадений,
обрезка по байтам (паникует), снятый отсев бинарных файлов.

**Компилятор подтвердил контракт CA-13.3.** Добавление варианта в `ToolCall` не собралось,
пока не появилась ветка в диспетчере — ровно то, ради чего enum и заведён. Заодно
вскрылось, что `ToolResult` с одним вариантом позволял разбирать себя неопровержимым
`let`: тестовый помощник `read_file` пришлось починить, когда вариантов стало два. Цена
того, что enum'ы рождаются маленькими и растут.

**F-1.5 разбита на три.** Карта переноса недосчитала: `grep.rs` (363) — это в основном
обработка `@deps`, которую мы выбросили, а настоящий поиск живёт в
`services/docs_search.rs` (383), которого в карте нет вовсе. Вместе с обходчиком
`infra/workspace_scanner.rs` (490) и `list_files.rs` (709) выходит ~1945 строк исходника
на одну фичу — слишком много, чтобы проверять за раз.

**F-1.5a: обходчик, и три сознательных расхождения с Atlas.**

| Решение | Почему |
|---|---|
| `hidden(false)` вместо `hidden(true)` | **Существенное.** Обходчик Atlas не видит точкофайлов. Для редактора документации это правильно, для кодинг-агента означает «не может прочитать `.github/workflows`, `.gitignore`, `.eslintrc`, `.env.example`» — ровно те файлы, про которые его спрашивают |
| Явный пропуск `.git` | Следствие предыдущего: видимые точкофайлы иначе втягивают тысячи loose objects, все бинарные |
| `require_git(false)` | `ignore` по умолчанию honours `.gitignore` **только внутри git-репозитория**. Atlas оставил дефолт, потому что редактор всегда открыт на репозитории. Здесь пользователь может указать на любой каталог, а лежащий в нём `.gitignore` — это утверждение о том, что считать мусором, независимо от того, запускали ли `git init` |

Последнее найдено тестом, а не чтением: `.gitignore` во временном каталоге не сработал,
потому что `.git` там нет. В Atlas это никогда не всплывало и багом не было — стало бы им
здесь.

Из API осталось две функции вместо пяти: `scan_files(root, max_depth)` и
`scan_entries(root, max_depth)` (с каталогами, для `listFiles`). Выброшены `scan`
(фильтр по документационным форматам), `is_new_file_indexable` (наблюдатель, этап 5) и
разделение `scan_all`/`scan_all_with_depth`. Ошибка — `std::io::Error`, а не свой тип:
у обходчика два разных потребителя, каждый отображает в своё.

Все три расхождения проверены мутациями — возврат к поведению Atlas роняет тесты.

**F-1.4: первый работающий инструмент, плюс то, чего в плане не было.**

`ToolCall`/`ToolResult` родились с одним вариантом, как и обещано в F-1.2. Диспетчер
`execute_tool(scope, call)` — **без** `deps` и `todos`, которые есть в сигнатуре Atlas: у
обоих пока нет потребителя, приедут с инструментом, которому понадобятся.

Три расхождения с планом:

1. **`text_diff.rs` уехал в F-1.6.** У него нет потребителя до записи файлов, а тянуть
   `similar` в зависимости раньше незачем.
2. **`readFile --outline` (CA-3.2) уехал на этап 5.** Он строит структуру файла через
   `detect_language` + `LanguageIndexer`, то есть зависит от индексного слоя целиком.
   Пока `totalLines` в ответе — единственный способ оценить, во что обойдётся чтение
   большого файла.
3. **`domain/flexible_args.rs` перенесён, хотя в плане его не было.** `ReadFileArgs`
   объявляет его в `deserialize_with`, так что без него не собирается. Перенесены
   `opt_u32` и `opt_bool`; `opt_usize`, `flag` и `opt_string_list` приедут со своими
   инструментами в F-1.5.

**Про `flexible_args` — это не мелочь.** Модель зовёт `readFile` с
`{"startLine": "1", "endLine": "90"}`, все скаляры в кавычках. Вызов семантически
безупречен, но `Option<u32>` отвергает его целиком, и типичное «восстановление» модели —
не починить тип, а **выбросить параметр**: повторить дословно, потом прислать без
диапазона и прочитать файл на 400 строк целиком. В одном транскрипте на это ушло 6 ошибок
инструментов из 10. Схемы уходят провайдеру без constrained decoding, так что JSON-Schema
для генератора токенов — пожелание. Принимаются однозначные написания, отвергается только
по-настоящему неразрешимое (`12.5` не округляется — это молча ответ на другой вопрос).

**Проверено мутациями:**

| Мутация | Роняет |
|---|---|
| Чтение целиком пересобирается из `lines()` | `a_whole_file_read_is_byte_identical` |
| Снят клампинг диапазона | 2 теста (без него — паника на выходе за границы среза) |

Первая тонкая: пересборка через `lines().join("\n")` дописывает финальный перевод строки
файлу, у которого его не было. Модель прочитает файл с лишним байтом и вернёт его же при
следующей правке — расхождение, которого никто не заказывал.

**F-1.3: два файла слились в один, машинерия алиасов исчезла.** Резолв в Atlas размазан
по `services/ai_tools/resolve.rs` (488) и `domain/paths.rs` (427). Большая часть обоих —
мир с двумя корнями: путь можно написать относительно каталога документации *или*
относительно репозитория, поэтому резолв перебирал варианты написания, а результаты
приходилось переводить между пространствами имён. Один корень удаляет это целиком:
`path_aliases`, `docs_root_prefixes`, `stripped_docs_prefixes`, `docs_rel_to_access_rel`,
`to_access_relative`, `access_and_docs_rel`, `resolve_mutable_docs_path`, `split_dep_path`
(`@deps`) и `reject_atlas_memory_path` не переносятся. Осталось ~200 строк.

`domain/paths.rs` **не** переехал в `domain/`: containment невозможно решить, не разрешив
симлинки, то есть файловая система здесь не деталь реализации, а то, что и спрашивают.
Трейт под это имел бы одну реализацию и один тестовый дубль, который не может ответить
честно. Оставлен в `services/ai_tools/resolve.rs` с пометкой в шапке модуля.

**Три защиты, каждая пристёгнута тестом и проверена мутацией:**

| Защита | Что ловит | Тест |
|---|---|---|
| Канонизация перед сравнением | Симлинк внутри корня, указывающий наружу | `a_symlink_leaving_the_root_is_refused` |
| `Path::starts_with` (покомпонентно, не по строке) | `/repo-backup` не внутри `/repo` | `a_sibling_sharing_the_roots_name_prefix_is_refused` |
| В сегменте только плоское имя | `..` и виндовый префикс диска | `parent_traversal_is_refused_even_when_it_stays_inside` |

**Найден мёртвый код.** В `join_relative` две проверки подряд: `if part == ".."` и
`components().next() != Normal(part)`. Первая недостижима — для `..` компонент это
`ParentDir`, и вторая отвергает его раньше. Обнаружено мутацией: снятие проверки `..` не
роняло ни одного теста. Дубль убран, объяснение перенесено в живую проверку.

Заодно это вскрыло дыру в тестах: они проверяли **исход** («обход отвергнут»), а не то,
какая защита его отвергает. На всяком уходящем за корень пути обе срабатывают, и тест не
различает, какая ещё жива. Разводит их путь с `..`, остающийся **внутри** корня
(`a/../a/file.txt`): канонизация пропустит, отвергает только явная проверка. Этот случай
теперь отдельный тест.

Честная оговорка: проверка виндового префикса диска на macOS отдельно не пристёгнута — на
Unix `C:` это законное имя файла, и путь просто складывается внутрь корня. Тест
утверждает инвариант «за корень не уходит» на всех платформах, а живость самой строки
держит тест про `..`.

**F-1.2: `ToolScope` схлопнулся в одно поле.** В Atlas у него семь: `mode`, три корня
(`root`, `repo_root`, `docs_root`), `docs_filter_prefix`, `allowed_tools` и `extra_roots`.
Шесть из них выражают одно правило — «читать можно весь репозиторий, писать только в
каталог документации», — которого у кодинг-агента нет. Осталось то, что всегда и делало
работу: канонизированный корень. `extra_roots` (`@deps/{name}`, внешние read-only корни)
ушли вместе с `requestDependencySources`; `allows()` — вместе с allowlist из F-1.1;
`allows_search_result()` — вместе с `docs_filter_prefix`.

Канонизация в конструкторе — не украшение: граница, которую сравнивают с путём, где ещё
остался `..` или неразвёрнутый симлинк, границей не является. Несуществующий корень —
ошибка, а не молчаливое «значит, везде».

`ToolError`: из 21 варианта перенесено 15. Выброшены `NotAllowed` (нет allowlist),
`OutsideDocumentation` (нет каталога документации), `SemanticSearch` (этап 5),
`Plan`/`Skill`/`Artifact` (этап 6 и невошедшее), `EditApplyFailed` — см. ниже. Восемь
`From<…> for ToolError` не перенесены: все ссылаются на домены, которых здесь пока нет.

**Две находки в апстриме, которых нет в карте переноса.** Обе всплыли при чтении
`ToolError`, обе меняют F-1.6:

1. **Уникальность якоря в `edit_file` уже реализована.** [04-reuse-map.md](04-reuse-map.md)
   §4.2 утверждает, что «в Atlas правка идёт списком `{old, new}`… нужна проверка
   уникальности якоря». Это устарело: в `apply_edits_exact` есть `EditTextNotFound`,
   `EditTextAmbiguous(old, count)` **и** `EditsOverlap` (две правки, попавшие в
   пересекающиеся области, отклоняют весь вызов). Писать заново нечего — переносить.
   Счётчик совпадений в ошибке важен: он говорит модели, что якорь короткий, вместо того
   чтобы оставить её гадать, почему точное совпадение отвергли.
2. **Есть `fast_apply`, о котором план не знал.** Когда точное совпадение не найдено или
   неоднозначно, `edit_file` умеет отдать файл целиком и намерение правки в LLM и
   попросить её внести изменение самостоятельно, после чего проверяет, что всё за
   пределами правки побайтово не изменилось. Это отдельная фича с зависимостью от
   провайдера. Решение принимается в F-1.6, не сейчас; по умолчанию — не переносить,
   потому что она превращает детерминированный инструмент в ещё один вызов модели.

**F-1.1: от `ai_access.rs` осталось 70 строк из 354.** Файл оказался в основном мёртвым
для этого продукта ещё до переноса:

| Кусок | Что с ним |
|---|---|
| `AiAccessMode { DocsOnly, FullRepo }` | Выброшен. Корень доступа для кодинг-агента всегда репозиторий, и enforce'ится он в `resolve.rs` (F-1.3), а не режимом |
| `default_allowed_tools(_mode)` | Выброшен. Параметр не использовался, функция возвращала **все 28 вариантов**, а её собственный комментарий признавал: настроить список нечем, UI нет |
| `ai_allowed_tools: Option<Vec<ToolName>>` (CA-2.5) | Выброшен вместе с ним. Fail-closed-механизм, охраняющий множество, где разрешено всё. Вернётся, когда появится UI, где список можно сузить |
| `RequestFullRepoAccess` | Выброшен — инструмент существовал только чтобы просить `DocsOnly` → `FullRepo` |
| `auto_approvable()` | Выброшен. Все четыре его спецслучая — непереносимые инструменты, функция схлопнулась в `requires_confirmation()` |
| `no_project_tools()` | Выброшен. У десктопа репозиторий открыт всегда |
| `call_requires_confirmation(name, args)` | Единственный аргументный случай — `Memory`, это этап 6. Форма сохранена, см. ниже |
| `requires_confirmation()` | Оставлено как `ToolName::is_mutating()` |
| `loop_weight()` | Оставлено. `Move` подешевел с 2 до 1: Atlas платил за переписывание `include::`/`xref:`, а эта часть не переносится |
| `from_wire_name()` | Оставлено, плюс обратный `wire_name()` — ради round-trip-теста |

Вместо `AiAccessMode` и allowlist — `ApprovalPolicy { always_allowed, skip_all }`:
«разрешать всегда для инструмента» (CA-2.3) и «не спрашивать вообще» (CA-2.7).

**Два решения, которые дальше нельзя ломать:**

1. **Гейт принимает вердикт вызова, а не выводит его из имени:**
   `requires_approval(name, risky)`. Сегодня все вызывающие передают
   `name.is_mutating()`. На этапе 2 `runCommand` передаст разбор командной строки —
   `ls` и `rm -rf /` это один инструмент с разными ответами, и по имени их не различить.
   Сигнатура сразу правильная стоит ноль, переделка вызовов потом — нет.
2. **`skip_all` не персистится.** Сохранённое «никогда не спрашивай» — это тормоз,
   снятый месяц назад и забытый. Turn, прошедший под ним, обязан сказать это в
   транскрипте (CA-2.7) — но это работа цикла, не структуры.

Тесты (11) проверены мутациями: расхождение `wire_name` с serde-формой и потеря
`skip_all` в гейте оба роняют сборку. Первое неочевидно и дорого: «разрешать всегда»
сохраняется в одном написании, а сверяется в другом — и инструмент спрашивает вечно.

**F-1.14 подробно.** Выбросить `needs_diagram_nudge`, `DIAGRAM_NOTE`,
`history_has_a_visualize_call`, `last_user_message_asks_for_a_diagram`. **Сохранить сам
механизм** «бэкстоп-подсказка по эвристике над последним сообщением» как точку расширения —
под кодинг пригодится («правил код и ни разу не запустил тесты»). Перевести на английский
все подсказки модели, включая `TRUNCATED_ROUND_NOTE`. Не потерять:

- взвешенный бюджет (`MAX_TOOL_BUDGET` + `ToolName::loop_weight`) и `MAX_TOOL_ITERATIONS`
  как backstop на кривой вес;
- `dedupe_repeat_result` — ключ «инструмент + сырые аргументы», гейт по хэшу payload'а;
- `is_peer_disconnected_error` — ретрай **только** если провайдер не отдал ни байта,
  иначе дублируется side-effect уже выполненного инструмента;
- `TRUNCATED_ROUND_NOTE` приписывается только к **упавшему** вызову: успевший закрыться
  выполнился нормально, и говорить ему об обрыве вредно.

**Готово ✅.** `cargo test` — 339 тестов; интеграционный тест на мок-провайдере «turn читает
файл, правит файл, получает `FileDiffStats`»; тест паузы и возобновления через настоящую
паузу, проверяющий **все семь** полей контрольной точки (история, раунд, бюджет,
`event_seq`, вызовы, todo, реестр чтений) — каждое закрывает конкретный баг. Разбор
этапа — в блоке «Этап 1 закрыт» выше.

---

## Этап 2. Исполнение команд — пробел №1

В Atlas нет ни одного инструмента, запускающего процесс. Пишется целиком (05-gaps §5.1).
Цель — сценарий S-1: починить падающий тест.

| Фича | Что |
|---|---|
| **F-2.1** ✅ | `domain/command_exec.rs` — типы запроса/результата, `thiserror`-энум, событие стриминга, обрезка вывода. Разбор ниже |
| **F-2.2** ✅ | `infra/process_runner.rs` — запуск, таймаут, **убийство дерева**, stdin в `/dev/null`, чтение байтами. Разбор ниже |
| **F-2.3** ✅ | Стриминг stdout/stderr через sink турна, обрезка — в F-2.1 |
| **F-2.4** ✅ | `tools/run_command.rs` + варианты в двух enum'ах + ветка + `ToolDeps`. Разбор ниже |
| **F-2.5** | Правила подтверждения на **существующем** `call_requires_confirmation` (он уже решает по аргументам, а не по имени инструмента). Разбор командной строки, не подстрочный матч: `rm -rf` в строковом аргументе ≠ `rm -rf` как команда; `git push --force` ловится и через алиас |

Оболочка задаётся явно в настройках, а не «что нашли в PATH» — на Windows разница между
`cmd`, PowerShell и Git Bash не косметическая. Персистентность `cd` между вызовами —
решить и записать в док, не оставлять наугад.

Отложено с заделом: песочница (CA-4.7) — точку в интерфейсе раннера оставить, реализации
нет; фоновые процессы (CA-4.5) — этап 7.

**F-2.1: типы. Главное в них — то, что здесь кончается изоляция.**

Каждый инструмент до этого разрешал пути относительно корня, и корень был настоящей
границей. Командная строка — нет: `cat ../../.ssh/id_rsa` это совершенно обычная команда, и
никакая проверка рабочего каталога этого не меняет. Поэтому `cwd` резолвится внутри корня
ровно потому, что так удобнее делать `cd`, **а не как мера защиты**, и честная формулировка
звучит так: с этого момента гейт подтверждений — единственная граница, которая есть.
Изменит это песочница (CA-4.7); до тех пор ничего здесь нельзя описывать как «ограничено
корнем».

| Решение | Почему |
|---|---|
| Команда идёт **через оболочку** | Команды кодинг-агента — это команды оболочки: `cargo test 2>&1 \| tail -20`, `cd crate && make`. Свой разбор argv отверг бы их, и модели пришлось бы перебором выяснять, какая половина её словаря работает |
| `timeout: 0` читается как «не указан» | Модель пишет ноль, когда имеет в виду «без ограничения». Из двух прочтений «убить немедленно» не нужно никогда |
| Потолок таймаута — 10 минут | Сборка, которой действительно нужно больше, — это фоновый процесс (CA-4.5), а не вызов инструмента, которого turn ждёт |
| `exit_code: Option<i32>` отдельно от `timed_out` | «Убит по таймауту» и «вышел с нулём» — разные ответы на тот самый вопрос, который модель и задаёт. Молчание успехом не считается |
| Обрезка вывода — **с двух концов**, а не с головы | У прогона тестов вердикт в последних строках (`test result: FAILED. 3 passed; 497 failed`), а первая настоящая ошибка — сверху. Срезать любой из концов значит выбросить половину того, что спрашивали. Маркер называет число пропущенных строк, чтобы модель могла запросить именно их, а не перезапускать всё вслепую |

**Мутации: шесть, все пойманы.** Ноль как мгновенное убийство; снятый потолок таймаута;
обрезка только с головы; маркер без счётчика; счётчик, забывший хвост; молчание, читаемое
как успех.

**F-2.2: раннер. Дерево процессов и труба, о которой забываешь.**

`Child::kill()` сигналит ровно одному процессу, а команда оболочки почти никогда не один
процесс. Ребёнок стартует в **своей группе процессов**, и сигнал идёт группе. Тест на это —
единственный в наборе, который ждёт по-настоящему: внуку нужно дать шанс записать файл,
прежде чем его отсутствие начнёт что-то значить.

**Дефект, который нашёлся мутацией — в моём собственном коде.** Мутация «не убивать группу»
уронила тест таймаута, как и ожидалось, но прогон занял 33 секунды вместо четырёх. Причина не
в тесте: выживший внук унаследовал **пишущий конец трубы**, поэтому читающие потоки не
видели EOF и `join()` ждал его до самого конца. То же самое случается и без всякой мутации,
на обычном пути: `sh -c 'server & exit 0'` — ребёнок вышел сам, таймаут уже позади, а вызов
висит столько, сколько живёт демон. Починка — убивать группу **и при нормальном выходе**;
фоновый процесс, переживающий turn, это CA-4.5, а здесь он был бы вызовом, который никогда не
возвращается. Закрыто отдельным тестом.

| Решение | Почему |
|---|---|
| Два потока-читателя, а не последовательное чтение | Команда, забившая трубу, которую никто не читает, встаёт навсегда; плюс модель видит длинную сборку идущей, а не молчащей |
| Чтение **байтами**, не строками | Компилятор вправе выдать последовательность, которая не UTF-8, а читатель, падающий на ней, потерял бы весь прогон из-за одного символа |
| `stdin` в `/dev/null` | Ввод никто не наберёт. Ждать его до таймаута — потратить turn на команду, которая может только упасть |
| Опрос `try_wait` раз в 25 мс | Таймаут точен до моргания и не крутит ядро |
| На Windows — `taskkill /T /F`, а не job object | `taskkill` обходит дерево в момент вызова, поэтому внука, чей родитель уже вышел, не найдёт. Написано так осознанно: непроверенный `windows-sys`-код хуже задокументированного потолка, а Windows как платформа — отдельный пункт (05-gaps §5.4) |

**Мутации: шесть, все пойманы.** Снятая группа процессов; сигнал ребёнку вместо группы;
открытый stdin; выключенная обрезка; перепутанная метка потока; отсутствие убийства при
нормальном выходе (та самая находка).

**Готово ✅** — оба критерия закрыты тестами, разбор в блоке «Этап 2 закрыт» ниже.

**F-2.3 + F-2.4: инструмент. Контракт границы выдержал — почти.**

Добавление `runCommand` — первая живая проверка CA-13.3 на инструменте, которому нужно **что-то
кроме пути**. Контракт говорит: вариант в двух enum'ах, модуль, ветка в диспетчере, и больше
нигде. На деле понадобилось ещё одно: у диспетчера появился параметр `ToolDeps` — оболочка и
sink вывода. Это не нарушение контракта, а его недостающая часть: в Atlas ровно для этого есть
`EmbeddingDeps`, и в шапке `tools/mod.rs` было записано «зависимости приедут с инструментом,
которому они понадобятся». Приехали.

**Тест на расхождение схем поймал настоящий промах.** Я добавил `runCommand` в `DEFINITIONS`,
но забыл ветку в `parse_tool_call` — модель получила бы описание инструмента, который на вызов
отвечает `unknown tool`. Это ровно то, ради чего тест из F-1.14b и писался.

**Заодно всплыл тест со сроком годности.** `unknown_wire_name_is_rejected` проверял, что
`from_wire_name("runCommand")` возвращает `None`, — «имя, которого у нас никогда не будет»
оказалось именем следующего инструмента. Заменено на заведомо несуществующее, с комментарием.

| Решение | Почему |
|---|---|
| `is_mutating` для `runCommand` — **всегда true** | Имя инструмента ничего не говорит о том, что делает командная строка: `ls` и `rm -rf /` это один и тот же вызов с разными аргументами. Сузить обратно — вопрос **к командной строке**, и его задаёт F-2.5, а не таблица имён |
| Вес 5 — самый дорогой инструмент | Единственный, чья стоимость не ограничена ничем: сборка, прогон тестов, скачивание |
| `CommandOutput`-события несут `seq: 0` | Вывод приходит из читающих потоков раннера, где курсора цикла нет. Упорядочивать их относительно остального потока не нужно: они принадлежат вызову, названному в `id`, а авторитетный текст — результат вызова |
| `cwd` проходит preflight, командная строка — нет | И это записано и в коде, и здесь: проверка рабочего каталога делает `cd` предсказуемым, а не ограничивает команду |

**Мутации: пять, все пойманы.** Команда, не требующая подтверждения; `cwd` мимо резолвера;
пустая команда, уходящая в оболочку; выключенный стриминг; вывод без метки вызова.

---

### Этап 2 закрыт

| Критерий | Чем закрыт |
|---|---|
| «Агент запускает падающий тест, читает вывод, правит файл, запускает снова» | `the_agent_runs_a_failing_test_fixes_the_code_and_runs_it_again` — сквозной тест через цикл: скриптован только собеседник, всё остальное настоящее (скрипт на диске, реальный процесс, реальная правка файла) |
| Таймаут убивает внуков | `a_timeout_kills_the_grandchildren_too` — и проверен мутацией: без группы процессов падает |

**Не сделано и отложено осознанно:** F-2.5 (сужение подтверждений по разбору командной
строки) — сейчас подтверждения требует **каждая** команда, что безопасно и шумно; песочница
(CA-4.7); фоновые процессы (CA-4.5); персистентность `cd` между вызовами — её нет, каждый
вызов начинается от корня или от своего `cwd`, и это записано в схеме инструмента.

---

## Этап 3. Десктоп: провод оболочки к ядру

Здесь макет становится приложением.

| Фича | Что |
|---|---|
| **F-3.1** ✅ | `commands/chat_events.rs` — адаптер `ChatEventSink` → `tauri::Emitter`. Единственное место, где события ядра становятся IPC. Разбор ниже |
| **F-3.2** ✅ | `commands/chat.rs` + резидентное состояние + `src/lib/chat.ts`. Разбор ниже |
| **F-3.3** ✅ | `src/lib/chatTurnReducer.ts` — написан заново под прототип, а не перенесён. Разбор ниже |
| **F-3.4** ✅ | `src/hooks/useAgentTurn.ts` + `useWorkspace.ts` — оркестрация turn'а из окна |
| **F-3.5** ✅ | Транскрипт: `describeTool.ts` + `ChatPanel`/`Composer`/`App` на живых данных. Разбор ниже |
| **F-3.6** | Подтверждения: `ApprovalCard` в `ChatPanel.tsx:39` уже нарисован — дотянуть до `PendingToolCall`/`ToolCallDecision` (разрешить раз / всегда / отклонить с комментарием) |
| **F-3.7** ✅ | Предпросмотр до применения (CA-11.3): `services/ai_tools/preview.rs` + карточка. Разбор ниже |
| **F-3.8** ✅ | `infra/chat_store.rs` + `commands/chat_history.rs` + `domain/chat_record.rs` (контракт формата) → список чатов в сайдбаре настоящий. Разбор ниже |
| **F-3.9** ✅ | `commands/settings.rs` + `ProviderSettings.tsx` — настройка провайдера и ключа. В исходном плане фичи не было |

**F-3.1: адаптер. Один канал, и тест, который сначала проверял не то.**

Канал один, а не топик на каждый вид события. Раздельные топики выглядят опрятнее и оставляют
**порядок между ними** неопределённым — а транскрипт целиком построен на этом порядке. Здесь
`seq`, `round` и `targetId` едут с каждым событием, и слушатель применяет его к именованному
блоку, отличая опоздавшее, повторное и пришедшее не по порядку. В Atlas это исправление уже
сделано (там было десять глобальных топиков), и оно переносится как есть.

`turnId` — транспортная забота, и такой и остаётся: `ChatTurnEvent` о нём не знает. Нужен он
потому, что канал глобальный: два одновременных turn'а иначе перемешают свои дельты
посимвольно в одно сообщение.

**Мутация снова показала тест, охраняющий фикстуру.** Первый вариант «два turn'а различимы»
собирал две структуры руками и сравнивал их `turnId` — это верно для любых двух структур и
ничего не говорит о sink'е; мутация «штамповать одну константу на всех» его проходила.
Переписано через `tauri::test::mock_app()` и настоящего слушателя. Чтобы это стало возможным,
`chat_event_sink` пришлось сделать обобщённым по `Runtime` — у мок-приложения свой рантайм.
Обобщение ровно ради теста, и так и подписано.

**Что тестом не закрыто:** переименование самого канала. Его вторая половина — строка в
TypeScript, и проверить её из Rust нечем; расхождение проявится как тишина в окне. Литерал
закреплён отдельным тестом — это не делает переименование безопасным, но делает его
осознанным: правка попадёт в диff вместе с напоминанием.

**F-3.2: команды. Бэкенд по-прежнему ничего не помнит — почти.**

Разговор здесь не хранится: `chat_start` получает историю целиком, `chat_resume` — контрольную
точку целиком. Именно это позволяет приостановленному turn'у пережить перезагрузку окна.
Резидентно только то, до чего нужно **дотянуться снаружи, пока turn идёт**: флаг остановки,
очередь заметок, открытая папка и политика подтверждений.

| Решение | Почему |
|---|---|
| `chat_start` сбрасывает флаг отмены, `chat_resume` — **нет** | Случайная отмена от уже закончившегося turn'а не должна гасить новый. А «стоп», нажатый пока висела карточка подтверждения, обязан дожить до возобновлённого turn'а — он читает флаг, а не получает его заново |
| Turn уходит в `spawn_blocking` | Он весь синхронный — провайдер, инструменты, процессы. Держать IPC-цикл минутами значит заморозить все остальные команды, включая ту, которая его останавливает |
| «Разрешать всегда» живёт до конца сессии и никуда не пишется | Сохранённое «больше не спрашивай» — это снятый месяц назад и забытый тормоз |
| Папка канонизируется один раз, при открытии | Каждый путь инструмента потом меряется относительно настоящего каталога, а не того написания, которое прислали |
| Обёртки отказываются работать вне приложения с внятной ошибкой | `invoke` в браузере падает с «`__TAURI_INTERNALS__` is undefined», что ничего не объясняет |

**Capability не понадобилась:** команды самого приложения из `generate_handler!` разрешены по
умолчанию, а `listen` на события покрыт уже имеющимся `core:default`. Правило из `AGENTS.md`
касается плагинов и API платформы — здесь ни того, ни другого.

**Мутации: три на фронте** (слушатель без фильтра по turn'у; контрольная точка, теряющая
реестр чтений; переименованный аргумент команды) **и пять на бэкенде** — все пойманы.

**F-3.3: редьюсер. Написан заново — по прототипу, а не по Atlas.**

План говорил «переносится как есть». На деле переносить нечего: 294 строки в Atlas собирают
блоки документационного продукта, а модель блоков здесь диктует
[прототип](prototype/atlas-cli.html) — `bubble` пользователя, `msg` агента, список `tool-item`
с раскрывающимся `tool-detail`, `approval-card`. Перенесён **протокол**, а не код: порядок и
дедуп по `seq`.

Три вещи ломаются у потока, пересёкшего границу процессов, и все три чинятся здесь, а не тем,
кто рисует:

| Случай | Что делает редьюсер |
|---|---|
| Событие пришло дважды | Отбрасывается: `seq <= lastSeq` — это повтор |
| Событие пришло рано | Кладётся в буфер и ждёт, пока закроется дырка перед ним; потом очередь сливается по порядку |
| Событие без номера вовсе | Вывод команды применяется **сразу, мимо очереди** |

Последнее — не недосмотр, а следствие решения из F-2.4: вывод команды пишется читающими
потоками раннера, где курсора turn'а не существует. Он принадлежит вызову, названному в
`id`, и ни с чем не упорядочивается. Пропусти я его через очередь — каждый чанк ждал бы
номера, которого не будет никогда, то есть длинная сборка снова стала бы молчанием.

**Что ещё закреплено тестами:** финальный текст раунда затирает то, что собрали дельты
(потерянная дельта иначе навсегда остаётся в сохранённом транскрипте); второй раунд начинает
свой абзац, а не дописывает чужой; раунд без прозы не рисует пустой абзац; вызов и результат —
один блок, склеенный по идентификатору вызова модели.

**Мутации: шесть, все пойманы.** Повтор, применяемый дважды; раннее событие, применяемое
сразу; вывод команды через очередь; снятая сверка финального текста; раунды, делящие один
абзац; результат, пришедший раньше своего вызова.

**Прототип не описывает** блок заметки, набранной посреди turn'а (`steeringApplied`): класса
под неё в `atlas-cli.html` нет. Блок редьюсер отдаёт, стиль добавится при вёрстке транскрипта
(F-3.5).

**F-3.4 + F-3.5: окно наконец что-то делает.**

`src/mock/data.ts` в репозитории уже не было — на его месте в `App.tsx` стояли три пустых шва
с комментарием «каждый станет хуком». Стали.

| Файл | Было | Стало |
|---|---|---|
| `App.tsx` | `session`/`chats`/`turns` = пустые константы | `useWorkspace()` + `useAgentTurn()`; модалка выбора папки |
| `ChatPanel.tsx` | `Turn[]` из `types.ts` с именами `Read`/`Grep`/`Edit`/`Bash` | `Block[]` редьюсера, группировка в реплики, карточка подтверждения с тремя кнопками и полем причины |
| `Composer.tsx` | «Sending is not wired yet» | Отправка; пока turn идёт — та же строка шлёт steering, а кнопка отправки становится «стоп» |
| `ChatEmptyState.tsx` | `Session \| null` | Путь открытой папки |
| `types.ts` | `Session`/`ToolCall`/`Approval`/`Turn`/`isApproval` | Удалены: типы транскрипта живут рядом с кодом, который их строит |

**Отображение вызова — на фронте, и это не лень.** `describeTool.ts` превращает
`{"path":"a.rs","startLine":119}` в `Read · a.rs · lines 119-124`. Вопрос «что хорошо читается
в списке» меняется без изменения протокола, и ответ на него не должен требовать пересборки
бэкенда. Разбор аргументов защищённый: пока модель пишет вызов, JSON приходит по символу и
половину времени невалиден — строка обязана рисоваться, а не падать.

**Три места, где UI пришлось дополнить прототип:**

1. **Поле причины в карточке подтверждения.** В `atlas-cli.html` карточка уже отвечена, поля
   там нет. Отказ без причины — это то, после чего модель повторяет тот же вызов, потом его
   вариант (`ToolCallDecision.reason` из F-1.10b), так что поле появилось.
2. **Кнопка «Всегда»** — третья, рядом с Allow/Deny. Зовёт `approval_always_allow` перед
   возобновлением.
3. **Блок steering** (`.bubble.steer`) — левая амберовая полоса. Обычный пузырь читался бы как
   новая постановка задачи, а модели сказано ровно обратное.

Кнопка отправки во время turn'а превращается в «стоп»: она на том же месте, и это
единственное, что с ней в этот момент осмысленно делать.

**Тесты: 51 на фронте** (+20), из них 11 на `describeTool` и 9 на транскрипт — включая то,
что причина отказа доезжает до решения, а не остаётся в поле ввода, и что вложенный вызов не
открывает третью реплику. Проверено в браузере: пустое состояние, модалка, консоль чистая.

**Замечено по дороге, не чинил:** в карточке «Getting started» текст на русском, хотя решение
по проекту — английский. Это вёрстка из прототипа, не моя фича; правится отдельно.

**F-3.9 (не было в плане): настройки провайдера.**

Пробел в самом плане: этап 3 расписан от адаптера событий до хранилища чатов, но turn в окне
нельзя запустить, пока провайдер не настроен, — а ключ запечатан, и руками в `settings.json`
его не вписать. Фича добавлена в этап как F-3.9.

| Решение | Почему |
|---|---|
| Ключ едет **только в одну сторону** | Команды, которая его читает, не существует. Окно узнаёт `hasApiKey: bool`, и это всё |
| Нетронутое поле ключа — это `null`, а не `""` | Пустая строка означает «удали сохранённый». Отправлять её при каждом сохранении значит стирать ключ, который только что ввели |
| Удаление провайдера уносит его ключ | Тот же идентификатор, заведённый позже под другой эндпойнт, унаследовал бы учётку, которую пользователь считал удалённой |
| Удаление активного провайдера снимает закреп | Иначе закреп указывает в никуда, каждый turn отказывается стартовать, и понять почему неоткуда |
| Правка провайдера сохраняется **на месте** | Список — это порядок, который выбрал пользователь, и первый в нём работает как «по умолчанию» |
| В модалке — сегментированный переключатель, а не выпадающий список | Причина уже записана в `Modal.css`: всплывающее меню обрезается overflow'ом модалки |

**Мутации на форме: три, одна выжила — и это была настоящая дыра.** Ключ, набранный для
одного провайдера, не очищался при переключении на другого: сохранение запечатывало его **под
чужим идентификатором**. Чужой эндпойнт получал бы учётку, а нужный оставался без неё.
Закрыто тестом.

Ещё одна мутация оказалась эквивалентной (начальное значение поля ключа не важно — эффект
всё равно сбрасывает его при загрузке настроек); заменена на ту, которая проверяет то же
свойство на выходе, а не на входе.

**Интерфейс переведён на английский** — три карточки онбординга оставались русскими со времён
прототипа. Русского текста в `src/` больше нет.

**F-3.7: предпросмотр. Одобрить запись — значит одобрить её содержимое.**

Карточка показывала имя инструмента и путь. `{"path":"Mapper.java"}` — не ответ на вопрос «что
изменится», и человек, нажимающий «Разрешить», соглашался вслепую. Теперь для
приостановленного раунда бэкенд считает, **что бы произошло**, ничего не делая:

| Вызов | Что показывается |
|---|---|
| `writeFile` / `editFile` | Дифф против того, что лежит на диске сейчас. Несуществующий файл диффится против пустоты — каждая строка читается как добавление, а не как ошибка |
| `deleteFile` | Весь файл как уходящий |
| `deleteDirectory` | **Сколько файлов** заберёт. Самое страшное для подтверждения — путь, оказавшийся шире, чем выглядел |
| `runCommand` | Команда и каталог, в котором она запустится |
| `move`, `createDirectory`, чтения | Ничего: аргументы уже всё сказали |

**Неудачный предпросмотр стоит удачного.** Правка, чей якорь больше не совпадает, отказывается
**до** согласия, а не после: иначе это стоит раунда и доверия к карточке. Тот же ответ приходит
на путь за пределами рабочего каталога — проверка та же, что у preflight.

Считается по требованию, для паузы, и **ничего не пишет** — на это есть отдельный тест: файл на
диске после предпросмотра байт в байт прежний.

Вёрстка взята из прототипа: `.diff-preview` с `.ln-add`/`.ln-del` — те же классы, что у
предпросмотра в панели изменений, чтобы два места читались одинаково. Длинный дифф прокручивается
внутри карточки, а не выталкивает за экран кнопки, которыми на него отвечают.

**Мутации: пять, все пойманы.** Молча пустой предпросмотр вместо ошибки правки; пустой вместо
удаления; счётчик файлов, всегда равный нулю; пустой вместо записи; удаляемые строки без цвета.

**Тесты проходили по файлам и падали вместе.** `chat.test.ts` ставил
`window.__TAURI_INTERNALS__`, чтобы обёртки считали себя в приложении, и не убирал за собой:
в других файлах компоненты начинали верить, что бэкенд есть, и звали замоканный `invoke`.
Починено `afterAll`, а карточка заодно перестала зависеть от того, что предпросмотры вообще
придут.

**F-3.8: разговор переживает закрытие окна.**

Список чатов в сайдбаре был пустым массивом в `App.tsx`. Теперь это каталог
`~/.atlas-desktop/chats` — **по файлу на чат**, а не база.

**Почему файл, а не SQLite.** Апстрим держит `chat.db` (843 строки, `rusqlite`, миграции,
`PRAGMA user_version`), потому что у него SQLite уже есть — под индекс. Здесь его нет, а то,
что нужно, — это папка разговоров, которую читают при открытии одного и пишут раз в конце
turn'а. Индексированная выборка начинает окупаться на тысячах чатов в одном проекте; до тех
пор это зависимость, схема и её миграции ради сортировки десяти строк.
**Пересмотреть, когда** список станет заметно медленным или чат понадобится искать по
содержимому, а не открывать по имени, — и то и другое приходит вместе с индексным слоем
(этап 5), который тащит зависимость всё равно.

Файл на чат заодно решает, **сколько стоит повреждение**. Одно хранилище — одна точка
потери: у апстрима в комментарии записано, как отказ открыть базу, записанную более новой
сборкой, спрятал от пользователя всю историю целиком — каждая команда `chat_*` открывает
хранилище, и один номер версии превратил и список, и загрузку, и сохранение в ошибки,
которые фронтенд глотает. Здесь нечитаемый чат — это одна строка, пропавшая из списка;
остальные открываются как обычно, а сам файл остаётся на диске нетронутым.

**Контракт формата — отдельный файл.** `domain/chat_record.rs`: `schemaVersion`, поля,
`derive_title`, `check_id`. Отдельный, потому что это договор с двумя сторонами — с
фронтендом, который читает эти поля обратно, и с любой будущей сборкой, которая будет читать
написанное этой. Тест перечисляет имена полей на проводе: переименование, о котором знает
только текущая сборка, — это чат, молча потерявший транскрипт. Более новый `schemaVersion`
отличается от повреждения **до** разбора и по нему принимается решение: не трогать файл.

**Хранятся два списка, не один.** `messages` видит модель, `blocks` видит читатель, и одно
из другого не выводится: один вызов инструмента — это два сообщения и один блок, а свёрнутая
деталь — блок без сообщения. Восстановление любой половины из другой теряет ту, которую
угадали.

**Заголовок берётся из первой фразы пользователя**, а не у модели: запрос на чат стоит денег
и может не ответить, а первая строка первого вопроса — это ровно то, что человек и написал бы.
Обрезается по слову.

**Два живых дефекта, вскрытых восстановлением чата.** Оба — про границу turn'а, о которой
редьюсер не знал:

1. `seq` нумеруется **внутри turn'а** и у каждого нового начинается с единицы, а `lastSeq`
   жил в состоянии разговора. Второй вопрос в сессии: все его события выглядели как повтор и
   отбрасывались — ответ не появлялся вообще. Сброс курсора там, где turn начинается, — в
   `appendUserMessage`.
2. Блок прозы адресовался как `round:1:message` — на весь разговор. Проза второго вопроса
   дописывалась в абзац первого, посреди транскрипта. Ключ теперь включает номер turn'а,
   который **считается** (количество блоков `user`), а не хранится, — поэтому он же верен для
   транскрипта, поднятого с диска.

**Мутации: двенадцать, все пойманы.** Бэкенд: список без фильтра по рабочему каталогу;
обратный порядок; `createdAt`, сбрасываемый при каждом сохранении; любой `id` как имя файла;
разбор без проверки версии; повреждённый файл, роняющий весь список. Фронтенд: курсор `seq`,
переживающий turn; ключ прозы без turn'а; сохранение без пометки «есть что сохранять»;
`open`, забывающий историю модели; сохранение на паузе подтверждения; новый id на каждый turn.

**Что сознательно не перенесено:**

- `pending_resume` — незавершённый turn на паузе подтверждения не сохраняется вовсе (эффект
  срабатывает только на `done`/`cancelled`). После перезапуска разговор открывается без
  зависшего раунда, и вопрос задаётся заново. Вернуть, когда пауза станет долгой — например,
  с фоновыми процессами (этап 7).
- `archived` — в прототипе нет ни архива, ни его переключателя. `chat_delete` есть в бэкенде
  и в обёртках, кнопки в сайдбаре нет: рисовать элемент, которого нет в прототипе, — не по
  этому плану.
- `memory_extracted_ordinal` и `active_plan_id` — поля чужих фич (память, планы), этап 6.
  Добавятся полем с `#[serde(default)]`, без смены версии.

**Что куда привязано в UI:** `useChatHistory(workspace.path)` → `Sidebar.chats`;
`agent.chatId` → `Sidebar.activeChat` (подсветка строки); `agent.open` →
`Sidebar.onSelectChat`; `onSaved: history.refresh` в `useAgentTurn` — список
перечитывается с диска после каждого сохранения, а не достраивается в `App.tsx`: что попадает
в список и в каком порядке — правило хранилища, и копия этого правила во фронтенде может с ним
разойтись. `App.tsx` при этом стал короче: локальный `chats` и `activeChat` удалены,
`ChatSummary` уехал из `types.ts` в `lib/chat.ts`, к командам, которые его возят.

**Выбор папки — системный диалог.** `@tauri-apps/plugin-dialog` + `dialog:allow-open` в
`capabilities/default.json`; обёртка `lib/dialog.ts`, `useWorkspace.pick()`. Модальное окно с
путём, набираемым руками, удалено. Это единственное место, где приложение **не** рисует диалог
сам: у файлового выбора есть боковая панель пользователя, его недавние места и его
представление о том, где что лежит, — своя реализация была бы худшим файловым браузером в
правильных цветах. Отмена — не путь и не ошибка: тост при нажатии «Отмена» был бы странным
ответом. Сообщение, набранное до открытия папки, после выбора **отправляется**, а не теряется:
поле ввода композер уже очистил.

**Первый живой запуск: «ключ API не сохраняется».** Бэкенд оказался ни при чём — путь
`llm_provider_save` → `llm_api_key_save` → `llm_settings_get` теперь закрыт тестом на уровне
команд (ключ переживает, `hasApiKey` возвращается `true`, форма провайдера с лишним полем
`hasApiKey` принимается). Настроек на диске не оказалось вовсе, то есть сохранение не
доезжало до бэкенда. Три причины, все в окне:

1. **Enter в поле ничего не делал.** Везде в приложении Enter отправляет — композер, поле
   пути к папке. Форма настроек молчала, и это неотличимо от формы, которая сохранила.
   Теперь `<form>` с `onSubmit` **и** явный `onKeyDown`: неявная отправка по Enter — та
   вещь, которую webview вправе не делать, а проверить это можно только в webview.
2. **Модалка обрезалась.** `.modal { overflow: hidden }` без `max-height`: при 520px
   (минимальная высота окна) диалог высотой 606px вылезал с обеих сторон, прокрутки не было,
   и кнопка Save с ним. Теперь `max-height: calc(100vh - 40px)`, шапка и подвал на месте,
   середина прокручивается.
3. **Успех был неотличим от неудачи.** Поле ключа очищается в любом случае: сохранилось —
   плейсхолдер «stored», не сохранилось — «not configured», и разница видна, только если
   знать, куда смотреть. Теперь форма прямо говорит «Saved.», и слово исчезает при первой же
   правке — «Saved.» над изменённым ключом было бы той же ложью наоборот.

**Готово, когда:** живой turn в окне — стрим текста, карточки вызовов с разворачиванием,
пауза на подтверждении и корректное возобновление, прерывание, список сессий в сайдбаре.
`src/mock/data.ts` удалён.

---

## Этап 4. Контекст: перенос логики из TypeScript в Rust

Главный риск переноса (05-gaps §5.7.1): ~2000 строк, которые в Atlas живут во фронтенде,
а обязаны быть в ядре.

| Фича | Что |
|---|---|
| **F-4.1** ✅ | `contextCompaction.ts` (216) → `domain/compaction.rs`: оценка токенов, порог, план сжатия. Разбор ниже |
| **F-4.2** ✅ | Ошибка «контекст переполнен» → сжать и повторить вместо падения (CA-6.4). Разбор ниже |
| **F-4.3** ✅ | Превентивное сжатие, заметка в транскрипте, ручное сжатие. Разбор ниже |
| **F-4.4** ✅ | `assistantConfig.ts` (2051) → `domain/prompt.rs`. Взят **как образец**, не построчно. Разбор ниже |
| **F-4.5** ✅ | `estimateToolSchemaTokens` — схемы инструментов тоже стоят токенов, часто забываемая статья (CA-6.6). Разбор ниже |
| **F-4.6** ✅ | Режимы разговора (`domain/conversation_mode.rs`) — разные наборы инструментов и разные системные промпты для agent/plan/ask. Разбор ниже |
| **F-4.7** ✅ | `ContextPanel.tsx` показывает реальный `ContextUsage`. Разбор ниже |

`describeToolActivity`/`describeToolResult` остаются во фронтенде: это рендер, не логика.

**F-4.7: счётчик контекста, подключённый к тому, что он показывает.**

**Чем он был неправ — тремя способами.** Показывал `usage.totalTokens` от провайдера: во-первых,
это промпт **и** ответ уже оплаченного запроса, а вопрос «сколько осталось в окне» — про промпт
**следующего**; во-вторых, до первого ответа он не показывал ничего, хотя ~4 000 токенов
(промпт + схемы) уже потрачены; в-третьих, не двигался, пока ход не отчитается.

**`ContextUsage` — три части, а не одно число**, потому что вопрос читателя не «сколько
токенов», а «что я могу с этим сделать»: сжатие укорачивает `conversation` и ничего больше,
остальные две платятся при любом разговоре. Отсюда и подсказка в три строки.

**Та же арифметика, что у порога, отдана наружу, а не посчитана второй раз на TypeScript.**
Это оценка, и счётчик честен уже тем, что он счётчик, — но это оценка, которая **решает**, так
что читатель, глядя, как он наполняется, смотрит на то, что свернёт ему разговор.
`compacts_at` берётся из `TRIGGER_PERCENT`: шкала с красной зоной не там, где случается сжатие,
хуже отсутствующей шкалы.

**Настоящие числа провайдера остались — рядом, а не вместо.** В подсказке строка «последний
запрос на самом деле стоил N» (`promptTokens`). Оценка и факт стоят рядом; прятать факт за
оценкой было бы нечестно, а выбрасывать оценку — вернуть счётчик, который до первого ответа
пуст.

**Одна правда про размер окна.** `contextLimit` больше не проп: лимит приходит внутри
`ContextUsage` из той же сессии, по которой считается порог. Ноль или опечатка читаются как
«окна нет» — и теперь одинаково в обоих местах, а не только в `should_compact`.

**Когда пересчитывается:** первый рендер, начало хода, конец хода, открытие или очистка чата и
сразу после сворачивания. Один дешёвый IPC (арифметика плюс одна сериализация схем, без
обращения к модели).

**Панели нет, и это решение.** План называл фичу по имени апстримного `ContextPanel.tsx`, но в
этом приложении место для контекста — счётчик в шапке, а в прототипе отдельной панели нет.
Разбивка живёт в его подсказке. Поповер с числами и явной кнопкой был бы лучше нынешнего «клик
сразу сворачивает без предпросмотра» — записано как следующий шаг, а не сделано молча.

**Мутации: шесть, все пойманы.** Счётчик снова ждёт отчёта прошлого хода; показан только итог
без разбивки; кольцо рисуется против выдуманного окна; оценка не обновляется после
сворачивания; метка сжатия не там, где порог; окно нулевого размера стало шкалой.

---

**F-4.6: три режима разговора — и это ворота, а не просьба.**

**Третья ось, независимая от двух прежних.** `ToolScope` говорит, *куда* инструмент может
дотянуться (граница, проверяемая по файловой системе). `ApprovalPolicy` — *кто должен
согласиться* перед вызовом. Режим говорит, какие инструменты вообще предлагаются, и это выбор
пользователя про разговор, а не средство защиты: человек, обдумывающий изменение, не хочет,
чтобы агент начал его вносить посреди мысли.

**Наборы:**

| | читать, grep, git | todo | писать/удалять/двигать | `runCommand` |
|---|---|---|---|---|
| **Agent** | ✅ | ✅ | ✅ | ✅ |
| **Plan** | ✅ | ✅ | — | — |
| **Ask** | ✅ | — | — | — |

**`runCommand` только в Agent — единственное спорное решение здесь, и оно записано.** Прогнать
тесты во время планирования было бы полезно, но ничто в приложении не отличает `cargo test` от
`rm -rf` до запуска строки: аргумент — непрозрачная строка, о чём прямо говорит
`domain::command_exec`. Режим, чьё обещание «ничего не изменится», не может держать его,
предлагая инструмент, который может. Цена реальная и записана: план нельзя проверить сборкой.

**`todo` в Plan**, потому что план — это и есть список шагов, а другого списка до этапа 6 нет.
Он меняет рабочую память чата и ничего на диске.

**Ворота в двух местах, и второе важнее.** Первое: инструмент, которого режим не даёт, не
попадает в запрос. Второе: если модель всё равно его вызовет — а модель, пользовавшаяся
`writeFile` пять раундов назад, вызовет его по памяти, — `preflight_tool_call` отвечает ошибкой
с причиной, и она уходит модели как результат вызова, не роняя ход. Предложение в промпте
воротами не является.

**Формулировка ошибки — для модели, а не для лога:** «инструмент недоступен в этом режиме
разговора — пользователь выбрал режим, который не меняет репозиторий. Скажи, что нужно было бы
сделать, вместо того чтобы делать». Без причины модель просто пробует ещё раз.

**Промпт: третье системное сообщение, не ветка внутри `INSTRUCTIONS`.** Три копии общего текста,
отличающиеся абзацем, — три места правки одного правила. Текст режима постоянен внутри режима,
так что кэшируемый префикс остаётся постоянным, пока режим не сменили. Каждый абзац говорит,
*для чего* режим, а не какие у него инструменты: инструменты уже едут в запросе, и решает их
`conversation_mode::tools` — промпт, описывающий набор уже, чем объявленный, это правило, которое
модель смотрит, как само себя нарушает.

**Режим живёт в `AgentState`, как и политика подтверждений, и не персистится.** Не передаётся с
каждым ходом по той же причине, что и она: приостановленный ход возобновляется из чекпоинта,
который старше чипа, и режим, протянутый через обе точки входа, пришлось бы правильно
переслать ровно в тот момент, когда это легче всего забыть. Смена действует со следующего хода
и никогда задним числом — ход, уже стоящий на карточке подтверждения, выполнит вызовы, которые
пользователь решил.

**UI: второй чип рядом с чипом разрешений, потому что это два разных вопроса.** Прототип
рисует один чип и подписывает его «Режим разрешений» — кто соглашается перед вызовом. Новый
отвечает на другой: существует ли вызов вообще. Ход в режиме Plan всё равно спрашивает
подтверждения, а агент «без присмотра» всё равно не может писать в Ask.

- `Composer` получает `conversation` / `onConversation` пропсами — компонент не зовёт команды.
- Состояние — в `useConversationMode`, не в `App`: так `AGENTS.md` и просит, и так его можно
  протестировать.
- Чип двигается **после** того, как бэкенд согласился. Чип, показывающий «Plan» над ходом,
  который всё ещё вооружён, — единственный отказ, стоящий здесь защиты.
- `Dropdown` получил необязательный `label`: «plan» — это то, что десериализует
  `domain::conversation_mode`, а не слово для меню.

**Чего нет, с причинами.** Уведомления о *смене* режима отдельным сообщением
(`buildModeChangeNotice` у апстрима): промпт называет текущий режим каждый ход, так что модель
всегда его видит; отдельная машинерия про переход — не про эту фичу. Инструмента
`requestModeSwitch`: у апстрима он есть, здесь режим меняется одним кликом в том же окне, и
инструмент, просящий пользователя нажать кнопку рядом с ним, — церемония.

**Мутации: семь, все пойманы.** Запрос объявляет все инструменты независимо от режима; ворота в
preflight убраны (осталось только необъявление); Plan тихо получил `editFile` обратно; режим не
доехал до промпта; чип двинулся раньше бэкенда; в меню показано проводное значение вместо слова;
чип режима заменил собой чип разрешений; чип разрешений снова завёл собственное состояние; вернулась мёртвая третья подпись; «Auto» отправляет `false`; чип разрешений двинулся раньше бэкенда.

**Хвост F-3.6, подобранный здесь.** Чип разрешений в композере был нарисован, но ни к чему не
подключён: `approval_set_unattended` жил на бэкенде два этапа без способа до него дотянуться.
Теперь подключён — и с двумя состояниями вместо трёх из прототипа, потому что два — это то, что
есть у `ApprovalPolicy`: спрашивать перед всем, что меняет дерево, или не спрашивать вовсе.
Третья подпись была бы органом управления, который двигается и ничего не меняет, а это хуже
отсутствующего органа: он читается как гарантия. «Always allow this tool» — третье настоящее
состояние, и оно не чип: на него отвечают на карточке, про один инструмент, в моменте.

Про включение «Auto» говорится вслух, про выключение — нет: вернуть подтверждения безопасно,
а снять их значит, что следующая запись случится, и никто её не увидит.

Оба чипа — один и тот же хук `useBackendSetting`: значение живёт на бэкенде, и правило про него
ровно одно — **чип двигается после того, как бэкенд согласился**. Правило стоит того, чтобы
лежать в одном месте, а не в двух.

---

**F-4.5: постоянная цена запроса, которую оценка не видела.**

Оценка контекста считала сообщения. Но самое крупное в запросе — не сообщения: системный
промпт едет **перед** историей на каждом раунде, схемы инструментов — **рядом** с ней. Ни то,
ни другое не сжимается вместе с разговором, и окно, измеренное одной историей, выглядит
свободнее, чем оно есть.

**Что это стоит здесь, в цифрах:** 14 инструментов сериализуются в 11 406 символов ≈ 2 852
токена, промпт — 4 384 символа ≈ 1 096 токенов. Итого ~3 948 токенов **в каждом запросе
каждого раунда**, прежде чем в нём появится хоть одно слово разговора. На окне 8k это
половина. У апстрима (24 инструмента, ~37 800 символов) пропуск этой статьи давал оценку
стабильно на ~36% ниже `promptTokens` самого провайдера.

**Схемы считаются сериализацией, а не константой.** Описания инструментов правятся там, где
живут инструменты; записанное сюда число было бы неверным в первый же раз, когда одно из них
обрастёт абзацем. Оценка чуть ниже проводной формы, которая заворачивает каждую запись в
`{"type":"function","function":{…}}` — около 40 символов на инструмент, внутри собственного
шума оценки.

**Меняющаяся половина промпта сознательно не считается.** Дата, путь, оболочка и чеклист —
несколько сотен символов против десятков тысяч; чтобы посчитать их точно, пришлось бы завести
`fixed_request_tokens()` рабочий каталог и список todo, которые ей больше ни для чего не нужны.

**Ошибка сериализации даёт ноль, а не ошибку.** Оценке позволено быть приблизительной; ей не
позволено уронить ход, который она оценивает.

**Мутации: пять, все пойманы.** Постоянная цена не прибавлена вовсе; забыты схемы; забыт
промпт; пустой список инструментов даёт цену JSON-скобок; считаются имена инструментов без
описаний.

**Оставлено для F-4.7.** Счётчик в шапке показывает `usage` от провайдера — правду, но
прошлого хода, и `totalTokens` вместо «сколько займёт следующий запрос». До первого ответа он
показывает ноль, хотя ~3 948 токенов уже потрачены. Это про панель контекста, а не про правила
сжатия.

---

**F-4.4: что модели говорят до того, как что-то спросят.**

До этой фичи системного промпта не было вообще: агент входил в чужой репозиторий без имени,
без правил обращения с инструментами и не зная, какая папка открыта. Всё, что он о себе знал,
приходило из описаний инструментов.

**Два сообщения, не одно, и в этом весь смысл.** `INSTRUCTIONS` — одни и те же байты в каждом
запросе каждого turn'а каждой сессии. Второе сообщение — только то, что верно прямо сейчас:
дата, открытая папка, оболочка, платформа, чеклист в том виде, в каком он на этом раунде.
Слитый промпт был бы префиксом, который меняется каждый раз, когда модель закрывает пункт
todo, — а кэш промпта у провайдера держится ровно на неизменном префиксе (этап 7 называет это
крупнейшей статьёй экономии на длинных сессиях). Тест `the_constant_half_is_the_same_bytes_whatever_the_turn`
сравнивает первое сообщение для двух разных контекстов: если в него просочилась хоть одна
подробность хода, кэшировать нечего.

**`domain/`, а не `services/context/` из исходного плана** — по тем же причинам, что и
`compaction.rs`: сборка текста не делает I/O. Дату не читают из часов, её передают строкой;
это и есть то, что позволяет тесту утверждать что-то о готовом тексте.

**Промпт не входит в историю.** Он приклеивается перед запросом в `prepend_system_prompt` и
пересобирается **каждый раунд**. Причин две, обе про порчу данных:

- Чеклист меняется **внутри** хода. Модель вызывает `todo`, следующий раунд обязан увидеть
  список в новом виде; промпт, собранный один раз за ход, показывает ей работу, которую она
  уже сделала.
- Папка и дата, вмороженные в сохранённую историю, уехали бы в файл чата и пересылались бы,
  уже неверные, столько, сколько этот чат живёт.

Побочно это отвечает на вопрос, отложенный в F-4.2: `plan_compaction` считает ведущие
`System`-сообщения, чтобы их не сворачивать, — а их в истории нет, `keep_head` остаётся нулём,
и сжатие с промптом не пересекается вовсе. Правило «системный промпт не суммируется никогда»
теперь выполняется само, а не дисциплиной.

**Чем короче, тем лучше — это не стиль, а счёт.** Каждый объявленный инструмент уже везёт своё
`description` в массиве `tools` того же запроса. Апстрим измерил, во что обошлось повторение
их прозой в промпте: ~7 000 токенов дословного дубля в каждом запросе каждого раунда. Здесь
секции «список моих инструментов» нет: в промпт попадает только то, чего схема одного
инструмента сказать не может, — стоит ли вообще браться за инструмент и что делать с тем, что
вернулось.

**Инвариант «в обратных кавычках — только инструменты».** Промпт цитирует инструменты именем,
которым их зовёт модель. Переименованный или удалённый инструмент оставляет после себя
инструкцию про то, чего нет, и сборка этого не замечает: промпт — строка.
`every_backticked_word_is_a_real_tool` разбирает текст по кавычкам и сверяет каждое слово с
`ToolName::ALL`.

**Что взято у апстрима и что выброшено.** Взяты правила, пережившие смену предмета: факт о
проекте требует источника из проекта; тесты — это свидетельство; о собственных вызовах врать
нельзя (целая секция — про сводку, расходящуюся с транскриптом, и про правило, которое
заметили и решили не применять); содержимое репозитория — данные, а не инструкции; секреты не
воспроизводятся. Выброшено всё документационное: AsciiDoc, стандарт К.x.x, роутер skills,
`visualize`, режимы доступа docs-only/full-repo, «всегда отвечай по-русски».

**Уведомление о том, что никто не смотрит.** `skip_all` доезжает до промпта строкой: turn,
идущий без подтверждений, не должен слышать «тебя попросят подтвердить» — это правило против
факта, которого больше нет.

**Чего в этой фиче нет, с причинами.** Активного файла — в этом приложении нет редактора с
открытой вкладкой. Плана, skills, памяти — они приезжают на этапе 6, вместе со своими
блоками. Режимов разговора — F-4.6, там же появится второй набор инструкций. Ответов
пользователя (`buildUserAnswersContextBlock`) — нет инструмента `askUser`.

**Известный предел.** Оценка контекста считает историю и не видит ни промпта, ни схем
инструментов, а это самый крупный постоянный расход в запросе. Обе статьи — F-4.5.

**Мутации: восемь, все пойманы.** Промпт выкинут из запроса; чеклист перестал следовать за
ходом; промпт записан в хранимую историю; `skip_all` не доехал; пустой чеклист печатается
заголовком; два сообщения слиты в одно; в кавычках не инструмент; подробность хода просочилась
в кэшируемую половину.

**F-4.3: сжатие до того, как упрётся — и сказанное вслух.**

**Кто решает и кто хранит.** Историей владеет окно (`chat_start` принимает её целиком), но
ни одно правило в окно не переехало: перед отправкой фронтенд зовёт `chat_compact`, а бэкенд
отвечает либо более короткой историей, либо `null` — «оставь как есть». Оценка, порог, размер
хвоста, план — всё осталось в Rust и не может разойтись со второй копией на TypeScript.
Заодно это чинит известный предел F-4.2: сжатое **сохраняется** у владельца, а не теряется
вместе с turn'ом, поэтому резюме оплачивается один раз, а не каждый ход.

**Размер окна теперь есть** — `contextLimit` в `ProviderConfig` и поле в форме настроек.
Пустое поле значит «не знаю», и это же выключает превентивный проход: приложение ходит в
шлюзы, о которых ничего не знает, и сжимать по догадке значит выбрасывать разговор ради
проблемы, которой может не быть. Ноль и опечатка читаются так же, а не как окно нулевого
размера.

**Ручное сжатие — тот же путь с `force`.** Порог пропускается, всё остальное нет: разговор,
в котором нечего сворачивать, остаётся как есть, кто бы ни просил, и ответ «нечего» виден.

**Счётчик контекста дорисован по прототипу** — кольцо и `8k / 200k` вместо голого числа.
Число токенов без окна не значит ничего: 8k — это пустяк на 200k-модели и конец дороги на
8k-й. Он же и кнопка «свернуть сейчас»: место, куда смотрят, когда беспокоятся о контексте.
Во время turn'а выключен — укорачивать историю из-под работающего запроса нечего предлагать.

**Заметка в транскрипте — блок `notice`, ничей.** Не под «Agent»: это говорит приложение, а
не модель о себе. Транскрипт при этом сохраняет **все** сообщения — короче стала копия
модели, и именно поэтому о ней надо сказать: история, исчезающая молча, выглядит как агент,
забывший без причины.

**Мутации: пять на фронтенде, все пойманы.** Короткая история, полученная и выброшенная;
молчаливое сворачивание; событие, проигнорированное редьюсером; заметка, приписанная агенту;
счётчик, переставший быть кнопкой.

**F-4.2: переполненный контекст перестал заканчивать turn.**

Было так: провайдер отвечает «не влезает», `stream_one_round` отдаёт `TurnError::Provider`,
turn заканчивается — с уже выполненными инструментами и без возможности продолжить. Теперь
раунд, получивший такой отказ, сжимает историю и спрашивает снова.

**Реактивно, а не по оценке.** Проход делается потому, что запрос **действительно** отвергли,
а не потому, что прикидка предположила, что отвергнут. Порог из F-4.1 пока никто не дергает —
для него нужен размер окна, а его неоткуда взять (см. ниже).

**Один раз за раунд.** Второй отказ после уже сжатой истории — не про её длину. Сжать ещё раз
значит потратить второй запрос, потерять ещё кусок разговора и всё равно упасть.

**Хуже, чем превентивно: хвост 6 сообщений вместо 12.** Окно не «почти заполнено», оно уже
переполнено, и проход, сжимающий так же мягко, как тот, что не предотвратил переполнение,
кончится тем же.

**Провал сжатия возвращает исходный отказ.** Ни одного сообщения не удалось свернуть (один
гигантский результат чтения занимает окно сам по себе), или суммаризатор сам упал — читать
надо «разговор не помещается», а не «суммаризатор тоже не смог».

**Пустое резюме не применяется никогда.** Это не короткая история, а удалённая.

**Запрос на резюме идёт без схем инструментов.** Единственный запрос, который существует
ради экономии контекста, не должен возить в себе описания инструментов — и суммаризатор со
схемами пытается ими воспользоваться вместо того, чтобы описать работу.

**Каждое сообщение обрезается до 1500 символов перед отправкой суммаризатору.** Иначе запрос,
который должен освободить место, оказался бы самым большим за сессию: результат чтения файла —
тридцать тысяч символов, и их в сворачиваемой части десятки.

**Распознавание — список фраз, а не код ответа.** В OpenAI-совместимом протоколе кода для
этого нет, каждый шлюз формулирует по-своему. Дорогая ошибка здесь — ложное срабатывание:
посторонний сбой, отвеченный запросом на резюме, стоит денег и куска разговора.

**Событие `historyCompacted`.** История, исчезающая сама по себе, — худший исход: модель
перестаёт помнить сказанное, и ничто в окне не объясняет почему. Заметка в транскрипте — F-4.3.

**Мутации: семь, все пойманы.** Сжатие на каждом отказе; любая ошибка как переполнение;
«нечего сворачивать», прочитанное как отмена; молчаливое сжатие; мягкий хвост на повторе;
пустое резюме поверх истории; схемы инструментов в запросе на резюме.

**Известный предел, с триггером.** Сжатая история остаётся внутри turn'а: фронтенд владеет
историей и на следующем ходу пришлёт снова длинную, то есть переполнение и резюме повторятся
раз за ход. Вернуть сжатое наружу — решение не про эту фичу: фронтенд хранит только пары
«вопрос — ответ» без раундов с инструментами, и что именно он должен хранить, решается в
F-4.4 (сборка промпта). Превентивный проход (F-4.3) отдаёт историю обратно вместе с
`ContextUsage`, туда же приедет и это.

**Размера окна пока нет.** `should_compact` без него честно отвечает «нет»; поле в
`ProviderConfig` и в форме настроек появится вместе с превентивным проходом.

---

**F-4.1: правила сжатия. Только арифметика, ни одного вызова.**

Резюме стоит запроса к модели — это сервис. Здесь лежит то, что решает, стоит ли этот запрос
делать и о каких сообщениях он будет: `estimate_tokens`, `should_compact`, `plan_compaction`,
`apply`. Поэтому `domain/compaction.rs`, а не `services/context/` из исходного плана: по
`AGENTS.md` чистые правила без I/O — это `domain`. Сам вызов суммаризатора приедет в F-4.3 и
будет сервисом.

**Кэш резюме не переносится.** У апстрима хранится вся переписка, а проводная история
собирается заново на каждом ходу — отсюда и кэш, чтобы не суммировать с нуля. Здесь сжатая
история **и есть** история с этого момента (транскрипт для читателя — отдельный список
блоков), так что предыдущее резюме — это просто сообщение, с которого начинает следующий
проход. Минус три функции (`CompactionCache`, `isCacheValid`, `estimateWireChatTokens` с
кэшем) и один класс ошибок: кэш от другого разговора.

**Зато появляется правило, которого у апстрима быть не могло.** У него сообщение несёт свои
вызовы инструментов внутри себя; здесь история — проводной формат, и результат инструмента
живёт отдельным сообщением, осмысленным только после того, которое его запросило. История,
начинающаяся с ответа на исчезнувший вопрос, отвергается провайдером целиком. Поэтому рез
сдвигается назад, пока не окажется в месте, где историю вообще законно резать, а раунд,
который длиннее всего хвоста, оставляется целым: длинная история — меньшее зло, чем
отвергнутая.

**Порог — проценты, а не доля.** `10_000 * 0.8` не равно `8_000` ни в `f32`, ни в `f64`, так
что записанный долей порог срабатывает на токен позже, чем читается. Заметно это только в
тесте, который попадает ровно в границу, — он и заметил.

**Системный промпт не суммируется никогда** (F-4.4 его ещё не добавил, но правило дешевле
написать сразу): инструкции — не разговор, и свернуть их в резюме значит тихо переписать
поведение агента.

**Мутации: шесть, все пойманы.** Рез посреди раунда; системный промпт как обычное сообщение;
отсутствие минимальной длины разговора; неизвестное окно, подменённое догадкой; бесплатные
аргументы вызова в оценке; хвост, потерянный при склейке.

**Не сделано в этой фиче:** лимит контекста пока неоткуда взять — у `ProviderConfig` его нет,
и `should_compact` без него честно отвечает «нет». Поле добавится вместе с триггером (F-4.2),
туда же и в форму настроек.

**Готово, когда:** длинная сессия компактится сама, в транскрипте видна заметка, ошибка
«превышен контекст» приводит к компакции и повтору, а не к падению. ✅ Этап закрыт.

---

## Этап 5. Индекс и поиск

> **Переписан 2026-09-17 после разведки по коду апстрима.** Что изменилось: решение по
> эмбеддингам (только встроенная модель), измеренный объём (не ~8 500, а ~13 100 строк), нарезка
> на фичи — и, главное, **три пункта из семи «что нельзя потерять» описывали код, которого в
> апстриме нет**. Разбор ниже.

### Прогресс этапа (на 2026-09-18)

**Этап 5 завершён: F-5.1 … F-5.14.** Все пункты «Готово, когда» в конце этапа выполнены: по
смыслу находит в `.rs`/`.ts`/`.java`, а не только в прозе (стенд F-5.13c, 93 вопроса на трёх
настоящих репозиториях); пропущенный файл виден счётчиком в окне (F-5.13d); повторный синк
эмбеддит только изменившиеся чанки (F-5.9/F-5.10); первый синк идёт в своём потоке и не держит
открытие папки (F-5.12); `readFile` с `outline` — F-5.14.

До F-5.13 (F-5.3 — тремя частями). Лексический слой завершён:
обход репозитория → разбор на семи грамматиках + Markdown → чанки → SQLite/FTS5 → запрос.
**Поиск по ключевым словам работает на всём репозитории без единого вектора**: первый синк
этого репозитория — 232 мс, повторный — 3 мс, запрос — доли миллисекунды.

| | Фича | Итог |
|---|---|---|
| ✅ | F-5.1 | типы индекса; `detect_language` без `None` (B-8), B-1 в прозаической нарезке |
| ✅ | F-5.2 | `repository_id` — имя индекса по remote, не по ревизии |
| ✅ | F-5.3a/b/c | `IndexStore`: схема и версии, чанки + FTS + символы, `search_bm25` и отметки эмбеддингов |
| ✅ | F-5.4 | один обход tree-sitter на 7 языков + Markdown через `pulldown-cmark` |
| ✅ | F-5.5 | нарезка кода по объявлениям (B-1 в backward-gap), предел 1 МиБ, отказы с причиной |
| ✅ | F-5.6 | `sync` — инкрементальный, атомарный по файлу, дешёвая пересборка derived |
| ✅ | F-5.7 | `search_query` — FTS-запрос, токены кода, snake_case как имя, camelCase → фраза |
| ✅ | F-5.8 | model2vec под LFS; замер памяти: ~1,07 ГБ живых данных |
| ✅ | F-5.8a | выгрузка при простое (10 мин) + возврат памяти ОС |
| ✅ | F-5.8b | словарь только русский + английский: ~520 МБ вместо ~1,07 ГБ, без потерь для ru/en/кода |
| ✅ | F-5.8c | таблица int8 в памяти, `model2vec-rs` заменён: **~320 МБ** |
| ✅ | F-5.9 | векторы в SQLite (int8, одна транзакция на батч — B-11 закрыт), поиск перебором: 11 мс на 100 тыс. |
| ✅ | F-5.10 | `RepoIndexer`: синк слов → векторов, один за раз, поиск во время синка; тиры не нужны |
| ✅ | F-5.11 | `FileWatcher`: сохранение → синк после 300 мс тишины (не позже 3 с), серия — один синк; B-12 |
| ✅ | F-5.12 | `WorkspaceIndex`: индекс открытой папки следует за `workspace_open`, одна модель на все папки, события в окно |
| ✅ | F-5.13a | ядро поиска `code_search`: имена → слова + смысл через RRF, строки для `readFile`, честная подсказка без модели |
| ✅ | F-5.13b | инструмент `semanticSearch` во всех режимах: `fts`, 240/1 200 символов текста, поиск открытой папки через `ToolDeps` |
| ✅ | F-5.13c | замер качества на 81 вопросе, 3 репозитория: ранжирование перенастроено (MRR 0,450 → 0,547); ru-en остаётся, `potion-code` и порог сходства отвергнуты |
| ✅ | F-5.13c-2 | сигналы Semble по одному на 93 вопросах: ни один не вышел из шума — код не менялся |
| ✅ | F-5.13d | окно: статус индекса рядом с папкой — по событиям с первого кадра, снимок после перезагрузки, счётчик пропусков |
| ✅ | F-5.14 | `readFile` с `outline`: объявления и заголовки со строками вместо текста (CA-3.2) — без индекса, не открывает запись |
| ❌ | отказ | `potion-code-16M-v2` — замер F-5.13c: слабее ru-en (0,487 против 0,547), русский не понимает |
| ❌ | отказ | порог сходства — замер F-5.13c: косинусы ответов и мусора перекрываются целиком |
| 💭 | предп. | словарь по частоте — отложен замером F-5.13c: модель даёт полноту и русский, память и так отдаётся при простое |

Тестов в Rust на конец этапа 5: 745 + стенд качества, запускаемый вручную; во фронтенде — 128 (было 569 до F-5.4). Каждая фича прошла мутационную проверку.

### Решение: эмбеддинги только встроенные

`Local` (Model2Vec `potion-multilingual-128M` int8, 256 измерений) переносится. Внешний
HTTP-провайдер эмбеддингов — **нет**.

**Что это убирает целиком:**

| Файл | Строк |
|---|---|
| `infra/embedding_providers/remote.rs` | 205 |
| `services/embedding_config.rs` | 387 |
| `infra/embedding_provider_manifest.rs` | 116 |
| `infra/embedding_credentials_store.rs` | 77 |
| диспетчер в `infra/embedding_providers/mod.rs` | ~108 из 118 |
| `EmbeddingProviderKind`, `EmbeddingPreset`, `EmbeddingProviderConfig`, `ResolvedEmbeddingConfig`, `embedding_model_id()`, `DEFAULT_REMOTE_DIMENSIONS` | ~130 из `domain/embeddings.rs` |
| фронтенд: `EmbeddingsTab.tsx` + `certField.ts` + `useEmbeddingSetup.ts` | 732 |
| 6 из 10 команд в `commands/embeddings.rs` (конфиг, ключ, тест соединения) | — |

Плюс варианты `Http` и `Tls` в `EmbeddingError`, ключ в связке ключей и вкладка настроек.

**Что это упрощает сверх удаления — две находки, ради которых разведка и делалась:**

1. **Кэш провайдера не нужен.** `ensure_provider` + `EmbeddingProviderSlot` существуют потому,
   что провайдер был настраиваемым: мьютекс, отпечаток API-ключа, проверка устаревания. У
   `local.rs` уже есть `static MODEL: OnceLock<StaticModel>` — загрузка один раз сделана там, где
   ей место. Слот уходит целиком.
2. **Кулдаун на сбой не нужен.** `EMBEDDING_OUTAGE_COOLDOWN` (60 с) написан под **эпизодический
   сетевой** отказ: «VPN лёг, эндпойнт недоступен», где `ureq` висит десятками секунд перед
   каждым поиском, который всё равно свалится в лексический тир. У локального вывода
   эпизодического отказа нет: модель либо загрузилась, либо нет — навсегда, и `OnceLock` уже
   кэширует этот вердикт. Кулдаун, его глобальный `Mutex<Option<Instant>>` и тройка
   `note_/clear_/embedding_outage_active` уходят вместе с ним.

**Что НЕ уходит:**

- **Трейт `EmbeddingProvider` остаётся.** Реализаций сейчас четыре: local, remote и **два
  тестовых дубля** (`embedding_sync`, `embedding_index`). После правки — одна настоящая и два
  дубля, то есть ровно критерий `AGENTS.md`: «реальная вторая реализация, например тестовая».
- **Пространство имён модели остаётся.** `embedding_dimensions_meta_key` и один
  `LOCAL_MODEL_ID`: встроенная модель меняется между версиями приложения, и старые векторы надо
  отличать от новых. Один идентификатор, меняемый руками при смене весов.
- **`embedding_sync.rs` (1730) не трогается вовсе.** Единственные упоминания remote в нём —
  комментарии в тестах; вся машина синхронизации не зависит от провайдера.

**Чего это стоит, честно:**

- Качество. Статическая таблица на 256 измерений; собственный док апстрима велит ожидать
  просадку против полного `bge-m3` и напоминает, что BM25 работает всегда. Аварийного выхода на
  модель получше больше нет.
- ~146 МБ весов в репозитории (Git LFS) и в каждом установщике, ~512 МБ RSS после первого
  кодирования. Раньше это было опциональной ценой — теперь единственный путь.
- Корпоративная сборка в закрытом контуре больше не может ходить во внутренний шлюз эмбеддингов.

**Триггер на пересмотр:** качество поиска на настоящем репозитории кода оказалось непригодным
(замер на этапе F-5.13), либо понадобился второй потребитель ядра с другой моделью. Трейт на
месте, возврат — один модуль и одна ветка.

**Побочный выигрыш:** собственный план индексации апстрима делает обработку 429 **зависимостью**
этапа — первичная индексация кода на `Remote` первой упирается в лимит, и ошибка одного батча
прекращает весь синк. Со встроенной моделью этой зависимости нет: ни лимита, ни сети.

### Размер: ~13 100 строк, не ~8 500

Замер 2026-09-17 по файлам, которые действительно переносятся (уже без remote-части):
**13 146 строк**. Прежняя оценка занижала вдвое, и нарезка ниже сделана под настоящую.

### Три пункта прежнего списка описывали несуществующий код

Проверено чтением апстрима 2026-09-17:

| Прежний пункт | Что на самом деле |
|---|---|
| 1. `Language::PlainText` + `is_lexical_only()` — «правка уже сделана в апстриме» | **Нет ни того, ни другого.** `ALL_LANGUAGES` длиной 5, `plain_text.rs` отсутствует, `grep` по `lexical_only` — ноль совпадений. Это предложение из плана апстрима, которое не поехало |
| 2. Порядок проверки в `EmbeddingIndex::sync` (пропуск lexical-only **до** сравнения хэшей) | Зависит от пункта 1, значит тоже не существует |
| 4. `MAX_LEXICAL_ONLY_FILE_BYTES` (1 МиБ) | Константы нет |

Пункты 5 (`CHUNK_VERSION`/`INDEX_VERSION`/`DERIVED_VERSION`), 6 (инвариант якорей) и 7
(`links = "tree-sitter"`) проверены и верны.

**Следствие — самое важное в этой разведке.** Апстрим **удаляет файл из индекса**, если
`detect_language` вернул `None` (`services/repo_index.rs:406`), а языков пять: AsciiDoc,
Markdown, Java, JSON, YAML. В этом репозитории 56 `.rs`, 22 `.tsx`, 22 `.ts`, 16 `.css` и ни
одного `.java`: из 158 отслеживаемых текстовых файлов пять языков апстрима опознают **16**.
Перенос слоя как есть даёт семантический индекс на десятую часть приложения, молчащий об
остальном. `PlainText` + whole-file чанкинг здесь не улучшение, а **условие, при котором слой
вообще что-то делает** — см. B-8 в [07-upstream-findings.md](07-upstream-findings.md).

То же про B-1: паники в апстриме уже нет, невалидный спан молча пропускается, и цена дефекта —
не краш, а тихая потеря чанков. Порт добавляет второй путь чанкинга и корпус кода, так что
чинить надо в `spans_from_backward_gap_symbols` (сортировка + пропуск вложенных), а не
дисциплиной вызывающего.

**Чего из плана апстрима здесь нет:** половина его посвящена честной границе `DocsOnly` —
`hidden_by_access_boundary`, подсказка модели «попадания есть, но вне доступа»,
`requestFullRepoAccess`. В atlas-desktop режимов доступа нет, `ToolScope` — один корень, так что
эта половина не переносится. Идея из неё остаётся одна и она общая: **индексируемость — не
доступ**, и файл, выпавший из индекса, обязан иметь видимую причину.

**F-5.13 делится на четыре части** (a — ядро, b — инструмент, c — замер, d — окно): инструмент
без ядра не проверить, а замер без инструмента меряет не то, чем пользуется модель.

**F-5.14: `readFile` с `outline` — структура файла вместо текста (CA-3.2).**

Отложен из F-1.4, потому что зависит от разбора языков. `outline: true` возвращает
`fileOutline { path, entries, totalLines }`: каждое объявление или заголовок — имя, уточнённое
охватывающими (`Store.open`, тот же `qualified_name`, что у чанков), и строки, которые следующий
`readFile` возьмёт как есть. Диапазон при этом игнорируется.

- **Без индекса.** Файл разбирается тем же `indexer_for`, что и при синке, но на месте, один файл:
  работает до первого синка, в неиндексированной папке и на файле, который индекс не взял.
  Апстрим делал так же.
- **Не чтение.** Реестр прочитанного не пополняется: модель видела имена и номера строк, а не
  текст, который собирается заменить, — запись по-прежнему требует чтения.
- **Язык без разбора** (текст, JSON, YAML) — пустой список и `totalLines`: сколько стоило бы
  прочитать, всё равно известно.
- **Вид объявления (`kind`) не передаётся**, в отличие от апстрима: в `Symbol` его нет, а
  добавлять ради подписи — правка всех семи грамматик. Имя и вложенность передают почти то же.
- **Окно:** «Read · lib.rs · outline · 12 entries · 340 lines», в раскрытии — `start-end  имя`.

**Мутации: десять, все пойманы.** Флаг не проверяется; имя без охватывающих; начало и конец со
сдвигом; ноль строк; чтение записывается (дважды — до и после ветки); язык всегда «текст»; окно
не узнаёт outline; в окне нет строк.

**F-5.13d: статус индекса в окне.**

Рядом с путём папки в шапке чата — значок: «Indexing…», «Indexing 40%», «Indexed · 2 skipped»,
«Words only» (модель не загрузилась — ищет по словам), «Index failed». Подробность — во
всплывающей подсказке: сколько отрывков ищется по смыслу, почему смысл недоступен, сколько
файлов не взято.

- **Слушать с первого кадра, а не с открытия папки.** Первый синк начинается внутри
  `workspace_open`, до того как окно узнаёт путь; на маленькой папке он к тому времени кончен.
  `useIndexStatus` подписан на канал с монтирования, копит состояние по каждому `root` и
  показывает то, что относится к открытой папке. События чужой папки (синк, переживший смену
  папки) остаются при своей.
- **Снимок для перезагруженного окна** — команда `workspace_index_status` (`IndexSnapshot`:
  идёт ли синк, сколько векторов, сколько пропусков, ошибка модели). Снимок **никогда не
  перекрывает** уже пришедшее событие: он мог быть снят раньше.
- **Типы на проводе** — `IndexEvent`/`IndexSnapshot` в `lib/chat.ts`; вся логика состояния —
  чистые `applyIndexEvent`/`describeIndex` в `lib/indexStatus.ts`, без React.
- **Счётчик пропусков — только число.** Причины по файлам есть в `IndexStatus.skipped`, но
  списка в окне нет: число говорит «что-то не взято», а чтобы разбираться, какие файлы и почему,
  нужна отдельная панель. Триггер — первый вопрос «почему файл не находится».
- **Не показано после перезагрузки:** сбой наблюдения за папкой — он приходит только событием,
  в `IndexStatus` его нет. Повторное открытие папки сообщит снова.

**Модель в собранном приложении.** `bundle.resources` кладёт `models/<имя>` в
`Contents/Resources/models/<имя>`, а `bundled_model_dir` ищет ровно там. Проверено сборкой
`bun run tauri build --bundles app`: веса и токенизатор в `.app` побайтно те же (не LFS-указатели),
весь `.app` — 93 МБ. Живой прогон `tauri dev` с глазами на окно — за владельцем: окно Tauri
инструментам этой сессии не видно.

**Мутации: 23, все пойманы** — 17 во фронтенде (фаза снимка, сброс ошибки новым синком,
пропуски, прогресс, ошибка модели, накопление между синками, проценты, «Words only», подпись с
пропусками, снимок поверх события, фильтр по папке, снимок не читается, значок не выводится,
подсказки нет, имя команды, ошибка сбоя) и 6 в команде (каждое поле снимка и «нет снимка»).

**F-5.13c-2: сигналы Semble — каждый по отдельности; ни один не взят.**

По плану F-5.13 список сигналов Semble мерится по одному. В F-5.13c проверены штраф тестам и
(как противоположность связности) предел чанков на файл. Здесь — остальные, на копии конвейера
инструмента в стенде; базовая строка копии совпала с инструментом (0,548 против 0,547), сетка
после замера удалена. Модель ru-en, ΔMRR к инструменту:

| сигнал Semble | как проверен | 81 вопрос | 93 вопроса |
|---|---|---|---|
| больший вес слов, если в запросе идентификатор | 2,5 и 4 вместо 1,5 | −0,006 | −0,005 |
| буст определений | ×1,25 чанку-объявлению | +0,001 | +0,001 |
| стемминг идентификаторов | ×1,25 / ×1,5, если имя чанка совпало со словом запроса по основе | +0,005 / −0,002 | +0,003 / −0,003 |
| связность по файлу | ×(1 + α·(n − 1)), n — кандидатов из того же файла | α 0,1: −0,005; 0,25: −0,040 | −0,004 / −0,036 |

±0,01 на таком наборе — это один вопрос, сдвинувшийся на ранг: **все четыре — шум**, связность
с α 0,25 — вред (длинный файл с многими средними совпадениями вытесняет короткий точный).
Почему у Semble они работают, а здесь нет: то, что они дают, у нас уже сделано иначе — тир
имён (точное имя первым), буст пути, K = 3, FTS по префиксу и имя чанка ×4 в BM25.

**Набор дополнен 12 вопросами «где вызывается X» (`"answer": "caller"`)** — без них адаптивный
вес было не на чем мерить: вопросов с идентификатором было 5, и почти все искали само имя. Теперь
их 17. MRR на вызовах — 0,478: первым идёт объявление X (тир имён), а не вызов. Это верное
разделение, а не дыра: все вхождения имени — вопрос `grep`, и описание инструмента это говорит.
Итог на 93 вопросах: BM25 — 0,508, **BM25 + ru-en — 0,539**, @10 68 против 75.

Код поиска не менялся — мутаций нет.

**F-5.13c: замер качества поиска — и ранжирование, перенастроенное по нему.**

Стенд — `services/search_bench.rs`, игнорируемый тест: `cargo test --release search_bench --
--ignored --nocapture`. Вопросы — `src-tauri/bench/search-queries.json`: **81 вопрос** на трёх
репозиториях — этот, апстрим (`docflow`) и `corp-wowtax-patent-notification-api` (Java + русский
AsciiDoc, 30 вопросов, 8 из них и ответы — из бенчмарка, который сделала модель в самом этом
репозитории). 49 на английском, 32 на русском, 15 с ответом в документации. Ответ — файл и,
где есть, объявление; сверен чтением кода. Мерится `code_search::search` — то, что отдаёт
инструмент. Метрики — ранг первого верного файла (file@1/5/10, MRR@10) и объявления (decl@5).

**Итог, модель ru-en, 81 вопрос:**

| | MRR | file@1 | @5 | @10 | decl@5 | русские | документация |
|---|---|---|---|---|---|---|---|
| было (F-5.13a) | 0,450 | 27 | 50 | 56 | 39 | 0,224 | 0,630 |
| **стало** | **0,547** | **34** | **59** | **64** | **46** | **0,302** | **0,656** |

**Три конфигурации моделей на новом ранжировании:**

| | MRR | @10 | английские | русские |
|---|---|---|---|---|
| только BM25 | 0,515 | 57 | **0,711** | 0,215 |
| **BM25 + ru-en** | **0,547** | **64** | 0,707 | **0,302** |
| BM25 + `potion-code-16M-v2` | 0,487 | 55 | 0,680 | 0,190 |

- **Что было не так.** RRF с опубликованным K = 60 вознаграждал согласие: длинная русская секция
  `06-port-plan.md`, средняя и по словам, и по смыслу, обходила функцию, первую по смыслу. Со
  смыслом MRR был **ниже**, чем без него (0,437 против 0,494 на первых 67 вопросах).
- **Что изменено** (сетка ~100 вариантов на трёх моделях, направление у всех одно):
  K = 3 — первое место по одной мере важнее шестого по обеим; вес слов 1,5 против смысла; проза
  (Markdown, AsciiDoc, текст) ×0,75 — при ×0,5 код выигрывал ещё, но вопросы по документации
  падали с 0,545 до 0,40; путь, содержащий слово запроса, ×1,5 — это **путевой тир апстрима,
  вернувшийся бустом**, и он дал больше всех остальных сигналов вместе.
- **Контрольная выборка.** 14 вопросов по WOWTAX добавлены *после* подбора: на этом репозитории
  MRR 0,486 → 0,597, BM25 — 0,458. Подгонкой под набор это не объясняется.
- **Честно про модель.** На английских вопросах BM25 с бустом пути не хуже (0,711 против 0,707).
  Модель окупается русскими вопросами (+40 %), полнотой top-10 (64 против 57) и объявлениями
  (decl@5 46 против 41). Для агента, которому пишут по-русски про английский код, этого достаточно.
- **`potion-code-16M-v2` — отказ.** Слабее BM25 в одиночку по MRR, русский не понимает (0,190).
  Модель для кода нам не нужна: код лучше находят слова и имена, а смысл нужен там, где
  вопрос на другом языке, чем код.
- **Порог сходства — отказ.** Косинус верного ответа — 0,31…0,82, косинус *неверного* первого
  места — 0,33…0,70; неверный в среднем выше. Порог 0,5 срезает 16 из 42 ответов и 10 из 38
  промахов. У статической модели косинус не мера уверенности.
- **Штраф тестам (как у Semble) и предел чанков на файл — не взяты.** Первый не сдвинул ничего
  (±0,005), второй поднял @10 на 2, но уронил decl@5 на 5: нужное объявление часто второй чанк
  того же файла.
- **Стем-тир не возвращён** и отдельно не мерился: FTS ищет по префиксу (`prefix = '4 5 6'`) и
  весит имя чанка ×4 — `notifications` находит `NotificationService` без него.
- **Словарь по частоте — отложен.** Модель даёт +0,03 MRR и русский; словарь по частоте — с
  потерями, требует корпуса и нового замера, а резидентные ~320 МБ и так отдаются через
  10 минут простоя. Триггер: жалоба на память или модель, остающаяся загруженной постоянно.
- **Время:** прогретый запрос ≤ 3 мс на апстриме (7 797 чанков); первые запросы после открытия
  индекса — до 50 мс, пока SQLite дочитывает страницы.

**Две находки про сам набор.** Первый вариант набора спрашивал только о
коде, и штраф прозе ×0,5 выигрывал даром. 15 вопросов с ответом в документации добавлены,
чтобы такое ранжирование ловилось. И стенд выбрасывает из выдачи собственные файлы: вопросы
лежат в индексируемом репозитории дословно.

**Мутации: пятнадцать, все пойманы** (четыре — после правки тестов). K = 60; вес слов 1,0 в
константе и в месте вызова; вес смысла 1,5; вес не участвует в сумме; счёт слияния выброшен;
штраф прозе 1,0 и 0,1; проза без AsciiDoc/текста и без Markdown; путь `all` вместо `any`; буст
пути 1,0; множитель не применён; сортировка наоборот и без сортировки. Выжили сначала: тест
прозы проходил за счёт буста пути (`gear.rs` для «widget gear») — файл переименован; счёт
слияния, заменённый единицей, — добавлен тест «проза, совпадающая намного лучше, всё равно
первая»; вес слов в месте вызова — тест перенесён с `fuse_rrf` на весь поиск.

**F-5.13b: инструмент `semanticSearch` — и провод от открытой папки до вызова в turn'е.**

`services/ai_tools/tools/semantic_search.rs`: аргументы `query`, `fts`, `topK`, `preview`;
результат `searchResults { matches, meta }` — совпадения из F-5.13a, текст которых обрезан до
240 символов (с `preview` — до 1 200; медианный чанк ~470 байт). Путь, строки и имя решают, какой
результат открыть; текст — чтобы узнать не тот поворот и чтобы человеку в транскрипте было что
прочесть. Описание для модели — английское, без русских примеров апстрима
(«уведомления→Notification»): как писать запрос, что класть в `fts`, когда вместо него `grep`.

- **Порт, а не тип сервиса.** Инструменты живут ниже сервиса, владеющего индексом, поэтому в
  `ToolDeps` лежит `CodeSearchFn` — замыкание той же формы, что sink'и. Его собирает
  `WorkspaceIndex::searcher()` и привязывает к индексатору папки: turn ищет в той папке, где
  начался. Команда берёт `WorkspaceIndex` из состояния приложения (`searcher_of`) — сигнатуры
  `chat_start`/`chat_resume` не менялись.
- **Нет индекса — модели сказано, что делать.** `ToolError::SearchUnavailable`: «search is
  unavailable: … — use grep for exact text, or listFiles to look around».
- **Во всех режимах** (`base_tools`): поиск ничего не меняет. Вес в бюджете — 2 (индексные
  выборки + перебор векторов, первый поиск после выгрузки грузит модель).
- **`fts` принимает и одну строку** (`opt_string_list`): `"fts": "read_source"` — список из одного
  слова, а не ошибка типа, по той же логике, что `flexible_args` для чисел. `fts` без слов,
  годных для поиска, — ранжирование по словам берёт `query` (проверено в F-5.13a).
  `fts5_query_from_terms` апстрима не понадобился — это `fts5_query(&terms.join(" "))`.
- **Окно:** `describeTool` показывает «Search · запрос · N matches», в раскрытии — `путь:строки
  имя` и подсказку, которую получила модель.

**Контракт CA-13.3 на этот раз — честный счёт.** Мест, которые пришлось тронуть: `ToolName`
(вариант, `ALL`, `wire_name`, `loop_weight`), `ToolCall` (вариант, `name`), `ToolResult`, ветка
`parse_tool_call`, модуль, ветка диспетчера, строка `DEFINITIONS`, `base_tools` режима, поле
`ToolDeps`. Всё, кроме `ToolDeps`, подсказал компилятор или тест (`all_is_complete`,
`the_smallest_described_call_parses`, пример вызова для сверки схемы) — забыть нельзя, но мест
больше, чем «два enum'а, модуль и ветка».

**Мутации: пятнадцать, все пойманы.** Две — только после новых тестов: «turn не передаёт поиск
инструменту» (тест turn'а с поиском в `Harness`) и «команда не берёт поиск» (`searcher_of` на
mock-приложении Tauri).

**F-5.13a: ядро поиска — `services/code_search.rs`, и что из 1 252 строк `search.rs` апстрима не
понадобилось.**

`search(indexer, query, top_k)` → `CodeSearchResult { matches, meta }` (типы — `domain/code_search.rs`).
Порядок: объявления, названные в запросе, — первыми; затем BM25 и поиск по смыслу, слитые RRF
(K = 60, по рангам, а не по очкам — косинус и BM25 несравнимы). Каждое совпадение — путь, строки
(1-based, как берёт `readFile`), имя (`RepoIndexer.sync`), текст и источник.

- **Имена — только похожие на имя.** Апстрим искал символом каждое слово запроса: «how does sync
  work» поставил бы все функции `sync` выше ответа. Здесь — только идентификаторы
  (`looks_like_identifier`: camelCase, snake_case) и запрос из одного слова. Поиск — индексом
  `idx_symbols_name (name COLLATE NOCASE)` и одним JOIN'ом символ → чанк, в котором его первый
  байт; `load_all_symbols` на каждый запрос не нужен.
- **Строки, а не байты.** `resolve_text` стал `resolve_chunk` и считает строки; чанк, отрезанный
  после `}` предыдущего объявления, начинается со своей первой непустой строки. Вызовов вне тестов
  у `resolve_text` не было.
- **Изменившийся с индексации файл пропускается** (хэш), кандидатов берётся вдвое больше, чтобы
  место заполнил следующий.
- **Смысл недоступен — сказано прямо.** Ошибка модели на запросе или `embedding_error` синка (модель
  не грузилась) заменяют подсказку «дождитесь индекса» на «поиск по смыслу недоступен (причина)»:
  ждать тут нечего.

**Не перенесено:** стем-тир (`notifications` → `NotificationService` — FTS уже ищет по префиксу и
весит имя чанка ×4), путевой тир (имя файла — вопрос `listFiles`), буст «связанных файлов» (граф
include'ов AsciiDoc и импортов Java), кулдаун удалённого провайдера, проверки устаревшего стора
(стор, открывшийся, — уже совместимый, F-5.3). Стем- и путевой — кандидаты на возврат по замеру
F-5.13c.

**Замечено для F-5.13c:** у поиска по смыслу нет порога сходства — он всегда возвращает
`top_k` ближайших, даже далёких. Через RRF это добирает список до `top_k` мусором, когда по
словам нашлось мало. Порог (или его отсутствие) решается замером.

**Мутации: двадцать, все пойманы** (три — после новых тестов). Без тира имён; все слова как
имена; без запроса-одного-слова; слова раньше смысла (ярлык); RRF без суммы; ничьи наоборот; без
дедупликации; устаревший текст не пропускается; без ограничения `top_k`; без ошибки синка в
подсказке; ошибка модели молча; без `semantic` в `tiersUsed`; без запаса кандидатов; без BM25; без
смысла; подсказка не заменяется; имя с учётом регистра; символ → любой чанк файла; строки с
ведущего перевода строки; конечная строка — пустая после `\n`. Попутно убрана дедупликация в
`chunks_declaring` — её уже делает поиск.

**F-5.12: индекс открытой папки — владелец, провод в окно, и чего из 1 504 строк апстрима не понадобилось.**

`services/workspace_index.rs` апстрима (1 504 строки) — это не владелец индекса кода, а граф
AsciiDoc-документов в памяти (якоря, include, ссылки, диагностика) для редактора документации;
сюда он не переносится. Владельца индекса кода у апстрима не было отдельным модулем — он жил в
командах эмбеддингов. Здесь это `WorkspaceIndex` (~100 строк кода):

- **один на приложение**, создаётся в `setup` (`lib.rs`): там известен каталог ресурсов
  установленного приложения, откуда `bundled_model_dir` берёт модель. **Одна модель на все
  папки** — переход в другой проект не грузит вторую копию;
- **`open(root, sink)`** открывает стор (`app_dir/index/<repository_id>`, вне папки — иначе запись
  стора будила бы наблюдатель), запускает `FileWatcher` и первый синк на своём потоке.
  Повторное открытие той же папки ничего не меняет. Открытие другой — сначала останавливает
  наблюдатель прежней: если новая не откроется, прежняя не останется «открытой»;
- **`workspace_open` стал `async`** и ждёт только открытия стора (`spawn_blocking` — оно читает
  прежние векторы), не синка. Индекс, который не открылся, **не мешает открыть папку**: причина
  приходит событием, агент работает без поиска. Папка, за которой нельзя наблюдать, индексируется
  один раз и остаётся доступной для поиска — с событием о том, почему не следит за правками;
- **`IndexEvent::Failed { error }`** — новый вариант. Без него синк, упавший на проходе по словам,
  оставлял бы окно в «syncing» навсегда: `SyncStarted` пришёл, а завершения нет;
- **`commands/workspace_events.rs`** — канал `workspace-index:event`, событие плоское и несёт
  `root`: синк прежней папки переживает переключение, и его `syncFinished` иначе показался бы для
  новой.

**Не нужно оказалось:** `busy_timeout` для стора. Два индексатора на одном сторе возможны на
мгновение (папка открыта заново, пока доживает её прежний синк; два клона одного remote), и я
добавил ожидание вместо «database is locked» — мутация «убрать его» выжила: `rusqlite` открывает
соединение с таймаутом 5 с сам. Строка убрана, тест
`a_write_waits_for_another_connection_instead_of_failing` оставлен — он закрепляет это поведение
`rusqlite` (мутация «таймаут 0» его роняет).

**Мутации: десять, все пойманы.** Нет повторного использования открытой папки; нет наблюдателя;
нет первого синка; прежняя папка не закрывается при неудачном открытии; нет события `Failed` при
открытии; ошибка наблюдателя проглочена; стор внутри папки; неудача наблюдателя роняет весь индекс;
событие без `root`; синк без `Failed`.

**Не проверено вживую:** `bun run tauri dev` не запускался — нет фронтенда, который слушает
канал; это F-5.13. Путь к модели в собранном приложении (`resource_dir/models/...`) проверяется
там же.

**F-5.11: наблюдение за файлами — сигнал «что-то изменилось», без путей, и найден B-12.**

`infra/file_watcher.rs` — `FileWatcher::start(root, on_change)` на `notify` 8. Вызов идёт, когда
дерево простояло тихо `QUIET` (300 мс), но не позже `MAX_WAIT` (3 с) после первого изменения —
иначе файл, который пишут без пауз (лог под git), держал бы индекс вечно. `on_change` крутится на
собственном потоке наблюдателя по одному вызову за раз; изменения, пришедшие во время вызова, ждут
в канале и дают ещё один вызов после него. Связка с индексом —
`FileWatcher::start(root, move || indexer.sync(&sink))`, её делает F-5.12; сквозной тест
`a_saved_file_becomes_findable_through_the_watcher` уже проверяет её здесь.

**Почему без путей.** Апстрим (два наблюдателя, 493 строки) разбирал события по файлам:
`Create`/`Modify` → обновить файл, `Remove` → удалить, с замком на путь. Здесь синк целого дерева
сам решает, что изменилось (размер + mtime; без изменений — 3 мс на этом репозитории), так что
путь из события не нужен. Отсюда же бесплатно: потерянное или переполненное событие безвредно —
ОС говорит «пересканируй» (ошибка или событие без путей), а пересканирование — единственное, что
наблюдатель вообще умеет.
`ponytail:` на каждое изменение — обход всего дерева; перейти на синк по путям, если обход
большого репозитория станет заметен.

**Что не считается изменением.** Чтение (`Access`): синк читает каждый индексируемый файл, и на
Linux (inotify) это будило бы наблюдатель снова, по кругу. Всё под `.git`: git переписывает
`.git/index` даже на `git status`, а редактор с git-интеграцией зовёт его постоянно. Такие события
не запускают синк **и не отодвигают** уже назначенный. Файлы из `.gitignore` (`target/`) —
считаются: сборка даст синк раз в 3 с, и он ничего не найдёт; фильтр по `.gitignore` не стоит
своего кода, пока это дёшево.

**B-12, найден при разборе.** Апстрим «дебаунсил» по переднему фронту: первое событие по пути
срабатывало сразу, следующие в окне отбрасывались. Последнее сохранение серии не индексировалось,
пока файл не изменят ещё раз, — а тест апстрима «одна серия — один вызов» закреплял именно это.
Здесь — по заднему фронту, тест `a_burst_of_saves_gives_one_call_after_the_last` проверяет, что
вызов идёт не раньше `QUIET` после **последнего** события.

**Мутации: одиннадцать, десять пойманы, одна эквивалентна.** Тишина не продлевается; нет
предельного ожидания; чтение считается; `.git` считается; посторонние события отодвигают вызов;
ошибка не считается; событие без путей не считается; закрытый наблюдатель зовёт `on_change`;
корень не канонизирован (тогда корень внутри любой папки `.git` на macOS глух — `/var` против
`/private/var`); `on_change` не зовётся. Эквивалентная — «таймаут не выходит из ожидания»: условие
цикла само видит, что срок прошёл; лишний `break` убран, выход решает одно условие.

**F-5.10: синк одного репозитория — и чего из 2 643 строк апстрима не понадобилось.**

`services/index_sync.rs` — `RepoIndexer`: стор, индекс векторов и модель одного репозитория.
`sync(sink)` делает проход по словам (`repo_index::sync`), затем эмбеддинг
(`EmbeddingIndex::sync`), и сообщает через `IndexEventSink` (`domain/workspace_index.rs`, один
enum: `SyncStarted`, `KeywordsReady`, `EmbeddingProgress`, `SyncFinished`; сериализуется
тегированным camelCase — форма для фронтенда закреплена тестом).

**Не перенесено — и почему.** `embedding_sync` + `embedding_state` апстрима (2 643 строки) делили
синк на тиры (каталог документации и открытые вкладки — сразу, остальное — фоновым backlog по 25
файлов), держали в согласии карты в памяти со стором, остывали после сбоя удалённого провайдера,
чинили размерность модели и усыновляли векторы из старой раскладки. Всё это обслуживало три вещи,
которых здесь нет: минутный эмбеддинг (удалённая модель, потом `bge-m3`), карты в памяти (F-5.6
сделал стор единственной копией) и удалённый сервис. Замер: апстрим целиком эмбеддится за 0,6 с.

**Что сохранено из того, ради чего были тиры:**
- **Первый синк ничего не держит.** Поиск по словам работает с `KeywordsReady`, по смыслу — по уже
  готовым батчам: `EmbeddingIndex` получил внутренний `RwLock` и берёт запись только на вставку
  батча, не на время работы модели.
- **Один синк за раз.** Запрос во время синка не запускает второй, а просит текущий пройти ещё
  круг: правка во время синка не теряется, пачка сохранений стоит два прохода, а не по одному на
  сохранение. «Решить, что круга не будет» и «отпустить синк» — под одним замком: иначе запрос
  между ними сложился бы в уходящий синк и потерялся. Упавший синк (ошибка или паника) отпускает
  его через `Drop` — иначе все следующие запросы складывались бы в синк, которого нет.
- **Сохранение не грузит модель** (пункт 2).
- **Смысл падает — слова остаются.** Отсутствующая модель (клон без `git lfs pull`) —
  `embedding_error` в `SyncFinished` и в статусе; индекс по словам уже записан.

**Тест, который висел вместо того, чтобы падать.** Мутация «один замок на весь синк и поиск»
давала взаимоблокировку: поиск ждал синк, синк ждал сигнала, который тест посылал после поиска.
Прогон мутаций висел 10 минут, а файл оставался мутированным (скрипт восстанавливает после
завершения `cargo test`) — процесс убит, файл восстановлен. Теперь поиск в тесте идёт под
таймаутом 5 с и при таймауте отпускает синк: тест падает с «search waited for the whole sync».
Попутно: на macOS нет `timeout`, прогоны мутаций — через `perl -e 'alarm …'`.

**Мутации: восемь, все пойманы** (одна — со второго захода, выше). Нет защиты от параллельного
синка; нет второго круга; упавший синк не отпускает; пустой индекс эмбеддит запрос; ошибка
эмбеддинга роняет синк; пропуски не попадают в статус; `syncing` не сбрасывается.

**F-5.9: векторы — строками стора, поиск — перебором, и найден B-11.**

**Вместо `usearch` — строка SQLite.** Апстрим держал векторы в HNSW-графе `usearch` (C++ через
`cxx`) в отдельном файле, а отметки о них — в SQLite. Разбор его синка дал **B-11**: отметки
пишутся на каждом батче, файл графа сохраняется только после цикла, и обрыв посередине навсегда
выбрасывает чанки из поиска по смыслу. Здесь вектор — колонка `vector BLOB` той же строки
`embeddings`; `upsert_embeddings` пишет батч одной транзакцией. Разойтись нечему, C++ из сборки
ушёл, `vectors_path` удалён.

**`QuantizedVector` (домен): int8 + масштаб на вектор — 260 байт вместо 1 024.** Масштаб на вектор,
а не общий: каждая компонента восстанавливается с точностью до полушага **своего** диапазона.
Замер на двух репозиториях, 12 запросов: **топ-10 совпадает с точными `f32`-векторами на 9,7–10
из 10.**

**Поиск — полный перебор в памяти.** Замер (release, Apple silicon, один поток): **11 мс на
запрос при 100 000 чанков** — вчетверо больше апстрима (7 797) и в 50 раз больше этого репозитория
(2 013). Точная полнота вместо приближённой, нет графа, который надо перестраивать после удаления.
`ponytail:` линейно — ~110 мс на миллион; пересмотреть, если такие репозитории появятся.

**Схема стора получила версию.** `CREATE TABLE IF NOT EXISTS` не меняет существующую таблицу, и
стор без колонки `vector` упал бы на первой записи. `SCHEMA_VERSION` в `meta`; при несовпадении
(или отсутствии штампа) все таблицы пересоздаются — стор производный. По ходу найдено: `wipe`
чистил `meta` целиком и стирал штамп — каждое открытие после сброса пересоздавало таблицы. Тест
`opening_twice_keeps_what_was_written` это поймал; `wipe` теперь оставляет штамп формы.

**`services/embedding_index.rs`:** индекс в памяти, зеркало стора. `sync` эмбеддит чанки, чей хэш
изменился, **группируя по файлу** (файл читается и хэшируется один раз, у апстрима — на каждый
чанк через `resolve_text`), пропускает файлы, изменившиеся после лексического синка (`stale_files`
— их перережет следующий лексический синк), и сохраняет каждый батч до следующего. `search`
возвращает косинус, больше — лучше, как у BM25 после F-5.3c.

**Эмбеддится всё.** Вторая ось апстрима — `Language::is_lexical_only`, «достоин ли вектора» —
существовала потому, что каждый вектор стоил вызова платной удалённой модели. Встроенная модель
эмбеддит этот репозиторий целиком за **0,49 с** (вместе с загрузкой модели), апстрим — за
**0,6 с**, а код — ровно то, что ищет агент. Решать оси нечего.

**Качество — честно:** английские запросы точны (`keyring fallback` → `master_key.rs`, в апстриме
`rate limit sliding window` → `llm_rate_limit.rs`). Русские вопросы к коду тянутся к русской
прозе: «как сжимается история при переполнении контекста» → `docs/`, а не
`context_compaction.rs`. Это ожидаемо для статической модели и задача F-5.13 — слияние с BM25 и
переранжирование.

**Мутации: пятнадцать, все пойманы, две — со второго захода.** «Хэш не сравнивается» выжила:
правка в тесте меняла длину файла, чанк получал новый id и эмбеддился как новый — сравнение хэшей
не проверялось вовсе. Теперь правка той же длины. «Общий масштаб вместо масштаба на вектор»
выжила: порог ошибки скалярного произведения 0,01 слишком груб; добавлен тест на полушаг
собственного диапазона. Остальные: векторы удалённых файлов не забываются; нет проверки
несвежего файла; батчи сохраняются только в конце (B-11 — ловит тест прерванного синка); нет
проверки числа векторов; сортировка по возрастанию; нет проверки ширины запроса; неверный
индекс частичной сортировки; нет масштаба; схема не проверяется; вектор не обновляется при
повторном эмбеддинге; ширина вектора не проверяется при загрузке.

**F-5.8c: таблица остаётся int8 в памяти — `model2vec-rs` заменён своим пулингом.**

`model2vec-rs` разворачивал int8-таблицу в `f32` при загрузке: четыре байта на вес там, где
одного хватает. `Int8Model` держит файл `model.safetensors` как прочитан (`Vec<u8>`, ноль
копий), разбирает заголовок safetensors вручную (8 байт длины + JSON — крейт не нужен) и на
пулинге переводит байт в `f32` — **точно**, поэтому арифметика та же. Зависимость `model2vec-rs`
удалена, у `tokenizers` убрана фича `progressbar` (она для обучения), комментарий про
`esaxx_fast`/`LNK2038` сохранён и уточнён.

| | ru + en, `f32` | **ru + en, int8** |
|---|---|---|
| резидентно | ~520 МБ | **~320 МБ** |
| загрузка | 0,42 с | 0,35 с |
| 2 000 коротких текстов | 19,7 мс | 13,6 мс |

**Проверено против `model2vec-rs` на двух репозиториях:** 10 953 фрагмента без `[UNK]` — косинус
**ровно 1**. 17 фрагментов с `[UNK]` отличаются — и это не регрессия, а **найденный дефект
`model2vec-rs` (B-10)**: он ищет id `[UNK]` по полю, которого у Unigram-токенизатора нет, и
усредняет `[UNK]` в вектор. Здесь `[UNK]` выбрасывается, как библиотека и задумывала; фраза F-5.8b
«чужие символы эмбеддятся в ничто» стала правдой только теперь.

**Упрощение по мутации.** Мутация «нет деления на количество» выжила — и оказалась
**эквивалентной**: вектор затем нормируется, а нормировке масштаб безразличен. Деление и флаг
`normalize` (у поставляемой модели всегда `true`; иной случай поймает тест паритета) удалены.

**Мутации: одиннадцать, все пойманы, четыре — со второго захода.** Проверки битой таблицы (строк
не столько, сколько токенов; не int8; не та ширина) не достигались: тест на мусор падал раньше, на
токенизаторе — теперь тест берёт настоящий токенизатор и три испорченные таблицы. Предел 512
токенов и предобрезка текста видны только на длинных текстах — теперь есть тест «хвост после
512 токенов не влияет» и тест «5-мегабайтный текст эмбеддится меньше секунды». Остальные:
`[UNK]` не выбрасывается; нет нормировки; байт как беззнаковый; строка берётся не с того смещения.

**Предполагаемое, не сделано — модель для кода вместо многоязычной.** В списке моделей
`model2vec-rs` есть `potion-code-16M-v2` (MIT): дистиллирована из CodeRankEmbed, словарь ~63,5 тыс.
токенов (43 тыс. добыты из кода), 256 измерений — **та же ширина, что у нас**, так что векторный
стор (F-5.9) замену переживёт без правок, достаточно нового `LOCAL_MODEL_ID`. Памяти — оценочно
десятки мегабайт против наших ~320. Цифры из её карточки (CoIR, NDCG@10), и они отрезвляют:

| | среднее |
|---|---|
| CodeRankEmbed (учитель, трансформер) | 59,14 |
| **BM25** | **42,31** |
| potion-code-16M-v2 | 39,08 |
| potion-code-16M-v2 + BM25 | 43,36 |

Статическая модель **для кода** в одиночку слабее BM25, а в паре даёт +1 пункт. Против неё:
только английский — русские документация и комментарии остаются без векторов (BM25 их видит);
Rust и TypeScript нет в списке обученных языков (есть JS). **Решается замером в F-5.13**: этот
репозиторий + апстрим, запросы на русском и английском, три конфигурации — BM25; BM25 +
текущая ru/en; BM25 + `potion-code-16M-v2`.

**Semble — готовый ориентир для F-5.13.** Библиотека поиска по коду для агентов от тех же авторов
устроена как наш слой: чанки по tree-sitter, BM25 + статическая модель, слияние RRF. Основной
выигрыш у них — не в модели, а в **переранжировании по признакам кода**: запрос, похожий на
идентификатор, получает больший вес лексики; буст определений; стемминг идентификаторов;
связность по файлу; штраф тестам и legacy-коду. Их заявленные цифры: NDCG@10 0,854 на своём
бенчмарке, индексация среднего репозитория ~500 мс, запрос ~1 мс. Взять список сигналов в F-5.13
и мерить каждый.

**Предполагаемое, не сделано — словарь по частоте.** Два репозитория (10 МБ кода и русской
документации) используют **44 051** токен из 271 538. Словарь из ~100 тыс. самых частых токенов
по большому корпусу русского, английского и кода сжал бы токенизатор и таблицу пропорционально —
оценка ~120 МБ резидентно. **С потерями**, в отличие от обрезки по письменности: редкое слово
лишается кусочка, на который резалось, и режется иначе. Нужны корпус и замер качества поиска —
решается в F-5.13, когда поиск станет измеримым.

**F-5.8b: словарь модели — только русский и английский.**

Решение владельца: остальные языки выбросить как явное ограничение, с возможностью вернуться к
полной модели. `scripts/prune-embedding-vocab.py` (только стандартная библиотека Python)
оставляет **271 538 из 500 353** токенов — те, чьи буквы целиком ASCII или кириллица (знаки
препинания и символы любые) — и соответствующие строки таблицы. `[PAD]`/`[UNK]` сохраняют id 0/1,
потому что порядок сохраняется; других ссылок на id в `tokenizer.json` нет.

**Без потерь для русского, английского и кода — доказательство, а не надежда.** Unigram-токенизатор
выбирает лучший путь через кусочки словаря, совпадающие с подстроками текста. Выброшенный кусочек
содержит букву другой письменности и **не может совпасть** с текстом на ASCII и кириллице — значит,
у такого текста те же кандидаты, тот же путь, те же строки таблицы. Проверено на двух
репозиториях (этот + апстрим с русской документацией, 10 957 фрагментов): разбиение совпало
везде, кроме **шести** фрагментов — и в каждом был `é`, `à`, `µ`, `Σ` или `世界`. Эти символы теперь
эмбеддятся в ничто. Тест паритета сравнивает обрезанную модель с векторами Python **полной**
модели и проходит.

| | полная | **русский + английский** |
|---|---|---|
| на диске / в установщике | 146 МБ | 80 МБ |
| резидентно | ~1,07 ГБ | **~520 МБ** |
| загрузка | 0,7 с | 0,42 с |

`LOCAL_MODEL_ID` → `local-potion-multilingual-128m-int8-ru-en`: для текста с другими
письменностями векторы другие, значит это другая модель, и старые векторы не сравниваются с
новыми. Полная модель убрана из рабочего дерева (в истории — коммит `4882a63`); путь назад описан
в `scripts/embedding-model.md` и шапке `local_embeddings.rs`.

**Тест на ограничение как свойство поставляемой модели**, а не скрипта: пересборка, забывшая шаг
обрезки, вернула бы вдвое больше памяти незаметно. Первая версия теста искала «буквы не из ASCII и
кириллицы» через `char::is_alphanumeric` и упала на `🅱` — в Python это символ (скрипт его
оставил), в Rust — буква. Тест проверял не то, что обещал; переписан на то, от чего страхует:
размер словаря в диапазоне обрезанного и явное отсутствие `世界`, `é`, `Σ`, `ß` при наличии
`▁context`, `▁контекст`.

**F-5.8a: модель выгружается при простое — и возвращает память системе.**

Решение владельца после F-5.8. `LocalEmbeddings::new(dir, idle)` ничего не читает; модель
грузится на первом `embed` и выгружается фоновым потоком после `DEFAULT_IDLE_UNLOAD` (10 минут)
без использования. Следующий `embed` платит 0,7 с загрузки снова. Конструирование бесплатно — это
и есть пункт 2 списка «нельзя потерять» (сохранение файла не строит провайдера): теперь строить
можно, грузиться он не будет.

**Одного drop мало — замер.** Выгруженная модель оставила **375 МБ** резидентно: токенизатор —
миллионы мелких блоков, и аллокатор держит их для повторного использования.
`release_freed_memory()` после выгрузки просит аллокатор вернуть их системе:
`malloc_zone_pressure_relief` на macOS, `malloc_trim` на Linux/glibc — остаётся ~50 МБ. **Windows —
ограничение:** у process heap нет вызова, который гарантированно отдаёт страницы; там выгрузка
вернёт крупные блоки (таблица 512 МБ), но не токенизатор.

**Ещё один честный перемер.** Раскладка F-5.8 («токенизатор ~450 МБ») снималась по RSS, который
включает удержанное аллокатором. С принудительным возвратом: **токенизатор ~500 МБ**, таблица
512 МБ, **итого ~1,07 ГБ** живых данных. Словарь — **500 353** токена Unigram; объём токенизатора
— его префиксное дерево, узел на символ каждого токена.

**Жизненный цикл вынесен в `IdleCell<T>`** — generic только ради тестов: первая версия тестов
грузила настоящую модель в каждом, параллельно (до ~5 ГБ на прогон, набор с 4 до 9 с). Теперь
цикл проверяется на числах, модель в наборе грузится один раз; набор снова 4 с.

**Мутации: шесть, все пойманы, одна — со второго захода.** Флаг «сборщик работает» не
сбрасывается после выгрузки — тест проверял один цикл, а при такой ошибке **вторая** загрузка не
выгружается никогда. Теперь тест ждёт и второй выгрузки. Остальные: сборщик не запускается;
сборщик не сбрасывает значение; часы не обновляются при использовании; загрузка на каждом
вызове; ячейка удерживается сильной ссылкой и не умирает с владельцем.

**F-5.8: встроенная модель эмбеддингов — и её настоящая цена.**

**Что приехало:** веса `potion-multilingual-128M-int8` (146 МБ, скопированы из апстрима как есть,
вместе с `parity.json`) под **Git LFS** — в индексе два указателя по 3 строки; `.gitattributes`;
`bundle.resources` в `tauri.conf.json`; `scripts/build-embedding-model.py`;
`infra/local_embeddings.rs`; `domain/embeddings.rs`. Зависимости `model2vec-rs` (`local-only` — без
hf-hub и его HTTP-клиента) и `tokenizers` напрямую **без `esaxx_fast`** — с комментарием про
`LNK2038` под MSVC, перенесённым вместе со строкой, как требует этап 0; `cargo tree -e features`
подтверждает, что `esaxx-rs/cpp` не включён.

**`domain/embeddings.rs`: из 383 строк апстрима осталось ~60.** Остальное — удалённый провайдер
и его настройка: base URL, модель, размерность, закреплённый CA, заголовки, отключение TLS,
пресеты, слитый конфиг, ключ в keychain. Решение владельца — только встроенная модель, и типам,
которые выбирали между двумя, выбирать не из чего. Остались `Embedding`, `EmbeddingError`,
`LOCAL_MODEL_ID` (тот же, что у апстрима) и трейт `EmbeddingProvider` — **не ради второго
провайдера, а ради тестов**: синк на сотнях чанков не должен грузить полгигабайта весов в каждый
тест.

**Путь к весам передаётся явно.** Апстрим угадывал его от `current_exe()` по трём раскладкам, и
ни одна не Linux (`/usr/lib/<app>`). Здесь `bundled_model_dir(resource_dir)`: каталог ресурсов
даёт слой команд (Tauri его знает, `infra` Tauri не спрашивает), запасной — копия в крейте для
dev и тестов.

**Размерность проверяется при загрузке.** У `model2vec-rs` нет геттера размерности, а все
хранимые векторы шириной 256. Веса другой модели отвергаются на загрузке с `Invalid`, а не
всплывают несовпадением длины в векторном сторе.

**Ошибки — про файлы, и LFS-указатель назван отдельно.** `NotFound`, `LfsPointer` («run
`git lfs pull`»), `Unreadable`, `Invalid`. Результат загрузки кэшируется на процесс, поэтому
ошибка `Clone`: второй вызывающий получает ту же ошибку, а не вторую попытку на полгигабайта.

**Замер (release, macOS) — и он расходится с документацией апстрима в 2,5 раза:**

| | |
|---|---|
| Резидентно после загрузки | **~1,3 ГБ**: токенизатор ~450 МБ, таблица `f32` 512 МБ, аллокатор ~150 МБ |
| Загрузка | ~0,7 с |
| Эмбеддинг всего репозитория (1 911 чанков) | 150 мс |

Апстрим писал «~512 MB RSS» — это одна таблица. Модель грузится не более раза на процесс и
только когда что-то реально эмбеддится; выгрузки нет (`OnceLock`). **Вопрос владельцу**, см. ниже.

**Находки в апстриме — B-9:** ревизия Hugging Face «закреплена» только в шапке скрипта
(`HF_REVISION = "main"`, в загрузку не передаётся); память занижена; тест паритета **проходил,
ничего не проверив**, если весов нет или они LFS-указатели. Здесь: шапка исправлена на правду,
цифры замерены, тест падает с сообщением про `git lfs pull`.

**Мутации: семь, все пойманы, одна — со второго захода.** Отключённая нормализация выжила: тест
паритета сравнивал по косинусу, а косинусу длина безразлична. Векторный стор апстрима и так
считает косинус — но паритет с Python это совпадение векторов, а не направлений, и стор,
перешедший на скалярное произведение, ранжировал бы по норме. Тест теперь проверяет и длину.
Остальные: нет проверки LFS; нет `NotFound`; ресурсы игнорируются; ресурсы без проверки наличия
весов; порядок векторов перевёрнут; размерность 128.

**Решено владельцем 2026-09-18: вариант 2 (выгрузка при простое) + обрезка словаря до русского и
английского** — см. F-5.8a и F-5.8b выше. Варианты, как они стояли:
1. Оставить как есть — платит только тот, кто ищет по смыслу, и один раз за процесс.
2. Выгружать при простое: `Arc` вместо `OnceLock`, drop после N минут без эмбеддинга — ~30
   строк, повторная загрузка 0,7 с.
3. `dimensionality=128` при сборке весов — минус 256 МБ таблицы, токенизатор не трогает;
   нужен замер качества.
4. Другая модель (англоязычная `potion-base-8M` — ~30 МБ, но русские вопросы к английскому коду
   она не сопоставит; это главный сценарий).

**F-5.7: из чего состоит запрос — до того, как его увидит индекс.**

`domain/search_query.rs`, два прочтения одной строки для двух индексов:
- `fts5_query` — **все** слова на любом языке, для тира FTS. OR, не AND (BM25 и так ставит чанк
  со всеми терминами выше, AND обнулил бы вопрос с одним словом, которого нет в коде). Каждый
  термин в кавычках — синтаксис FTS5 во вводе (`NOT`, `*`, `:`, `"`) становится текстом.
  Слова от 4 символов — префиксы длиной не больше 6: `unicode61` не стеммит кириллицу, и
  `уведомлений` встречается с `уведомления` на общем префиксе.
- `extract_search_tokens` — только то, что похоже на код (идентификаторы → пути → слова), для
  тира символов и путей (F-5.13). Плюс `symbol_name_matches_token`, `path_segment_matches`,
  `english_stem` (только множественное `s`).
- `weak_search_hint` — «слабый ли результат» и **какой другой запрос** сделать; подсказки
  переведены на английский и переписаны под кодовую базу, а не под патентное ведомство.

**Не перенесено:** словарь корней RU→EN (`уведомлен` → `Notification`, пять записей словаря
одного проекта апстрима); `fts5_query_from_terms` — появится с аргументом `fts` инструмента в
F-5.13; каскад тиров, `fuse_rrf` и выбор тира при сбое — они работают с типом результата
инструмента и переезжают вместе с ним в F-5.13.

**Починено против апстрима: `snake_case` — это имя.** Апстрим узнавал только camelCase/PascalCase
(правильно для Java), и любое имя Rust или Python — `compact_if_needed`, `read_source` — считалось
«в запросе нет идентификаторов» с подсказкой модели его добавить.

**Найдено замером и починено: camelCase-запрос не находил snake_case-код.** Токенизатор хранит
`readSource` из TS как один токен, а `read_source` из Rust — как два соседних. Модель по умолчанию
называет вещи в camelCase, и `readSource`/`spansFromDeclarations` на этом репозитории находили
только прозу, цитирующую слово. **Первая версия исправления была хуже, чем ничего**: части
отдельными OR-терминами (`read*`, `source*`) размыли ранжирование — `ContextUsage` съехал с
определения типа на тесты, упоминающие context. **Итоговая форма — фраза**: `readSource` → слово
целиком **и** `"read source"`, ровно та последовательность токенов, которой хранится snake-форма.
Это не заплатка, а вторая половина одной нормализации; делать её на стороне индекса значило бы
писать свой токенизатор FTS5 на C-API. После: `readSource` → определение в `chunk_text.rs`,
`spansFromDeclarations` → определение в `chunk_index.rs`, `ContextUsage` → команда и тип.

**Тесты на стыке со стором:** список префиксов схемы (`prefix = '4 5 6'`) выводится из констант
запроса и сверяется (раньше их связь держал комментарий); любой построенный запрос — включая
`"unclosed`, `NOT`, `col:value` — выполняется в SQLite без ошибки; русское слово находит другую
форму; camelCase-запрос находит snake-имя и **только** его, а не текст, где `read` и `source`
стоят порознь.

**Мутации: четырнадцать, все пойманы.** AND вместо OR; термины без кавычек; префикс не
обрезается; префикс 7 (ломает и связь со схемой); нет фразы (ловят и доменный, и сторовый тест);
snake_case не имя; дедупликация с учётом регистра; нет предела терминов; нет стемминга
множественного; короткий токен ищется подстрокой в пути; «только текст» не слабый; нет стема
файла из пути; порядок токенов перевёрнут.

**F-5.6: синк лексического индекса — первый поиск без единого вектора.**

**`RepositoryIndex` апстрима не перенесён — стор единственная копия.** Апстрим держит
`DashMap` метаданных и символов всех файлов в памяти, пересобирает его при каждом открытии, а
стор рядом служит кэшем, *из которого* пересобирать. Два экземпляра одного факта и код, который
держит их в согласии. Здесь `services/repo_index.rs::sync(root, store, options)` читает, что
записал стор, сравнивает с деревом и пишет обратно изменившееся. То, чему служила карта в памяти
(поиск символа, разрешение Java-импортов), вызывающих в порте не имеет.

**Порядок решения по файлу:**
1. размер и mtime (до секунды — стор хранит секунды) совпали → не изменился, **даже не открыт**;
2. `read_source` отказал → в `SyncReport.skipped` с причиной **и удалён из индекса**, если был:
   файл, выросший за предел, не должен находиться по старому тексту;
3. хэш совпал (`touch`, checkout вернул те же байты) → двигается только строка файла, чанки и
   их будущие векторы на месте, **и файл не разбирается** — checkout, тронувший тысячу файлов
   и изменивший десять, платит за десять;
4. иначе → разбор, нарезка, запись.

Записанные файлы, которых обход не увидел, удаляются: удаление, переименование, новый
`.gitignore`. При устаревшем `DERIVED_VERSION` синк читает всё и делает дешёвую пересборку
`replace_derived_for_file` — векторы остаются (тест кладёт эмбеддинг и проверяет, что он пережил).
У апстрима дешёвая пересборка жила в `embedding_sync`; она лексическая, поэтому здесь.

**Новое в сторе: `replace_file` — строка файла, чанки, FTS и символы в одной транзакции.** Строка
файла — то, с чем сравнит следующий синк, и она может двигаться только вместе с работой, за
которую ручается. Раздельными записями падение между строкой и чанками оставило бы строку
«актуален» над чанками из старого текста, и следующий синк, увидев совпавший хэш, не тронул бы
файл никогда. Тест роняет вызов чанком без строки файла (нарушение FK) и проверяет, что записана
старая версия. Тела `upsert_files`/`replace_chunks_for_file`/`replace_symbols_for_file` вынесены
в функции от транзакции и переиспользуются.

**Замер на этом репозитории** (release): первый синк **232 мс** (150 файлов, 16 отказов «binary»,
1 813 чанков), повторный **3 мс** (150 unchanged, ни один файл не открыт); запросы
`compact_if_needed`, `"context window"`, `qualified_name:spans_from_declarations`, `keyring` —
60–175 **мкс**, все с правильным файлом первым. Стор с WAL — 9 МБ.

**Мутации: четырнадцать, все пойманы, четыре — со второго захода.** Три из них — одна
слепая зона: «не изменился» бывает двумя путями (mtime или хэш), и счётчик их не различает.
Нет быстрого пути; mtime сравнивается с наносекундами, которых стор не хранит; после `touch`
строка не двигается — всё это давало «unchanged» и зелёные тесты, при этом каждый синк читал
бы и хэшировал весь репозиторий. Различает файл **без прав на чтение**: быстрый путь его не
открывает, медленный спотыкается. Четвёртая — дешёвая пересборка без перезаписи FTS: старые
строки и так содержали искомое слово; тест теперь сначала подкладывает «устаревшее» содержимое и
проверяет, что оно исчезло. Остальные: отказ не в отчёте, отказанный файл остаётся в индексе,
`touch` переразбирает файл, удалённые не удаляются, отметка версии не ставится, пропуск быстрого
пути при досчёте, `replace_file` двумя транзакциями, `replace_file` без символов.
(Первый прогон мутаций был ложным: `cargo test a b` с двумя фильтрами до `--` — ошибка
использования, и grep её не показал. Фильтры — после `--`.)

**Отложено:** счётчик пропусков в UI — F-5.13, данные уже в `SyncReport.skipped`. Параллельный
разбор файлов — 232 мс на первый синк этого не требуют; пересмотреть по замеру на репозитории
в десятки тысяч файлов.

**F-5.5: текст → чанки, и файл, который индекс не берёт, говорит почему.**

**Стратегий как модулей нет — есть функция `spans_for(language, …)` в домене.** Апстрим держал
их в `infra/chunk_strategies/` за трейтом и `HashMap`, а сборщик — в сервисе, потому что тот
читал файл через индекс репозитория. Ни тому, ни другому не нужно ничего, кроме текста и
символов; обе вещи чистые и остались в `domain/chunk_index.rs` (`spans_for`, `build_chunks`).
Трейт `ChunkStrategy` удалён, `match` исчерпывающий — забыть язык нельзя. План говорил
`infra/chunk_strategies/` + `services/chunk_builder.rs`; отступление сознательное.

**Нарезка кода: `spans_from_declarations`.** Чанк на каждое **самое внутреннее** объявление, и
каждый забирает промежуток **перед** собой — док-комментарий, атрибут, аннотация, декоратор
едут с тем, что описывают. Первый чанк начинается с байта 0 (импорты и шапка класса идут с
первым членом), последний идёт до конца файла (закрывающие скобки — с последним). Контейнер
без членов — сам себе якорь. Имя чанка — цепочка объемлющих объявлений через точку:
`UserService.find`, `Turn.new` (точка — разделитель для FTS-токенизатора, оба слова находятся).
Новый `ChunkKind::Declaration` (+ `"declaration"` в сторе).

**B-1 в его родной функции.** Это та самая `spans_from_backward_gap_symbols`, которая падала на
вложенном якоре. Теперь якоря сортируются (контейнер раньше содержимого), и символ остаётся,
только если следующий начинается не раньше его конца. Одна проверка выбрасывает и контейнеры,
и первый из двух **пересекающихся** символов (парсер, восстанавливающийся после битого
синтаксиса, такие даёт). Вторая проверка — «не перекрывает ли последний сохранённый» — была
написана и оказалась **мёртвым кодом**: мутация её убрала и ничего не упало, а разбор показал,
что условие недостижимо. Удалена.

**`build_chunks` покрывает файл целиком**: каждый байт ровно в одном чанке, по порядку, что бы
ни пришло в символах. Спан не на границе символа (символ, сохранённый для другого содержимого)
выбрасывается, а не режется в панику.

**`services/chunk_text.rs`:**
- `read_source(path, max_file_bytes)` — **предел 1 МиБ, тот же, что у `grep`**: чего агент не
  ищет по шаблону, того не должен находить и по смыслу. Проверяется **до** чтения (по `stat`) и
  ещё раз после (файл мог вырасти между вызовами). Отказы — `SourceSkip { TooLarge{size,limit},
  Binary, NotUtf8, Io }`, каждый с текстом для счётчика. Не-UTF-8 отказ, а не lossy-декод:
  смещения чанков — байтовые, и после первой буквы с диакритикой они указывали бы в другое
  место. Поле `max_file_bytes` добавлено в `ChunkBuildOptions` вместе с исполнителем.
- `resolve_text(root, metadata)` — текст чанка из файла, только если файл всё ещё хэшируется в
  `file_hash`. Кэш апстрима (64 МиБ `moka`) не перенесён: текст чанков в цикле пока никто не
  читает. Вернуть, когда профиль поиска покажет, что дорого чтение с диска.

**Замер на этом репозитории** (release): 164 файла под git → 148 текстовых, 16 отказов «binary»
(все — иконки); 1 761 чанк; склейка чанков совпала с исходником **в каждом файле**. Размер:
медиана 467 байт, p90 1 607, максимум 16 368 (предел 16 КиБ). Мелких (<200 байт) 312, это 18% —
`const`, короткие методы. Склейку соседних мелких чанков **не делаю**: model2vec эмбеддит
дёшево, а BM25 от точного имени только выигрывает. Триггер пересмотра — F-5.13, если поиск по
смыслу на живом репозитории проседает на коротких чанках.

**Мутации: пятнадцать, все пойманы, три — со второго захода.** Контейнеры оставлены якорями;
перекрывающиеся оставлены (мёртвый код, удалён; новая мутация «оба пересекающихся оставлены»
поймана); код режется как проза; последний чанк не доходит до конца файла; первый не от нуля;
имя без объемлющих; нет разреза больших; JSON режется по символам; срез по байтам без проверки
границы (первый тест не строил плохой спан — у файла с одним символом спан всегда `0..len`);
пустые символы оставлены; предела нет вообще (проверка «после» маскировала отсутствие «до» —
теперь её отличает файл без прав на чтение: `stat` работает, `read` нет); предел на единицу
строже; бинарники пропускаются; lossy-декод; нет проверки свежести.

**F-5.4: языковые индексеры — один обход на все языки.**

**Один generic-обход дерева tree-sitter и строка таблицы на язык**, `infra/language_indexers.rs`.
Строка — грамматика и два списка видов узлов: **контейнеры** (класс, `impl`, модуль — символ, и
обход идёт внутрь за членами) и **листья** (функция, метод, тип — символ, и внутрь **не** идёт).
Всё остальное проходится насквозь без символа — так находится функция под `export` и за
декоратором Python. У апстрима на одну только Java 273 строки рукописного обхода; здесь язык —
это крейт и ~10 строк таблицы.

**Инвариант B-1 держит обход, а не комментарий.** Раз в лист не заходим, символ либо стоит рядом
с другим, либо вложен в контейнер — и никогда не внутри функции. Замыкание, анонимный класс,
локальная функция покрыты диапазоном своего метода. У апстрима это держалось комментарием в
одной функции Java-индексера; здесь — обходом, общим для всех языков.
**Но вложение в контейнер остаётся** (методы внутри `impl`/класса). Прозаический
`spans_from_forward_gap_symbols` с этим уже справляется; кодовая стратегия F-5.5 обязана тоже.

**Регистр — `match`, а не `HashMap` апстрима.** Язык, добавленный в enum без индексера, —
ошибка компиляции, а не пустой поиск в рантайме.

**Решения по составу символов.**
- `const x = …` в JS/TS — символ, **только если справа функция**: так объявлена половина
  React-компонентов, а `const LIMIT = 5` никто по имени не ищет. В Rust `const`/`static` —
  символы: там это осознанные именованные сущности модуля.
- Java — **без полей**. Апстрим давал символ на каждый декларатор, то есть в Spring-бине символ
  на каждую внедрённую зависимость — граница чанка на каждой строке шапки класса.
- Go — `type_spec`, а не `type_declaration`: один блок `type ( … )` объявляет несколько типов.
- Rust `impl` называется по типу (`impl Display for Turn` находится по `Turn`).
- JSON/YAML — без символов, как у апстрима (сканирование `key:` врёт на значениях с
  двоеточием); файлы всё равно индексируются и режутся на чанки.

**Markdown — через `pulldown-cmark`, не tree-sitter.** Markdown-грамматика tree-sitter — это две
грамматики (блоки и инлайны), которые надо гонять по очереди, а текст заголовка — вопрос
инлайнов. `pulldown-cmark` отвечает на оба за проход и знает, что `#` в блоке кода — не
заголовок (в документации этого репозитория таких строк полно). Setext-заголовки тоже
находятся.

**Зависимости:** `tree-sitter` 0.27 + `tree-sitter-{rust 0.24, typescript 0.23, javascript 0.25,
python 0.25, go 0.25, java 0.23}` + `tree-sitter-language` 0.1 (только ради типа `LanguageFn` —
в графе он и так есть через каждую грамматику) + `pulldown-cmark` 0.13 без default-фич.
`cargo tree -i tree-sitter-language`: одна версия на все семь крейтов.

**Два теста против тихой деградации, которую иначе ничего не ловит.** Опечатка в имени узла —
не ошибка нигде: обход просто никогда его не встречает, и язык молча теряет вид символов.
Поэтому каждое имя из таблицы проверяется через `id_for_node_kind` самой грамматики. И каждая
грамматика грузится в этот рантайм — несовпадение ABI дало бы «ноль символов» для всех файлов
языка, неотличимое от файлов, где их нет. Оба теста — страховка на обновление крейтов.

**Замер на этом репозитории** (release, 147 файлов, 93 мс): Rust 60 файлов → 1 459 символов,
пустых нет; TypeScript 22 → 111 (пустые: `vite.config.ts`, `vite-env.d.ts`, сетап тестов);
TSX 22 → 54 (пустые: `main.tsx` и тест из одних вызовов `test(...)`); Markdown 11 → 112.
Все пять пустых — честно без объявлений. **Для F-5.5:** кодовый файл без символов обязан
падать в чанки по целому файлу, а не в ноль чанков.

**Мутации: девять, все пойманы.** TSX через грамматику TypeScript (первая фикстура это
**пропустила** — TS-грамматика восстанавливается после простого JSX и находит те же имена;
новая, с фрагментом `<>…</>` и `.map`, даёт ноль символов вместо двух); обход заходит в листья;
любой `const` — символ; опечатка в имени узла; нет фолбэка на `type` для `impl`; диапазон по
имени, а не по объявлению; `Code`-спаны выпали из имени заголовка; строки с нуля; Java получила
грамматику Go.

**F-5.3c: поиск по ключевым словам и отметки об эмбеддингах.**

**Знак переворачивается на выходе, и это не косметика.** `bm25()` в SQLite возвращает
отрицательное значение — меньше значит лучше, — поэтому сортировка по возрастанию. Все остальные
тиры этого поиска отдают «больше значит лучше», а тир, который их соединяет, **умножает**:
отрицательное число, доехавшее туда, превратит буст в штраф и утопит ровно те результаты, ради
которых буст и делался. Тест проверяет обе половины — что лучший результат первый и что его
оценка положительная; без второй половины мутация «убрать минус» проходит незамеченной.

**Веса колонок `0.0, 4.0, 1.0`.** `chunk_id` — `UNINDEXED`, совпасть не может. Имя чанка стоит
четырёх попаданий в тело: это то, ради чего имя вообще отдельная колонка, — запрос-идентификатор
должен попадать в то, что носит имя, а не в прозу, его упоминающую. Тест ставит два чанка с
одинаковым телом, различающиеся только наличием термина в имени.

**Отметка об эмбеддинге пишется отдельно от строки чанка, и порядок имеет значение.** Сначала
вектор ложится в файл модели, потом появляется запись. Синк, убитый между ними, оставляет чанк
без записи — следующий прогон это заметит и переделает. Обратный порядок оставил бы запись о
векторе, которого никто не писал, и искать его больше никто не пойдёт.

**Ключ составной — (чанк, модель).** Один чанк может быть вложен двумя моделями одновременно, и
это то, что делает смену модели дешёвой в откате: `clear_embeddings` забывает одну и не трогает
вторую. Три теста на то, что `delete`, `clear` и загрузка не путают модели между собой.

**Что не переносится:** `has_unassigned_embeddings` и `assign_unassigned_embeddings` — чистые
помощники миграции для апстримных баз, писавшихся до появления колонки `model_id`. Таких баз
здесь нет.

**Мутации: восемь, все пойманы.** Знак не переворачивается; имя весит как тело; сортировка от
худшего; лимит игнорируется; удаление по чанку без модели; очистка одной модели чистит все;
загрузка отдаёт чужие модели; битая запись роняет загрузку.

**Стор закрыт.** `index_store.rs` — 40 тестов, 26 мутаций за три фичи.

---

**F-5.3b: запись чанков, строк FTS и символов.**

**Два метода записи чанков, и разница между ними — это те самые два номера версии в действии.**
`replace_chunks_for_file` удаляет и вставляет заново: файл, который изменился, сдвигает все
байтовые диапазоны после правки, так что уцелевшие чанки — те, что до неё, и искать их дешевле
не выходит. Удаление строк чанков каскадом уносит их векторы, и это правильно: вектор описывает
диапазон, которого больше нет.

`replace_derived_for_file` переписывает только дешёвую половину — имя чанка и строку в FTS, —
не трогая ни хэши, ни `embeddings`. Стор, у которого строки чанков верны, а векторы считались
час, не должен терять ни то, ни другое из-за смены токенизатора. Два теста стоят парой: один
проверяет, что дешёвый путь векторы сохраняет, второй — что обычный их сносит. Без второго
первый доказывает только то, что метод ничего не делает.

**Осиротевшая строка FTS.** Файл, правленный со времени последнего синка, пересобирается в
другие диапазоны и другие id — то есть чанк, пришедший в дешёвый путь, может не совпасть ни с
одной сохранённой строкой. Вставить строку FTS всё равно значит оставить ту, на которую никто не
указывает: её нельзя найти, значит нельзя удалить, и она совпадает с запросами вечно. Поэтому
`UPDATE ... WHERE chunk_id` возвращает 0 строк — и только что вставленная строка FTS удаляется
тут же.

**Чанки для неизвестного файла отвергаются внешним ключом**, а не записываются «висячими» — по
той же причине: ничьи строки никогда не перечитываются и никогда не удаляются.

**Нечитаемая строка чанка пропускается, а не роняет загрузку.** У апстрима неизвестный язык или
вид даёт `Err` на весь `load_all_chunks`; это превращает одну устаревшую колонку в индекс,
который не открывается. Здесь — тот же выбор, что в `load_all_files`: файл будет перечитан, что
диф и так собирался сделать.

**Текст живёт только в FTS.** Реляционные таблицы не хранят второй копии репозитория; сниппет
читается из файла в момент выдачи. Имя чанка кладётся в FTS рядом с текстом — это колонка,
которую BM25 взвешивает выше тела, и ради которой у документации вообще появились хлебные
крошки.

**Символы заменяются целиком, включая случай «символов не осталось».** У них нет собственного
id, так что устаревшую строку потом не отличить от свежей; отдельный тест на то, что файл,
потерявший последний заголовок, теряет его и в индексе.

**Мутации: восемь, все пойманы.** Старые строки FTS остаются при перечанковке; перечанковка
дописывает вместо замены; дешёвая перестройка идёт разрушительным путём; несовпавшая строка FTS
остаётся сиротой; нечитаемая строка роняет всю загрузку; повторный разбор дописывает символы;
имя чанка не доезжает до FTS; `ChunkKind` переименован в одну сторону.

---

**F-5.3a: скелет стора — схема, две версии, файлы.**

**Здесь всё производное, и это меняет смысл отказа.** Файл чата — написанное пользователем:
потерянный воспроизвести некому, поэтому `chat_store` не удаляет то, что не смог прочитать, а
более новый формат не трогает вовсе. В сторе индекса ни одной такой строки: всё пересобирается
из рабочего дерева. Поэтому стор, не совпавший с ожиданиями сборки, **выбрасывается и строится
заново** — включая написанный *более новой* сборкой, что делает откат версии обычным делом.
Пересборка стоит минут; схема, которую сборка читает неправильно, стоит тихо неверных ответов.
Тест на обе стороны этого различия стоит рядом.

**Миграций нет, и это не упущение.** У апстрима их две, обе — под базы, уже лежащие на реальных
машинах до появления колонки. Этот стор никогда не выходил; миграция под состояние, которого не
было, — ветка, которую никто не может проверить.

**Страж версий переехал в `open()`.** У апстрима он в `embedding_state::repair_stale`, то есть
порядок вызовов надо помнить. Здесь открывшийся стор — стор, которому можно верить, и забыть
нечего. Плюс стор после очистки **переклеймляется**: иначе он очищался бы при каждом открытии, а
выглядело бы это как индекс, который не строится.

**Две версии делают разные вещи, и тест это фиксирует.** `CHUNK_VERSION`/`INDEX_VERSION`
обнуляют всё, включая векторы; `DERIVED_VERSION` просит только пересчитать имена чанков и строки
FTS — одно чтение на файл, все векторы остаются. Мутация «дешёвая версия тоже чистит стор»
ловится именно поэтому.

**Виртуальная таблица не участвует в каскадах.** `chunks_fts` не принимает внешний ключ, так что
ни `ON DELETE CASCADE`, ни `DELETE FROM files` до неё не доходят. Строка, пережившая свой файл,
продолжает совпадать с запросами — результат, указывающий в никуда, хуже отсутствия результата.
Поэтому `delete_files` чистит её явно, через `chunks.fts_rowid`, а `wipe` перечисляет её
отдельно. Две мутации на это.

**Имена языков в базе записаны руками**, а не выведены из `Debug`: переименование варианта в Rust
иначе молча осиротит все уже записанные строки. Мутация «`Tsx` пишется как `typescript`» ловится.
Неизвестное имя языка при чтении **пропускается**, а не превращается в `PlainText`: файл просто
прочитается заново, что диф всё равно собирался сделать.

**Битый хэш не роняет чтение.** Апстрим делает `expect("hash column is always 32 bytes")` — это
правда про каждую строку, которую пишет его код, и неправда про файл, который чей-то бэкап
восстановил наполовину. Здесь такая строка пропускается.

**Мутации: десять, все пойманы.** Стража версий нет вовсе; дешёвая версия чистит стор; очистка
без переклеймления; строки FTS остаются после удаления файла; `wipe` забывает FTS; битый хэш
роняет чтение; неизвестный язык становится PlainText; один файл векторов на все модели; upsert
добавляет строку вместо замены; язык переименован в одну сторону.

---

**F-5.2: имя репозитория, под которым лежит его индекс.**

**Индекс не живёт в репозитории.** Он в каталоге приложения, в папке с именем репозитория:
рабочее дерево остаётся чистым от базы, которую никто не просил, а работа переживает
`git clean`. Для этого нужно имя — это и есть модуль.

**Личность — это remote, а не ревизия.** Переключение веток, pull, коммит не меняют, в какую
папку отображается репозиторий. Иначе фича бесполезна ровно на том сценарии, ради которого
существует. Меняется с содержимым хэш чанка, слоем выше. Тест на это есть: коммит не двигает
идентификатор.

**Два клона делят папку сознательно** — чанки ключуются хэшем содержимого, так что клон,
проиндексировавший файл первым, экономит работу второму.

**Что решено иначе, чем у апстрима:**

- **`revision` не переносится.** У апстрима он лежит в структуре с пометкой «информационно, в
  идентификатор не входит». Читателя у него нет; появится место в метаданных стора — добавится
  одной строкой (F-5.9).
- **`remote_url()` не переносится** — он для ссылок на веб-интерфейс (`domain::git_browse`),
  которого здесь нет.
- **Хэш blake3, а не SHA-256.** Это имя папки, а не граница безопасности, и blake3 уже есть в
  зависимостях ради хэшей чанков. Одной зависимостью меньше.
- **Запасной вариант — канонический путь, а не UUID в файле внутри проекта.** Апстрим пишет
  сгенерированный идентификатор в `.atlas/` внутри репозитория; это файл в чужом рабочем дереве
  навсегда ради экономии одной переиндексации. Перемещённая папка переиндексируется — дёшево и
  редко, а осиротевшая папка собирается вместе с остальным кэшем.
- **Несколько remote без `origin` → откат на путь.** Апстримный `default_remote_name` в этом
  случае возвращает ошибку (он писался под push, где угадать не тот remote разрушительно).
  Здесь угадывание означало бы тихо разделить индекс с репозиторием, которого пользователь не
  называл; путь даёт приватный индекс. Форк с `origin` + `upstream` при этом работает: `origin`
  выигрывает всегда.

**Мутации: шесть, все пойманы.** Личность всегда по пути (клоны перестают делить индекс);
учётные данные остались в идентификаторе; лоуэркейс всего URL, а не только хоста; `origin`
проиграл порядку сортировки; неоднозначные remote угаданы; суффикс `.git` сохранён.

---

**F-5.1: типы индекса, и один дефект починен, а не перенесён.**

**`detect_language` возвращает `Language`, а не `Option<Language>`.** Это вся суть фичи.
Апстрим отвечает `None` и `RepositoryIndex::update_file` **удаляет файл из индекса**; здесь
отвечать нечем — файл без разбора это `Language::PlainText` и чанки по целому файлу. Контракт
«прошёл отбор — получил чанки» держит тип возврата, а не память каждого вызывающего.

**Языки: PlainText, Json, Yaml, Markdown, Rust, TypeScript, Tsx, JavaScript, Python, Go, Java.**
Java — не наследство апстрима, а решение владельца: Spring на бэке и React на фронте это и есть
типичный enterprise-репозиторий, и без неё половина такого проекта резалась бы по счётчику байт.
Индексер при этом свой (F-5.4), апстримный не переносится.
`.tsx` — отдельный вариант, а не флаг: он не разбирается грамматикой TypeScript, и склеенные
вместе они дали бы каждому React-компоненту дерево из ошибок и ноль символов — что снаружи
неотличимо от пустого файла.

**Грамматики — официальные крейты напрямую.** Пак `ast-grep-language` (27 языков одной
зависимостью) рассматривался и отклонён: он существует ради конфликта версий, которого больше
нет (пункт 9 выше), а стоит `ast_grep_core` — движок паттернов, обёртка над AST, мета-переменные
— в графе сборки ради `tree_sitter::Language`, который грамматика отдаёт одной строкой. Плюс его
`conditional_lang!` при выключенной фиче компилируется и падает `unimplemented!()` **в рантайме**,
а не ошибкой сборки. Пересмотреть, если языков станет больше десятка.

**`SymbolKind` и половина `ChunkKind` не переносятся.** У апстрима шесть видов символов, пять из
которых порождает только его Java-индексер, и четыре вида чанков. Здесь `Symbol` без поля вида,
`ChunkKind` из двух вариантов — секция и целый файл. Ветка, которую никто не конструирует, это
ветка, под которую кто-то пишет `match` и никогда не видит его срабатывания. Структурный
индексер кода привезёт свои виды вместе с собой.

**B-1 починен в том месте, где он живёт.** Апстримный `spans_from_forward_gap_symbols`
(прозаический вариант — тот, что здесь и нужен) берёт `end = anchors[i + 1].start_byte` и
доверяет, что якоря пришли отсортированными. Ничто этого не гарантирует: инвариант держится
дисциплиной каждого индексера. Теперь якоря упорядочиваются внутри функции, а якорь, который не
может начать спан (вложенный, совпадающий по началу, начинающийся за концом файла), выбрасывается
— спан «задом наперёд» стал непредставимым, а не маловероятным. `debug_assert!` не годится: он
вырезается ровно из той сборки, где ущерб был бы тихим.

**Золотой хэш на `CHUNK_VERSION`.** Версия лежит в дайджесте чанка, чтобы её повышение
обесценило все сохранённые чанки без миграции. Мутация «убрать версию из хэша» выжила: ни один
тест этого не замечал, то есть повышение версии молча не перестраивало бы ничего. Теперь есть
тест на конкретный дайджест, и он **обязан падать при смене `CHUNK_VERSION`** — это и есть
подтверждение «все чанки на всех дисках теперь несвежие», которое иначе никто не даёт.

**Мутации: одиннадцать, все пойманы.** Якоря приняты как отсортированные (код апстрима);
вложенный якорь оставлен; якорь за концом файла оставлен; неизвестное расширение перестало быть
PlainText; `.tsx` склеен с TypeScript; `CHUNK_VERSION` выпал из хэша; хэш файла выпал из хэша;
части разрезанного чанка делят id родителя; разрез перестал искать читаемую границу; соседний
заголовок засчитан как предок; порядковые номера розданы без сортировки.

**Отложено сюда не попавшее:** предел на файл (`max_file_bytes` в `ChunkBuildOptions`) —
в F-5.5, вместе с проверкой, которая его применяет: поле настройки без исполнителя забывается
быстрее, чем появляется.

### Нарезка на фичи

| Фича | Что | ~строк |
|---|---|---|
| **F-5.1** ✅ | `domain/repo_index.rs` + `domain/chunk_index.rs` — язык, чанк, якоря, версии. Разбор ниже | 920 |
| **F-5.2** ✅ | `infra/repository_identity.rs` — чьи это векторы. Разбор ниже | 205 |
| **F-5.3a** ✅ | `infra/index_store.rs` — схема, версии, файлы. Разбор ниже | ~450 |
| **F-5.3b** ✅ | То же — запись чанков и строк FTS, символы. Разбор ниже | ~450 |
| **F-5.3c** ✅ | То же — `search_bm25` и отметки эмбеддингов. Разбор ниже | ~480 |
| **F-5.4** ✅ | `infra/language_indexers.rs` — один generic-обход дерева + таблица «язык → грамматика + имена узлов», Markdown через `pulldown-cmark`. Разбор ниже | ~540 |
| **F-5.5** ✅ | `spans_for` + `build_chunks` в `domain/chunk_index.rs`, `services/chunk_text.rs` — текст → чанки, предел на файл, отказы с причиной. Разбор ниже | ~600 |
| **F-5.6** ✅ | `services/repo_index.rs::sync` + `IndexStore::replace_file` — синк лексического индекса; поиск без единого вектора. Разбор ниже | ~560 |
| **F-5.7** ✅ | `domain/search_query.rs` — FTS-запрос, токены кода, `weak`/`hint`. Каскад и RRF — в F-5.13 вместе с инструментом. Разбор ниже | ~470 |
| **F-5.8** ✅ | `infra/local_embeddings.rs` + веса (Git LFS) + `scripts/build-embedding-model.py` + урезанный `domain/embeddings.rs`. Разбор ниже | ~330 |
| **F-5.9** ✅ | векторы строками стора (int8) + `services/embedding_index.rs`, поиск перебором; `usearch` не перенесён. Разбор ниже | ~700 |
| **F-5.10** ✅ | `services/index_sync.rs` (`RepoIndexer`) + `domain/workspace_index.rs` (`IndexEvent`) — синк одного репозитория, один за раз, поиск во время синка. Тиры и backlog не нужны. Разбор ниже | ~450 |
| **F-5.11** ✅ | `infra/file_watcher.rs` — один наблюдатель вместо двух, сигнал без путей, задний фронт (B-12). Разбор выше | ~110 + тесты |
| **F-5.12** ✅ | `services/workspace_index.rs` + `commands/workspace_events.rs` — владелец `RepoIndexer` на открытый проект (путь стора через `repository_id` + `app_dir`, каталог ресурсов модели), `spawn_blocking`, события в Tauri, `FileWatcher::start(root, || indexer.sync(..))` (F-5.11). Стор — вне корня проекта: иначе его запись будит наблюдатель на лишний пустой синк. `domain/workspace_index.rs` уже есть (F-5.10) | ~100 + тесты (вместо ~500) |
| **F-5.13** | Также — решение по словарю модели по частоте (предполагаемое из F-5.8c). `tools/semantic_search.rs` (каскад тиров, `fuse_rrf`, выбор тира при сбое — перенесены сюда из F-5.7) + `commands/{search,repo_index,embeddings}.rs` + UI, включая счётчик пропусков из `SyncReport.skipped`; замер качества на настоящем репозитории | ~1100 |

Три фичи (F-5.3, F-5.10, F-5.12) крупнее, чем «одна небольшая фича», и делятся при подходе —
швы названы в их строках.

### Что нельзя потерять (проверенный список)

1. **Контракт «прошёл отбор — получил чанки».** `detect_language = None` не может быть причиной
   удаления файла из индекса. Формулируется в доке и фиксируется тестом (B-8).
2. **Ранний выход в инкрементальном тике**: сохранение файла не должно конструировать провайдера
   эмбеддингов — иначе на проекте без индекса каждое сохранение пишет строку ошибки в лог.
   ✅ **F-5.8a/F-5.10:** конструирование `LocalEmbeddings` бесплатно, модель грузится на первом
   `embed`, а синк без изменившихся чанков не зовёт `embed` вовсе — тест
   `a_sync_with_nothing_new_never_asks_the_model`; пустой индекс не эмбеддит и запрос.
3. **Предел на файл** в `ChunkBuildOptions` (порядок 2 МиБ), проверяемый **до чтения**: один
   минифицированный бандл иначе даст больше FTS-строк, чем весь рукописный корпус. Проверить,
   что нарезка на 16 КиБ не превращает большой файл в тысячи чанков.
   ✅ **F-5.5:** 1 МиБ (как у `grep`), `read_source` проверяет по `stat` до чтения; файл у предела
   режется максимум на ~64 чанка.
4. **Явный список исключений** (`is_index_excluded`) — данными, а не ветвлением. Замер апстрима
   на его репозитории: 944 файла под git против 27 808 в `node_modules` и 84 887 в `target`.
   Здесь `.gitignore` уже учитывается `workspace_scanner`, но список нужен отдельно: ключи,
   бинарники, сверхбольшое.
5. **Каждый пропуск имеет видимую причину.** Счётчик «сколько файлов пропущено и почему» —
   формат, скрытый, по `.gitignore`, слишком большой. Тихий пропуск неотличим от пропавшего
   файла, и это главная жалоба, которую слой порождает.
   ◐ **F-5.5/F-5.6:** причины есть (`SourceSkip`), и синк отдаёт их пофайлово в
   `SyncReport.skipped`; показать в UI — F-5.13.
6. **Два номера версии:** `CHUNK_VERSION`/`INDEX_VERSION` (дорогой слой) и `DERIVED_VERSION`
   (пересчитываемое из исходников). Починка второго не сносит первый.
7. **Инвариант «якоря не пересекаются»** — чинить в `spans_from_backward_gap_symbols`
   сортировкой и пропуском вложенных, не дисциплиной вызывающего (B-1). После F-5.4 символ
   внутри функции невозможен, но член внутри контейнера (метод в классе) — норма: стратегия
   должна резать класс по членам, а не выбрасывать их как «вложенные».
   ✅ **F-5.5:** `spans_from_declarations` — якоря сортируются, остаются только самые внутренние;
   тест `no_arrangement_of_symbols_produces_a_bad_span`.
8. **Первый синк не блокирует окно.** Тиры `split_sync_tiers` уже отправляют всё сверх
   25 изменённых файлов в фоновый backlog с прогрессом через `ProgressSink`. Не менять, покрыть
   тестом.
   ✅ **F-5.10, другим способом:** тиры существовали, потому что эмбеддинг был медленным; здесь весь
   апстрим эмбеддится за 0,6 с. Синк идёт целиком на пуле потоков, прогресс — через
   `IndexEventSink`, а поиск работает всё время: по словам — с `KeywordsReady`, по смыслу — по уже
   готовым батчам. Тест `during_a_sync_requests_fold_in_and_search_answers_from_what_is_done`.
9. ~~`tree-sitter` тянет `links = "tree-sitter"` — одна версия на весь граф сборки.~~
   **Устарело, проверено 2026-09-17.** Современные грамматики (`tree-sitter-rust` 0.24.2,
   `tree-sitter-typescript` 0.23.2) зависят от `tree-sitter-language = "0.1"` и **не объявляют
   `links` вовсе**. Апстрим напоролся на старую раскладку: `tree-sitter-asciidoc` требовал ABI
   15, `tree-sitter-kotlin` был закрыт на `<0.23`, и одна версия на граф сделала их
   несовместимыми — отсюда выброшенный Kotlin. Здесь ограничения нет; остаётся обычная
   дисциплина «не тащить экзотику».

**Отложено с явным триггером:** query-driven извлечение символов через `tags.scm`
(05-gaps §5.5). Рукописный `java.rs` не перенесён — его заменил общий обход с таблицей (F-5.4),
который и есть дешёвая половина идеи `tags.scm`. Переход на запросы — когда таблице не хватит
выразительности (символ определяется не видом узла, а его окружением); решается в F-5.13 по
живому поиску.

**Готово, когда:** `semantic_search` на этом репозитории находит по смыслу — включая `.rs` и
`.ts`, а не только `.md`; ни один файл не исчезает из индекса без причины в счётчике; повторный
синк после правки одного файла не переэмбеддит корпус; первичная индексация не держит открытие
папки. Профилирование — на настоящем репозитории кода, а не на фикстуре (05-gaps §5.7.5).

---

## Этап 6. Skills, память, планы, наблюдаемость

Закрывает вкладки `AsidePanel`, которые сейчас на моках (`skills`, `rules`).

- `domain/agent_skills.rs` (505), `services/agent_skills.rs` (635), `tools/skill.rs` (135) —
  формат `SKILL.md`, слияние встроенного и пользовательского каталогов, **ленивая загрузка
  тела по требованию модели**. Почти без правок.
- `services/plans.rs` + `domain/plan.rs` + `tools/plans.rs` — с правкой под кодинг.
- `services/agent_memory.rs` (537), `domain/memory_policy.rs` (742),
  `services/memory_pipeline.rs` (440). Переносить целиком, **включить по умолчанию
  выключенной**: автозапоминание фактов в кодинг-агенте — осознанный выбор пользователя.
  Не потерять `MemoryExtractGuard` (не извлекать дважды из одного чата) и дедуп новых
  фактов по близости к существующим.
- `infra/tool_call_log.rs` (613) + `domain/tool_call_log.rs` (58) + UI лога.
  `redact_args`/`redact_result` вырезают содержимое файлов, включая контекстные строки
  grep'а — именно это позволяет держать лог включённым по умолчанию (CA-12.1).
- Файл проектных инструкций в репозитории (аналог `AGENTS.md`), подхватываемый
  автоматически (CA-9.3).

**Готово, когда:** вкладки `skills`/`rules` показывают реальные каталоги с переключателями;
лог вызовов открывается и фильтруется; в записях лога нет содержимого файлов.

### Прогресс этапа (на 2026-09-18)

| | Фича | Итог |
|---|---|---|
| ✅ | F-6.1 | skills: `SKILL.md` из `~/.atlas-desktop/skills`, каталог в промпте, инструмент `skill` грузит тело и файлы по требованию |
| ✅ | F-6.2 | вкладка `skills`: реальный список, сломанные с причиной, переключатели (`skills.disabled` в настройках) |
| ✅ | F-6.3 | `AGENTS.md`/`CLAUDE.md` из корня папки — в промпте каждого хода; вкладка `rules` с переключателями (CA-9.3) |
| ✅ | F-6.4 | лог вызовов: каждый вызов хода (и отказ до запуска, и отказ пользователя) — в `tool_calls.db`, без содержимого файлов (CA-12.1) |
| ⏭ | F-6.5 | окно лога: открыть, фильтр по инструменту и исходу |
| | F-6.6 | планы: `services/plans.rs` + инструмент, с правкой под кодинг |
| | F-6.7 | память: `agent_memory` + `memory_policy` + `memory_pipeline`, **выключена по умолчанию** |

Порядок — по «Готово, когда»: сначала обе вкладки на моках (F-6.2, F-6.3), затем лог (F-6.4,
F-6.5), затем то, что меняет поведение агента (планы, память).

Тестов в Rust после F-6.3: 787 + стенд качества; во фронтенде — 142. Обе вкладки на моках
закрыты — первая половина «Готово, когда» этапа выполнена.

Тестов в Rust после F-6.4: 799 + стенд качества; во фронтенде — 142.

**F-6.4: лог вызовов инструментов — всё, что делал агент, без того, что он читал (CA-12.1).**

`~/.atlas-desktop/tool_calls.db` (SQLite, WAL): время, папка, раунд, провайдер и модель,
инструмент, аргументы, исход (`ok`/`error`/`denied`), ошибка, результат, длительность.
Хранится 30 дней, лишнее удаляется при записи. Запись — best-effort: недоступный диск
теряет строку лога, а не вызов. `query` (фильтр по папке, инструменту, исходу, поиск по
инструменту/ошибке/аргументам, страница + общий счёт) и `clear` готовы; команды и окно — F-6.5.

- **Чистка — по имени поля во всём значении, а не по списку инструментов.** Апстрим
  перечислял для каждого инструмента, какие поля несут текст; инструмент без строки в этом
  списке писал содержимое целиком, и ничего не падало. Здесь поле с именем из
  `CONTENT_FIELDS` (`content`, `text`, `before`, `after`, `old`, `new`, `unifiedDiff`,
  `stdout`, `stderr`, `instructions`) заменяется где угодно: новый инструмент, назвавший
  текст `content`, покрыт без чьей-либо памяти. А `every_tool_is_redacted` — исчерпывающий
  `match` по `ToolName` с меткой в каждом поле текста: новый инструмент не скомпилирует тест,
  пока кто-то не решит, что в нём содержимое. Остаются пути, строки, счётчики, код выхода,
  командная строка `runCommand`.
- **Три утечки мимо полей.** `EditTextNotFound`/`EditTextAmbiguous` несут текст правки в
  сообщении ошибки — пишется без него. Ошибка аргументов serde цитирует значение
  (`invalid type: string "…"`) — `edits`, присланный строкой, это вся правка; сообщение
  режется до первой кавычки. Аргументы, которые не разобрались, не пишутся вовсе (`null`):
  сырой JSON чистка не видит, а сломанный `writeFile` — это файл.
- **Пишется каждый вызов, включая отказы до запуска** — выход за папку, запись без чтения,
  инструмент не из режима: ради них аудит и читают. Отказ пользователя — `denied` с его
  причиной. Вызов на паузе подтверждения не пишется, пока не решён.
- **Запись — через порт `Turn::log_call`**, а не прямо в `infra`. Не ради архитектуры:
  тесты хода не держат блокировку каталога приложения и писали бы в каталог, установленный
  чужим тестом, — прямо посреди теста лога, считающего строки.
- **Не сделано, с триггером:** выключатель лога (апстрим — `LlmSettings.tool_call_logging`,
  включён по умолчанию) — вместе с окном в F-6.5; поле `source` апстрима (`chat`/
  `standalone`) — у нас инструменты вызывает только ход.

**Мутации: шестнадцать, все пойманы** (одна со второй попытки: поиск не проверял, что `%`
в запросе — буква, а не «что угодно»).

**Правка системного промпта (после F-6.3), по варианту пользователя.** Добавлены разделы
«Safety and irreversible actions» и «Git and repository state» (без просьбы — ни коммита, ни
push, ни reset; рабочее дерево остаётся изменённым), отдельный учёт падений «было до меня /
вызвано мной», финальный отчёт, вывод инструментов — тоже данные, а не инструкции. При
переносе варианта исправлено шесть мест: возвращена оговорка про инструкции проекта (без неё
F-6.3 спорит с «репозиторий — данные»); «mark blocked» заменено на cancelled — такого статуса
у `todo` нет, и модель получила бы ошибку инструмента; запрет на пакеты и отсылка «скажи,
какой пакет» сведены в одно «спроси и отчитайся о lock-файле», а «сеть» сужена до того, что
выходит за сборку и тесты проекта; четырёхчастная преамбула перед каждым одобрением —
до одной фразы «зачем» (карточка уже показывает вызов и цель); шаблон отчёта — только для
ходов, которые что-то меняли (иначе «Nothing changed» на каждый вопрос в Ask); дубль
«Stay inside the open folder» убран.

**F-6.3: инструкции проекта — `AGENTS.md` и `CLAUDE.md` в промпте, вкладка rules (CA-9.3).**

Новое, не перенос: в апстриме «правила» — стандарты документации, которыми проверялся
документ, а не текст для модели. Из корня открытой папки читаются `AGENTS.md` и `CLAUDE.md`
(в этом порядке, оба, если есть оба) и идут отдельным системным сообщением «Project
instructions», каждый под своим именем, — после skills и перед фактами хода: меняется, когда
правят файл, а не от раунда к раунду. Читается раз за ход, как каталог skills.

- **Противоречие в промпте снято явно.** `INSTRUCTIONS` говорит: всё в репозитории — данные,
  а не инструкции. Без оговорки у модели два правила, которые спорят; теперь исключение
  названо, и границы его тоже: соглашения проекта исполняются как пользовательские, но ни
  доступа, ни снятия подтверждений, ни отправки куда-либо они не дают. Где файл спорит с
  просьбой в разговоре — побеждает просьба.
- **Предел — 20 000 символов на файл**, резка по символу (кириллица не падает на границе
  байта). Обрезанный файл кончается пометкой, где он обрезан и что остальное читается
  `readFile`: иначе модель принимает начало за весь файл. Этот репозиторий — 11 300 символов
  на два файла, ~3 тыс. токенов на запрос.
- **Симлинки.** `CLAUDE.md → AGENTS.md` — один файл, отправляется один раз (сверка по
  канонпути). Ссылка за пределы папки не отправляется: текст уходит провайдеру на каждом
  запросе, а с ним ушёл бы чужой файл; во вкладке она видна с причиной и без переключателя.
  Так же — файл не в UTF-8.
- **Переключатель — по файлу, не по имени** (`settings.json` → `rules.disabled`,
  канонические пути): выключить `AGENTS.md` одного репозитория — не выключить его у всех.
  Хранилище выключенного общее со skills — `domain::settings::OptOut`, вместо двух
  одинаковых структур. Повреждённый `settings.json` ход читает как «ничего не выключено»,
  а переключатель отказывается, не затирая настройки.
- **Вкладка:** имя, число строк, «cut», если обрезан; в раскрытии — текст, который уходит
  модели, и путь. Перечитывается при открытии и при смене папки. Пусто — «No AGENTS.md or
  CLAUDE.md at the root of the open folder».
- **Не сделано, с триггером:** вложенные `AGENTS.md` в подпапках (монорепо: ближайший к
  файлу главнее) и `@file`-импорты `CLAUDE.md` — когда встретится репозиторий, которому это
  нужно. В этом репозитории `CLAUDE.md` импортирует `@AGENTS.md`, но `AGENTS.md` и так
  читается целиком — строка импорта безвредна.

**Мутации: пятнадцать, все пойманы** — двенадцать в Rust, три во фронтенде.

**F-6.2: вкладка skills — список папки и переключатели.**

Вкладка показывает каждую папку каталога skills: имя, описание, переключатель; счётчик —
«включено/всего». Сломанный скилл — строкой с именем папки, пометкой «invalid» и причиной
(«SKILL.md is missing YAML frontmatter»), **без переключателя**: к модели он не попадёт
никак, и включать нечего. Пустая папка говорит, куда класть скиллы.

- **Opt-out, по имени:** `settings.json` → `skills.disabled`. Скилл, положенный в папку,
  включён, пока его не выключили; имя, чьей папки уже нет, ничего не стоит. У апстрима ключ
  был `источник:имя` — источник у нас один.
- **Инструмент грузит только то, что перечислил промпт хода.** `ToolDeps.skills` — каталог
  этого хода; имя вне его — «no skill named … — use a name from the Skills list», даже если
  папка на диске есть. Иначе выключатель держался бы только на том, что модель не угадает
  имя (или не вспомнит его из старого чата). Заодно скилл, добавленный посреди хода, до
  следующего хода не грузится — промпт и инструмент не расходятся.
- **Выключение не портит настройки.** Каталог для хода читает повреждённый `settings.json`
  как «ничего не выключено» — ход не падает; а переключатель на таком файле **отказывается**
  с ошибкой: сохранение поверх затёрло бы всю конфигурацию значениями по умолчанию.
- **Список перечитывается при каждом открытии вкладки** — скиллы правят вне приложения. Пока
  вкладка скрыта, диск не трогается. Переключатель срабатывает сразу, затем список
  перечитывается: если сохранение не удалось, вкладка показывает ошибку и то, что на самом
  деле сохранено.
- **`ItemList` стал управляемым**: переключатель показывает `item.enabled` и сообщает
  `onToggle`; без `onToggle` переключателя нет. Раньше он держал своё состояние и никуда
  его не отдавал — выглядел рабочим и ничего не делал.
- Кнопки «Install skill» больше нет: установка — это папка в каталоге, и пустая вкладка
  называет его. Импорт папки и удаление (апстрим умел) — когда понадобятся.

**Мутации: двенадцать, все пойманы** — восемь в Rust (одна со второй попытки: тест хода
вызывал `listFiles`, а не `skill`, и не видел, что каталог не дошёл до инструмента), четыре
во фронтенде.

**F-6.1: skills — каталог в промпте, тело по требованию.**

Скилл — папка `~/.atlas-desktop/skills/<name>/` с `SKILL.md` (формат agentskills.io:
YAML-шапка `name`/`description`, дальше markdown) и любыми файлами рядом. Модель видит
в промпте только имена и описания; тело приходит вызовом `skill {name}`, файл рядом —
`skill {name, path}`.

- **Поиска нет — каталог в промпте.** Главное расхождение с апстримом. Там модель каталога не
  видела: скилл существовал, только если его находил подстрочный поиск без стемминга, и апстрим
  держал тест со списком фраз, на которые каждый скилл обязан откликаться («задачу» и «задача» —
  разные слова). Здесь каталог — собственные скиллы пользователя, их немного, и модель выбирает
  чтением, что умеет лучше подстрочного матча. Отдельное системное сообщение между режимом и
  фактами хода: меняется, только когда пользователь правит папку, — оно в кэшируемом префиксе.
  Описания — до 8 000 символов суммарно, дальше скиллы перечисляются одними именами: каждый
  остаётся достижим, а текст перестаёт расти с папкой. Пустая папка — ни заголовка, ни строки.
- **Встроенного каталога нет.** Пять встроенных скиллов апстрима — про документацию, ни одного
  про код; `SkillSource` с одним вариантом ничего не различает. Вернётся с первым скиллом,
  который стоит поставлять.
- **`requires-project` не перенесён** — любой ход здесь идёт в открытой папке.
- **Шапка — настоящий YAML** (`yaml_serde`, как в апстриме). Скиллы других агентов пишут
  длинное описание через `>`; построчный разбор прочёл бы описание как один символ. Неизвестные
  поля спецификации (`license`, `metadata`, `allowed-tools`) принимаются и игнорируются —
  скилл от другого агента грузится без правок. `allowed-tools` не исполняется: что можно
  запускать, решает гейт подтверждений, а не скилл.
- **Имя сверяется с папкой** и проверяется по спецификации (`[a-z0-9-]`, без двойных дефисов)
  **до** склейки с путём — это и есть защита от `../`. Файлы рядом — только внутри папки
  скилла: `..` и абсолютный путь отклоняются до диска, симлинк наружу — после разрешения.
- **Сломанный скилл** в каталог модели не попадает, но остаётся записью `scan()` — вкладка
  F-6.2 покажет, что с ним не так.
- **Файлы рядом** перечисляются до 100, скрытые пропускаются, обход не идёт по симлинкам на
  каталоги (ссылка на родителя — бесконечный обход).
- **Во всех режимах**, вес 1, не мутирующий. Папку, которую не удалось прочитать, ход
  переживает без скиллов: ничего из запрошенного от неё не зависит.
- **Окно:** «Skill · release · 1 file», в раскрытии — инструкции и список файлов.

**Мутации: девятнадцать, все пойманы** (одна — со второй попытки: тест на `../elsewhere` не
создавал саму папку `skills`, и путь `skills/../x` не разрешался и без проверки имени).
Не проверен только предел в 100 файлов.

---

## Этап 7. Расширения

- **Anthropic Messages API** (05-gaps §5.2). Трейт готов, реализации нет. Работа
  аккуратная, не концептуальная: блоки `thinking` и правила их сохранения в истории,
  `tool_use`/`tool_result` как блоки контента (а не роль сообщения), системный промпт
  отдельным полем, явные точки кэширования. Тест: turn с инструментами переживает
  round-trip без потерь.
- **Кэширование промпта** — в Atlas нет ни для одного провайдера, а на длинных сессиях
  кодинг-агента это крупнейшая статья экономии.
- **MCP-клиент** (05-gaps §5.3). Встраивать **внутрь** `execute_tool`, не рядом:
  MCP-инструменты обязаны проходить тот же allowlist, тот же гейтинг подтверждений и тот
  же лог с редактированием, иначе получится вторая неохраняемая дверь. Решить до
  реализации: неймспейс сервера в allowlist, поведение при отвале сервера посреди turn'а,
  `loop_weight` для инструмента неизвестной стоимости. Вкладка `mcp` уже нарисована.
- Хуки жизненного цикла (до/после вызова инструмента, конец turn'а).
- Фоновые процессы (CA-4.5) — вкладка `terminal` под это уже есть.
- Ветвление сессии от произвольного сообщения — хранилище чатов линейное.
- Явная политика «куда уходят данные» (CA-12.6).

---

## Проверка

После каждой фичи:

```bash
cd src-tauri && cargo check && cargo test
```

```bash
bun run tsc --noEmit && bun test
```

С этапа 3 — в живом приложении:

```bash
bun run tauri dev
```

Порт 1420 — `strictPort`: если занят, dev-сервер выходит, и `tauri dev` даёт пустое окно.
`vite-dev` из `.claude/launch.json` годится только для просмотра вёрстки — бэкенда там нет
и каждый `invoke()` падает.
